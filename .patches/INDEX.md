# Индекс уроков — 2026.06 fb2-to-epub

> Строка = правило. Читается на старте сессии вместо всех файлов (правило HANDOFF §5, 2026-09-02).
> Отдельный урок открывай, только если он относится к текущей задаче. Стабильное правило → в устав агента/скилл, урок помечается «встроено».

| Файл | Тема | ПРАВИЛО НА БУДУЩЕЕ |
|---|---|---|
| `001-dmg-verify-real-render.md` | 001 — DMG: проверять реальный рендер Finder, не только структуру | Для ЛЮБОГО артефакта с визуальным выводом (DMG-окно, установщик, UI) приёмка = РЕАЛЬНЫЙ скриншот отрендеренного результата, поэлементно, ДО «готово». Структурные проверки (байты/`ls`/`mount`) — необходимое, но НЕ достато… |
| `002-macos26-icon-rendering-cache.md` | 002 — macOS 26 (Tahoe) иконки + растеризация ассетов + кэш Finder | (раздел «правило на будущее» не найден) |
| `003-dmg-window-width-and-codesign-race.md` | Урок 003 — ширина DMG-окна на macOS 26 + гонка codesign в iCloud-папке | Фон DMG ≥ окна, контент по центру. Под macOS 26 фон рисуем 960×440 (шире/выше окна 920×408), тёмным до всех краёв; контент центрируем под видимые 920 (центр x=460, слоты иконок 290/630). Меньше окна = белый провал у коне… |
| `004-build-artifact-spotlight-duplicate.md` | 004 — Build-артефакт `.app` в репозитории → дубль приложения в Spotlight | Репо в `~/Documents`/iCloud → любой собранный `.app`/`.dmg` попадает в Spotlight и выглядит как установленное приложение. После сборки нативного приложения: держать `.metadata_never_index` в каталоге build-вывода и/или ч… |
| `005-inert-buttons-shipped.md` | 005 — В релиз попали «неживые» кнопки (prepared-but-inert M2-заглушки) | Видимый кликабельный контрол = ОБЯЗАН работать. Если действие не реализовано — контрол скрыт или явно `disabled` (с поясняющим состоянием), НИКОГДА не показан как живой. Перед каждым релизом — аудит ВСЕХ контролов каждог… |
| `006-overbuilt-update-instead-of-sibling-pattern.md` | 006 — Авто-обновление: переусложнил вместо проверенного образца человека | Если фича повторяется между проектами человека (обновление, кредит-подвал, тёмный UI-язык…) — СНАЧАЛА разведка: искать его спеку (`*-spec.md` в корне VibeCoding2) и рабочую реализацию в соседних репо (`ls ~/Documents/Vib… |
| `007-cover-queue-validate-file-existence.md` | 007 — Очередь обложек показывала удалённые книги (нет валидации существования) | Persistent-очередь/список, ссылающийся на внешние файлы (epub, документы, пути) — на входе ВАЛИДИРОВАТЬ существование цели и не предлагать действие, если объекта уже нет. Файлы пользователя живут своей жизнью (он их удал… |
| `008-research-ignores-embedded-cover.md` | 008 — «Искать ещё» не искал бы по EPUB: Calibre синтезирует обложку → has_embedded_cover==True | Действие вида «найди альтернативы / поищи ещё / перегенерируй» НЕ должно гейтиться условием «у объекта уже что-то есть» — пользователь просит замену именно потому, что текущее не подходит. Короткозамыкатели («уже есть → … |
| `009-gear-rendered-as-flower.md` | 009 — «Изящная шестерёнка» отрендерилась цветком (иконку не проверили рендером) | Любой ВИЗУАЛЬНЫЙ элемент (иконка, форма, кривая) проверять РЕНДЕРОМ крупным планом, а не описанием геометрии/кода. «8 ровных зубцов» на словах ≠ шестерёнка на экране. Для иконок — снять зум и убедиться, что читается имен… |
| `010-plain-button-no-contentshape-dead.md` | 010 — Кнопка ‹ «не работает»: Button(.plain) с прозрачным лейблом без .contentShape = неживой контрол | Любому кастомному тап-таргету (`Button`/`.onTapGesture`) с ПРОЗРАЧНЫМ фоном — ОБЯЗАТЕЛЕН `.contentShape(Rectangle())`. `.buttonStyle(.plain)` сам область тапа не создаёт. Интерактивные элементы проверять РЕАЛЬНЫМ КЛИКОМ,… |
| `011-ring-counter-flew-from-window-corner.md` | 011 — Счётчик кольца «влетал» из угла окна (identity-swap + refit-гонка) | В SwiftUI элемент (центр/оверлей), меняющийся СИНХРОННО с AppKit-refit/`setFrame` (переразмер окна), держать ОДНИМ стабильным контейнером с `.opacity`-тумблером — НЕ через `if/else` (смена view-identity → `.transition`-в… |
| `012-cover-svg-fonts-ttc-cyrillic-cairosvg.md` | 012 — SVG-обложки: Didot/Futura/Avenir дают tofu в cairosvg (кириллица) | Любой шрифт в SVG-шаблоне, который пойдёт через cairosvg, проверять фактическим рендером кириллицы (SVG→PNG → глазами/PIL), а не доверять `fc-match`/наличию глифов в файле. «В целом подхватился ≠ кириллица отрисовалась».… |
| `013-cover-window-outgrew-screen.md` | 013 — Окно выбора обложки переросло экран (фикс-высота под контент без cap) | Окно фикс-ширины, чья высота подгоняется под контент, ОБЯЗАНО иметь cap по высоте экрана (`visibleFrame.height − запас`) + прокрутку переменной части — иначе на максимуме контента нижние контролы (Применить/submit) уезжа… |
| `014-ring-angular-gradient-seam-at-start-cap.md` | 014 — Посторонний цвет в начале прогресс-кольца (AngularGradient-шов под round-cap'ом) | AngularGradient на полном круге поверх trimmed-дуги с `lineCap:.round` ВСЕГДА имеет цветовой шов °/360°; если он попадает под старт-/конец-колпачок — туда подмешивается «чужой» цвет. Решения: перекрыть старт сплошным кол… |
| `015-verify-override-leaked-into-installer-rewrote-plist.md` | 015 — Verify-override протёк в инсталлер и переписал РЕАЛЬНЫЙ plist агента человека | Тест-/verify-оверрайды НИКОГДА не вешать на геттер/путь, питающий инсталлер, запись plist, перезапуск агента или иные побочные эффекты на РЕАЛЬНОЙ системе человека. Оверрайд — строго в листе-наблюдателе (read-only) либо … |
| `016-continue-on-fail-published-corrupt-copy-stubs-missed-it.md` | 016 — «Continue-on-failure» опубликовал битую work-копию; стаб-тесты этого НЕ ловили | «Continue on failure» ≠ «опубликуй частичный результат». Если шаг мутирует файл на месте и может упасть посреди — работай на temp-копии и заменяй оригинал (`mv`) ТОЛЬКО при полном успехе шага. При сбое — удали temp, ориг… |
| `017-sudden-termination-boilerplate-killed-delegate-lifecycle.md` | 017 — NSSupportsSuddenTermination из бойлерплейта отрезал весь terminate-lifecycle | Каждый ключ Info.plist — осознанное решение, не бойлерплейт. Копируешь шаблон — объясни в комментарии каждый нетривиальный ключ или удали его. Делегатные lifecycle-пути (terminate/reopen/last-window-closed) проверяются Т… |
| `018-test-isolation-class-recurred-thrice-standing-guard.md` | 018 — Класс «тест дотянулся до боевого» всплыл ТРИЖДЫ (015 → B1 → H1): долг процесса | Каждый раннер, который бутстрапит агентов или порождает процессы с HOME-производными путями, обязан нести СНАПШОТ-СТРАЖ: зафиксировать mtime/size/hash боевых артефактов (реальный лог, state.json, covers/jobs, LaunchAgent… |
| `019-live-window-bench-blocked-by-same-bundle-id-and-own-agent.md` | 019 — Живое окно тест-сборки на боевой машине невозможно: тот же bundle-id + свой агент | Для живого окна нативного приложения на машине с установленным боевым — меняй bundle-id тест-сборки (напр. суффикс `.dev` в build-app.sh при dev-версии), иначе фронт/скриншот ловит боевой инстанс. Либо (проще) — офскрин-… |
| `020-tahoe-fda-script-grant-dead-real-not-panel.md` | 020 — Tahoe: грант скрипту мёртв + панель FDA врёт о списке (реальность > панель) | (раздел «правило на будущее» не найден) |
| `021-fda-node-fixed-three-releases-boevoy-found-all.md` | 021 — Один узел (FDA/агент) чинился ТРИ релиза подряд, и каждый раз находил БОЕВОЙ тест | Для фич, зависящих от системных политик ОС (TCC/launchd/подпись), «зелёные тесты» НЕ являются гейтом релиза. Гейт — прогон на РЕАЛЬНОЙ машине в РЕАЛЬНОМ состоянии пользователя. Планировать его как обязательный шаг ДО рел… |
