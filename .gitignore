.DS_Store
*.log
__pycache__/

# Build artifacts (binaries are not committed; .dmg ships via GitHub Releases).
# EXCEPTION: packaging/fb2-to-epub-agent IS committed on purpose — it is the
# frozen ad-hoc-signed FDA helper whose cdhash users' TCC grants are pinned to
# (see packaging/agent-src/PROVENANCE.md). Never rebuild it casually.
build/dist/
build/.venv/
*.app
*.dmg
*.dmg.sha256

# Local-only ledgers (time tracking etc.) — never committed
*.local.md
