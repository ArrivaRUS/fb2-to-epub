.DS_Store
*.log
__pycache__/

# Build artifacts (binaries are not committed; .dmg ships via GitHub Releases)
build/dist/
*.app
*.dmg
*.dmg.sha256

# Local-only ledgers (time tracking etc.) — never committed
*.local.md
