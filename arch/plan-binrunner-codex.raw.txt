Reading additional input from stdin...
2026-07-23T04:41:58.238795Z ERROR codex_models_manager::cache: failed to load models cache: missing field `supports_reasoning_summaries` at line 88 column 5
OpenAI Codex v0.144.6
--------
workdir: /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019f8d47-c52a-7363-b398-a259e057d781
--------
user
Ты архитектор. Задача v1.0.2 проекта fb2-to-epub (macOS-приложение + launchd-агент). Верни КОМПАКТНЫЙ архитектурный план БЕЗ кода: выбор варианта с обоснованием, компоненты (ответственность/зависимости/как тестировать), milestone по зависимостям, риски. Прочитай файлы: packaging/fb2-to-epub-runner.sh, packaging/installer.sh, build/build-app.sh, app/EngineClient.swift (runnerPath), app/FolderAccessCard.swift.

ДАНО (доказано tccd-логом на macOS 26.5.2 Tahoe): launchd-агент с ProgramArguments[0]=shell-скрипт получает TCC-subject /bin/bash (AUTHREQ_SUBJECT subject=/bin/bash, bundle attribution null, "Platform binary prompting is Deny"). Следствия: грант Full Disk Access, выданный строке скрипта, мёртв; грант FDA приложению агента launchd-агент не покрывает; дочерние процессы атрибутируются к responsible-процессу.

НУЖНО: ProgramArguments[0] должен стать Mach-O бинарём, чтобы FDA-грант снова матчился, И грант должен переживать авто-апдейты. Ограничения: приложение подписано ad-hoc, TeamID нет, $99-сертификата нет и не будет; авто-апдейт заменяет .app целиком.

ТЕКУЩАЯ АРХИТЕКТУРА: стабильный-по-байтам runner.sh (installer сохраняет его cmp'ом — на неизменности байтов держались старые FDA-гранты), runner.sh exec'ает обновляемый watcher.sh. UI-онбординг FDA уже есть (карточка + клипборд пути из plist ProgramArguments[0] + открытие панели Privacy + recheck) — он переиспользуется, меняется только ЦЕЛЬ гранта.

РЕШИ РАЗВИЛКУ (оцени каждый вариант точно, с механикой TCC-матчинга):
(A) standalone stable-bytes Mach-O helper в ~/Library/Application Support/.../bin: собран один раз; хранить в git как бинарный артефакт или воспроизводимая сборка? universal (arm64+x86_64) или arm64-only? снимать ли quarantine после установки? Ключевой вопрос: как TCC матчит standalone ad-hoc CLI-бинарь — по cdhash или по пути+identity? переживает ли грант апдейты приложения, если байты helper'а не меняются?
(B) helper внутри .app-бандла: bundle attribution → существующий FDA-грант приложению покрывает helper, нулевой дополнительный UX. НО ad-hoc подпись → TCC пиннит cdhash? Тогда каждый авто-апдейт (замена .app целиком, новый cdhash) молча убивает грант? Оцени точно, ЧТО пиннит TCC для ad-hoc-подписанного бандла без TeamID и что происходит с грантом при замене бандла на новый ad-hoc-собранный.
(C) SMAppService / BundleProgram: цена рефакторинга относительно текущего launchd-plist-подхода, и решает ли он вообще проблему при ad-hoc подписи.

ТАКЖЕ ВКЛЮЧИ В ПЛАН: миграция существующих установок (installer обновляет plist на новый ProgramArguments[0]); правки README и текста FDA-карточки; тест «апдейт приложения → грант жив»; риски каждого шага.

Формат вывода: 1) Вердикт-выбор (A/B/C или гибрид) + почему, с точной механикой TCC; 2) Таблица компонентов; 3) Milestones по зависимостям; 4) Миграция; 5) Тест апдейт-выживания гранта; 6) Риски. Без кода, компактно.
codex
Прочитаю указанные файлы и сверю выводы о TCC/SMAppService с актуальной документацией Apple: здесь критична точность механики, а она зависит от версии macOS. Использую только первичные источники для технической части.
exec
/bin/zsh -lc "sed -n '1,260p' packaging/fb2-to-epub-runner.sh; sed -n '1,340p' packaging/installer.sh; sed -n '1,300p' build/build-app.sh; sed -n '1,280p' app/EngineClient.swift; sed -n '1,320p' app/FolderAccessCard.swift" in /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
 succeeded in 0ms:
#!/bin/bash
# Stable FDA "responsible target" for the fb2-to-epub LaunchAgent.
#
# WHY THIS EXISTS:
#   macOS TCC attributes file-access permission (incl. Full Disk Access) to the
#   *executable named in ProgramArguments*, not to the script it runs. If the
#   agent pointed ProgramArguments at /bin/bash, the user would have to grant FDA
#   to /bin/bash itself (broad, and the grant is keyed to that binary). By giving
#   the agent its own stable runner at a fixed App Support path, Full Disk Access
#   can be granted to THIS file specifically, and the grant survives reinstalls
#   as long as the path and bytes are stable.
#
#   => Grant Full Disk Access to:
#      ~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh
#
# It is intentionally minimal: it locates the watcher next to itself and execs it,
# inheriting WATCH_DIR / PATH / EBOOK_CONVERT / PYTHON3 from the LaunchAgent's
# EnvironmentVariables. Keep this file stable (avoid churn) so the TCC grant holds.

set -u

HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
WATCHER="$HERE/fb2-to-epub-watcher.sh"

if [[ ! -x "$WATCHER" ]]; then
  echo "fb2-to-epub: watcher not found at $WATCHER" >&2
  exit 1
fi

exec /bin/bash "$WATCHER"
#!/bin/bash
# fb2-to-epub installer (all install logic lives here).
#
# Responsibilities:
#   - detect Calibre (ebook-convert) and python3 (clear message if missing)
#   - accept a WATCH_DIR (arg or env); create it if absent
#   - copy watcher + cover-finder + runner into App Support/bin
#   - generate the LaunchAgent plist via `plutil` (NOT sed) so arbitrary paths
#     with spaces / unicode are encoded safely
#   - (re)load the agent idempotently: bootout -> bootstrap -> enable -> kickstart
#   - print Full Disk Access guidance when WATCH_DIR is in a TCC-protected zone
#
# Usage:
#   installer.sh ["/path/to/watch folder"]
#   WATCH_DIR="/path/to/folder" installer.sh
# Default WATCH_DIR: ~/Desktop/fb2-to-epub
#
# Invoked both by the .app applet (do shell script) and by the advanced CLI
# install.sh. Idempotent: re-running re-points the agent without leaving dupes.

set -euo pipefail

# ---------------------------------------------------------------------------
# Тест-защёлка (урок .patches/015) — ДО констант, т.к. от неё зависит LABEL.
#
# Однажды verify-оверрайд протёк в боевой инсталлер и переписал РЕАЛЬНЫЙ plist
# агента человека. Поэтому любой тест-оверрайд здесь двухступенчатый:
#   • read-only  (подмена кандидатов детекта) — нужен FB2_CALIBRE_TEST_MODE=1
#     и существующий FB2_CALIBRE_TEST_ROOT;
#   • MUTATING   (метка агента = какой plist мы перезапишем) — нужно ЕЩЁ и то,
#     чтобы install-root ($HOME/Library/Application Support/fb2-to-epub) лежал
#     ВНУТРИ канонизированного TEST_ROOT.
# Не сошлось — переменные молча игнорируются (боевой путь неотличим от прежнего).
# Swift-близнец: app/CalibreLocator.swift → CalibreTestLatch.
# ---------------------------------------------------------------------------
calibre_test_root() {
  [[ "${FB2_CALIBRE_TEST_MODE:-}" == "1" ]] || return 1
  local raw="${FB2_CALIBRE_TEST_ROOT:-}"
  [[ -n "$raw" && -d "$raw" ]] || return 1
  local root
  root="$(cd "$raw" && pwd -P)" || return 1
  # Ужесточение защёлки (m2-близнец — parity со Swift CalibreTestLatch.testRoot):
  # слишком широкий TEST_ROOT сделал бы боевые пути «внутри TEST_ROOT», и мутирующие
  # оверрайды (метка агента, а с H1 — ещё и HOME/state/covers/log в plist) навелись бы
  # на прод. Такой TEST_ROOT — не защёлка, отвергаем:
  #   • "/" — внутри него вообще всё;
  #   • TEST_ROOT, СОДЕРЖАЩИЙ настоящий домашний каталог пользователя. Берём passwd-home
  #     (dscl NFSHomeDirectory, фолбэк `eval echo ~user`), а НЕ $HOME: изолированные тесты
  #     подменяют $HOME throwaway-путём ВНУТРИ TEST_ROOT — сверять надо с реальным домом.
  [[ "$root" == "/" ]] && return 1
  local uname phome
  uname="$(id -un 2>/dev/null || true)"
  if [[ -n "$uname" ]]; then
    phome="$(dscl . -read "/Users/$uname" NFSHomeDirectory 2>/dev/null | sed 's/^NFSHomeDirectory: //')"
    [[ -z "$phome" ]] && phome="$(eval echo "~$uname" 2>/dev/null || true)"
    if [[ -n "$phome" && -d "$phome" ]]; then
      phome="$(cd "$phome" && pwd -P)"
      # isInside(passwd-home, root): равен корню или лежит под ним (граница сегмента).
      if [[ "$phome" == "$root" || "$phome" == "$root"/* ]]; then
        return 1
      fi
    fi
  fi
  printf '%s' "$root"
}
LATCH_ROOT="$(calibre_test_root || true)"

latch_allows_mutation() {
  [[ -n "$LATCH_ROOT" ]] || return 1
  [[ -d "$HOME" ]] || return 1
  local install_root
  install_root="$(cd "$HOME" && pwd -P)/Library/Application Support/fb2-to-epub"
  case "$install_root" in
    "$LATCH_ROOT"|"$LATCH_ROOT"/*) return 0 ;;
    *) return 1 ;;
  esac
}

# Read-only режим для parity-теста: напечатать результат детекта и выйти ДО
# любой записи на диск/в launchd. По построению ничего не мутирует.
calibre_detect_only() { [[ "${FB2_CALIBRE_DETECT_ONLY:-}" == "1" ]]; }

# ---------------------------------------------------------------------------
# Constants
# ---------------------------------------------------------------------------
LABEL="com.arrivarus.fb2toepub.agent"
# Тест-оверрайд метки: изолированная активация агента в тестах. МУТИРУЮЩИЙ —
# только под полной защёлкой (см. выше), иначе игнорируется.
if [[ -n "${FB2_AGENT_LABEL:-}" ]] && latch_allows_mutation; then
  LABEL="$FB2_AGENT_LABEL"
fi
APP_SUPPORT="$HOME/Library/Application Support/fb2-to-epub"
BIN_DIR="$APP_SUPPORT/bin"
PLIST="$HOME/Library/LaunchAgents/$LABEL.plist"
LOG_FILE="$HOME/Library/Logs/fb2-to-epub.log"
AGENT_PATH="/usr/bin:/bin:/usr/sbin:/sbin"
# Cover apply-jobs (M5): the app drops jobs here; this dir is in the agent's
# WatchPaths so a dropped job fires the agent. It must exist before (re)load.
COVERS_JOBS_DIR="$APP_SUPPORT/covers/jobs"

RUNNER_DST="$BIN_DIR/fb2-to-epub-runner.sh"
WATCHER_DST="$BIN_DIR/fb2-to-epub-watcher.sh"
COVER_DST="$BIN_DIR/fb2-to-epub-cover-finder.py"
# FB3 support: transform + its genre map, installed next to the other bin scripts
# so the watcher (which resolves them relative to itself) finds them after install.
FB3_DST="$BIN_DIR/fb2-to-epub-fb3.py"
FB3_GENRE_DST="$BIN_DIR/fb2-to-epub-fb3-genre.json"

# Resolve where our source scripts are. Search order:
#   1) FB2_SRC_DIR override (used by build/tests)
#   2) a sibling layout when run from a checkout (packaging/.. -> bin/, packaging/)
#   3) the same directory as this installer (the .app Resources layout)
SELF_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
find_src() {
  local name="$1"
  local c
  for c in \
    "${FB2_SRC_DIR:-}/$name" \
    "$SELF_DIR/$name" \
    "$SELF_DIR/../bin/$name" \
    "$SELF_DIR/bin/$name"; do
    [[ -n "$c" && -f "$c" ]] && { printf '%s' "$c"; return 0; }
  done
  return 1
}

# ---------------------------------------------------------------------------
# 1. Detect Calibre + python3
# ---------------------------------------------------------------------------
# КОНТРАКТ ДЕТЕКТА (plans.md, инвариант 5) — bash-близнец app/CalibreLocator.swift.
# Порядок кандидатов: app-owned → /Applications → ~/Applications; валидная
# локация = исполняемы ВСЕ ТРИ CLI (частичный Calibre = «движка нет»).
# Синхронность двух реализаций держит tests/run-calibre-locator-parity.sh.
CALIBRE_MACOS_DEFAULT="/Applications/calibre.app/Contents/MacOS"

# Валидна ли папка Contents/MacOS: три CLI, каждый — исполняемый НЕ-каталог.
calibre_dir_valid() {
  local d="$1" cli
  for cli in ebook-convert ebook-meta ebook-polish; do
    [[ -f "$d/$cli" && -x "$d/$cli" ]] || return 1
  done
  return 0
}

# Кандидаты в порядке контракта. Под защёлкой первым ВСТАВЛЯЕТСЯ (не заменяет)
# <TEST_ROOT>/calibre.app, а FB2_CALIBRE_DISABLE_SYSTEM=1 убирает кандидатов 2–3.
CANDIDATES=()
if [[ -n "$LATCH_ROOT" ]]; then
  CANDIDATES+=("$LATCH_ROOT/calibre.app/Contents/MacOS")
fi
CANDIDATES+=("$APP_SUPPORT/calibre.app/Contents/MacOS")
if [[ -z "$LATCH_ROOT" || "${FB2_CALIBRE_DISABLE_SYSTEM:-}" != "1" ]]; then
  CANDIDATES+=("$CALIBRE_MACOS_DEFAULT")
  CANDIDATES+=("$HOME/Applications/calibre.app/Contents/MacOS")
fi

CALIBRE_MACOS_DIR=""
CALIBRE_PARTIAL_DIR=""   # нашли ebook-convert, но набор неполный

if [[ -n "${EBOOK_CONVERT:-}" ]]; then
  # Явный env-оверрайд — ВЫСШИЙ приоритет (как и до CAL-1): CLI-инсталл и
  # нестандартные раскладки Calibre. Кандидаты в этом случае не перебираем.
  if [[ ! -x "$EBOOK_CONVERT" ]]; then
    if calibre_detect_only; then printf 'CALIBRE_MACOS_DIR=NONE\n'; exit 0; fi
    cat >&2 <<EOF
fb2-to-epub: Calibre not found.

Expected: $EBOOK_CONVERT

Install Calibre first:
  - Download: https://calibre-ebook.com/download_osx
  - or:       brew install --cask calibre

Then run this installer again.
EOF
    exit 1
  fi
  CALIBRE_MACOS_DIR="$(cd "$(dirname "$EBOOK_CONVERT")" && pwd)"
else
  for cand in "${CANDIDATES[@]}"; do
    if calibre_dir_valid "$cand"; then
      CALIBRE_MACOS_DIR="$(cd "$cand" && pwd)"
      break
    fi
    if [[ -z "$CALIBRE_PARTIAL_DIR" && -f "$cand/ebook-convert" && -x "$cand/ebook-convert" ]]; then
      CALIBRE_PARTIAL_DIR="$cand"
    fi
  done

  if [[ -z "$CALIBRE_MACOS_DIR" ]]; then
    if calibre_detect_only; then printf 'CALIBRE_MACOS_DIR=NONE\n'; exit 0; fi
    if [[ -n "$CALIBRE_PARTIAL_DIR" ]]; then
      # Calibre вроде есть, но набор неполный — тот же честный отказ, что и раньше.
      {
        echo "fb2-to-epub: Calibre is installed but some required tools are missing:"
        echo
        for cli in ebook-convert ebook-meta ebook-polish; do
          [[ -f "$CALIBRE_PARTIAL_DIR/$cli" && -x "$CALIBRE_PARTIAL_DIR/$cli" ]] || \
            echo "  - $cli   ($CALIBRE_PARTIAL_DIR/$cli)"
        done
        echo
        echo "These ship with a normal Calibre install. Update Calibre, then re-run:"
        echo "  - Download: https://calibre-ebook.com/download_osx"
        echo "  - or:       brew upgrade --cask calibre"
      } >&2
      exit 1
    fi
    {
      echo "fb2-to-epub: Calibre not found."
      echo
      echo "Looked in:"
      for cand in "${CANDIDATES[@]}"; do echo "  - $cand"; done
      echo
      echo "Install Calibre first:"
      echo "  - Download: https://calibre-ebook.com/download_osx"
      echo "  - or:       brew install --cask calibre"
      echo
      echo "Then run this installer again."
    } >&2
    exit 1
  fi
fi

# ebook-convert/meta/polish — производные ОДНОГО CALIBRE_MACOS_DIR (их и пишем в
# plist). Env-оверрайды остаются рабочими для нестандартных раскладок.
EBOOK_CONVERT="${EBOOK_CONVERT:-$CALIBRE_MACOS_DIR/ebook-convert}"
EBOOK_META="${EBOOK_META:-$CALIBRE_MACOS_DIR/ebook-meta}"
EBOOK_POLISH="${EBOOK_POLISH:-$CALIBRE_MACOS_DIR/ebook-polish}"

missing=()
[[ -x "$EBOOK_META" ]]   || missing+=("ebook-meta   ($EBOOK_META)")
[[ -x "$EBOOK_POLISH" ]] || missing+=("ebook-polish ($EBOOK_POLISH)")
if [[ ${#missing[@]} -gt 0 ]]; then
  if calibre_detect_only; then printf 'CALIBRE_MACOS_DIR=NONE\n'; exit 0; fi
  {
    echo "fb2-to-epub: Calibre is installed but some required tools are missing:"
    echo
    for m in "${missing[@]}"; do echo "  - $m"; done
    echo
    echo "These ship with a normal Calibre install. Update Calibre, then re-run:"
    echo "  - Download: https://calibre-ebook.com/download_osx"
    echo "  - or:       brew upgrade --cask calibre"
  } >&2
  exit 1
fi

# Детект завершён и валиден. В detect-only выходим ЗДЕСЬ — до создания папок,
# копирования скриптов, генерации plist и launchctl (parity-тест ничего не пишет).
if calibre_detect_only; then
  printf 'CALIBRE_MACOS_DIR=%s\n' "$CALIBRE_MACOS_DIR"
  printf 'EBOOK_CONVERT=%s\n' "$EBOOK_CONVERT"
  printf 'EBOOK_META=%s\n' "$EBOOK_META"
  printf 'EBOOK_POLISH=%s\n' "$EBOOK_POLISH"
  exit 0
fi

detect_python3() {
  local cand
  for cand in /usr/bin/python3 /opt/homebrew/bin/python3 /usr/local/bin/python3; do
    [[ -x "$cand" ]] && { printf '%s' "$cand"; return 0; }
  done
  cand="$(command -v python3 2>/dev/null || true)"
  [[ -n "$cand" ]] && { printf '%s' "$cand"; return 0; }
  return 1
}
if ! PYTHON3="$(detect_python3)"; then
  cat >&2 <<EOF
fb2-to-epub: python3 not found.

Install the Xcode Command Line Tools (provides /usr/bin/python3):
  xcode-select --install

Then run this installer again.
EOF
  exit 1
fi

# ---------------------------------------------------------------------------
# 2. Resolve WATCH_DIR
# ---------------------------------------------------------------------------
WATCH_DIR="${1:-${WATCH_DIR:-$HOME/Desktop/fb2-to-epub}}"
# Normalize a literal leading tilde from user input (it would not expand inside
# the quoted arg/env). We match the literal '~' on purpose, then expand via HOME.
# shellcheck disable=SC2088
case "$WATCH_DIR" in
  "~"|"~/"*) WATCH_DIR="$HOME/${WATCH_DIR#\~/}" ;;
esac
mkdir -p "$WATCH_DIR"
WATCH_DIR="$(cd "$WATCH_DIR" && pwd)"

# ---------------------------------------------------------------------------
# 3. Copy scripts into App Support/bin
# ---------------------------------------------------------------------------
mkdir -p "$BIN_DIR" "$(dirname "$PLIST")" "$(dirname "$LOG_FILE")" "$COVERS_JOBS_DIR"

src_runner="$(find_src fb2-to-epub-runner.sh)"   || { echo "fb2-to-epub: missing fb2-to-epub-runner.sh source" >&2; exit 1; }
src_watcher="$(find_src fb2-to-epub-watcher.sh)" || { echo "fb2-to-epub: missing fb2-to-epub-watcher.sh source" >&2; exit 1; }
src_cover="$(find_src fb2-to-epub-cover-finder.py)" || { echo "fb2-to-epub: missing fb2-to-epub-cover-finder.py source" >&2; exit 1; }
src_fb3="$(find_src fb2-to-epub-fb3.py)"            || { echo "fb2-to-epub: missing fb2-to-epub-fb3.py source" >&2; exit 1; }
src_fb3_genre="$(find_src fb2-to-epub-fb3-genre.json)" || { echo "fb2-to-epub: missing fb2-to-epub-fb3-genre.json source" >&2; exit 1; }

# runner.sh is the FDA-granted "responsible" target — the TCC grant is keyed to
# this file. On update, only (re)install it if missing or actually changed, so an
# idempotent re-run never churns the file and risks dropping the user's FDA grant.
if [[ ! -f "$RUNNER_DST" ]] || ! cmp -s "$src_runner" "$RUNNER_DST"; then
  install -m 0755 "$src_runner" "$RUNNER_DST"
fi
install -m 0755 "$src_watcher" "$WATCHER_DST"
install -m 0755 "$src_cover"   "$COVER_DST"
install -m 0755 "$src_fb3"       "$FB3_DST"
install -m 0644 "$src_fb3_genre" "$FB3_GENRE_DST"

# ---------------------------------------------------------------------------
# 4. Generate the LaunchAgent plist via plutil (safe for spaces/unicode)
#    Build a minimal valid skeleton, then insert/replace typed values whose
#    contents are passed as separate argv -> never spliced into XML.
# ---------------------------------------------------------------------------
gen_plist() {
  local out="$1"
  # Minimal valid empty-dict plist to seed plutil edits.
  cat > "$out" <<'PLIST'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict/>
</plist>
PLIST

  plutil -replace Label -string "$LABEL" "$out"

  # ProgramArguments -> [ runner ]
  plutil -replace ProgramArguments -json '[]' "$out"
  plutil -insert  ProgramArguments.0 -string "$RUNNER_DST" "$out"

  # WatchPaths -> [ WATCH_DIR, COVERS_JOBS_DIR ]
  # The watch folder fires the agent on new books; covers/jobs fires it when the
  # app drops a cover apply-job (M5) so the agent applies the chosen cover under
  # its Full Disk Access. The dir must EXIST for launchd to watch it (created
  # below before (re)load).
  plutil -replace WatchPaths -json '[]' "$out"
#!/bin/bash
# Build fb2-to-epub.app — NATIVE SwiftUI app (M0+).
#
# Steps:
#   1. compile app/main.swift for arm64 + x86_64 (xcrun swiftc) and lipo them
#      into a universal Contents/MacOS/fb2-to-epub
#   2. copy installer.sh + watcher + cover-finder + runner into Contents/Resources
#   3. build AppIcon.icns from branding/icon-app.svg (svg->png->iconutil)
#   4. write a clean Info.plist from scratch: CFBundleIdentifier=com.arrivarus.fb2toepub
#      (stable! a drifting id breaks TCC grants on every rebuild),
#      CFBundleExecutable=fb2-to-epub, version, icon, LSMinimumSystemVersion=11.0
#   5. ad-hoc codesign (-s -) and strict verify, inside a retry loop (iCloud
#      FinderInfo race — see .patches/003)
#
# Unsandboxed, no external Swift deps (SwiftUI/AppKit/Foundation), offline build.
#
# Output: build/dist/fb2-to-epub.app
#
# Usage: build/build-app.sh [version]   (default version below)

set -euo pipefail

VERSION="${1:-0.1.0}"
BUNDLE_ID="com.arrivarus.fb2toepub"
APP_NAME="fb2-to-epub"
MIN_MACOS="11.0"

REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BUILD_DIR="$REPO_DIR/build"
DIST_DIR="$BUILD_DIR/dist"
APP="$DIST_DIR/$APP_NAME.app"
# Swift sources (compiled together into one binary, whole-module). main.swift
# drives the AppKit/SwiftUI window; EngineClient(+Status) is the engine bridge
# (M1/M2); Tokens/StateModel/StatusView make up the M2 Status screen;
# SetupView is the M3 first-run "Установка" screen; CoverSelectView is the M5
# "Выбор обложки" screen (reads the cover queue, writes an apply-job);
# SettingsView is the "Настройки" screen (replaces the old gear NSMenu).
SWIFT_SRCS=(
  "$REPO_DIR/app/main.swift"
  "$REPO_DIR/app/CalibreLocator.swift"
  "$REPO_DIR/app/CalibreInstaller.swift"
  "$REPO_DIR/app/EngineClient.swift"
  "$REPO_DIR/app/EngineClient+Status.swift"
  "$REPO_DIR/app/StateModel.swift"
  "$REPO_DIR/app/Tokens.swift"
  "$REPO_DIR/app/StatusView.swift"
  "$REPO_DIR/app/SetupView.swift"
  "$REPO_DIR/app/EngineSetupCard.swift"
  "$REPO_DIR/app/FolderAccessCard.swift"
  "$REPO_DIR/app/CoverSelectView.swift"
  "$REPO_DIR/app/SettingsView.swift"
  "$REPO_DIR/app/UpdateChecker.swift"
  "$REPO_DIR/app/CoverGenerator.swift"
)
ICON_SVG="$REPO_DIR/branding/icon-app.svg"

# --- tool checks -----------------------------------------------------------
for t in xcrun lipo sips iconutil plutil codesign; do
  command -v "$t" >/dev/null 2>&1 || { echo "build-app: required tool '$t' not found" >&2; exit 1; }
done
xcrun --find swiftc >/dev/null 2>&1 || { echo "build-app: swiftc not found (install Xcode)" >&2; exit 1; }
for s in "${SWIFT_SRCS[@]}"; do
  [[ -f "$s" ]] || { echo "build-app: missing $s" >&2; exit 1; }
done
[[ -f "$ICON_SVG"  ]] || { echo "build-app: missing $ICON_SVG" >&2; exit 1; }

SDK_PATH="$(xcrun --show-sdk-path --sdk macosx)"
[[ -d "$SDK_PATH" ]] || { echo "build-app: macOS SDK not found via xcrun" >&2; exit 1; }

# cairosvg rasterizes the SVG with a TRANSPARENT background (qlmanage forced a WHITE
# backing outside the squircle → opaque white corners in the .icns → a white "frame"
# ring around the icon in Finder/DMG). Resolve the venv binary; require it explicitly.
# cairocffi needs to dlopen brew's libcairo — /opt/homebrew/lib is NOT on the default
# dyld search path (Apple Silicon), so point the fallback there explicitly. NB: invoke
# via `python3 -m cairosvg`, NOT the venv's bin/cairosvg: with a space in the repo path
# venv generates a #!/bin/sh wrapper, and SIP strips DYLD_* at that sh exec, losing the
# fallback path before python starts.
export DYLD_FALLBACK_LIBRARY_PATH="/opt/homebrew/lib:/usr/local/lib${DYLD_FALLBACK_LIBRARY_PATH:+:$DYLD_FALLBACK_LIBRARY_PATH}"
VENV_PY="$BUILD_DIR/.venv/bin/python3"
if [[ -x "$VENV_PY" ]] && "$VENV_PY" -c "import cairosvg" >/dev/null 2>&1; then
  CAIROSVG=("$VENV_PY" -m cairosvg)
elif command -v cairosvg >/dev/null 2>&1; then
  CAIROSVG=("$(command -v cairosvg)")
else
  echo "build-app: cairosvg not found. Install it:" >&2
  echo "    python3 -m venv build/.venv && build/.venv/bin/pip install cairosvg" >&2
  echo "    (needs system cairo: brew install cairo)" >&2
  exit 1
fi

# --- clean + build native universal binary ---------------------------------
rm -rf "$APP"
MACOS="$APP/Contents/MacOS"
RES="$APP/Contents/Resources"
mkdir -p "$MACOS" "$RES"

echo "==> compiling native SwiftUI binary (arm64 + x86_64)"
BIN_TMP="$(mktemp -d)"
for arch in arm64 x86_64; do
  echo "    swiftc -> $arch"
  xcrun swiftc \
    -sdk "$SDK_PATH" \
    -target "${arch}-apple-macos${MIN_MACOS}" \
    -O \
    "${SWIFT_SRCS[@]}" \
    -o "$BIN_TMP/$APP_NAME-$arch" 2>&1 | sed 's/^/    /'
  # swiftc exit code is hidden by the pipe to sed — verify the artifact exists.
  [[ -f "$BIN_TMP/$APP_NAME-$arch" ]] || {
    echo "build-app: swiftc failed to produce $arch binary" >&2; rm -rf "$BIN_TMP"; exit 1; }
done

echo "==> lipo -> universal $MACOS/$APP_NAME"
lipo -create "$BIN_TMP/$APP_NAME-arm64" "$BIN_TMP/$APP_NAME-x86_64" \
  -output "$MACOS/$APP_NAME"
chmod 0755 "$MACOS/$APP_NAME"
rm -rf "$BIN_TMP"
lipo -info "$MACOS/$APP_NAME" | sed 's/^/    /'

# --- bundle the install logic + scripts ------------------------------------
echo "==> copying scripts into Resources"
install -m 0755 "$REPO_DIR/packaging/installer.sh"             "$RES/installer.sh"
install -m 0755 "$REPO_DIR/packaging/fb2-to-epub-runner.sh"    "$RES/fb2-to-epub-runner.sh"
install -m 0755 "$REPO_DIR/bin/fb2-to-epub-watcher.sh"         "$RES/fb2-to-epub-watcher.sh"
install -m 0755 "$REPO_DIR/bin/fb2-to-epub-cover-finder.py"    "$RES/fb2-to-epub-cover-finder.py"
# FB3 support: the transform (executable, like cover-finder) + its genre map (data,
# 0644 like the cover templates). The watcher resolves both next to itself.
install -m 0755 "$REPO_DIR/bin/fb2-to-epub-fb3.py"            "$RES/fb2-to-epub-fb3.py"
install -m 0644 "$REPO_DIR/bin/fb2-to-epub-fb3-genre.json"    "$RES/fb2-to-epub-fb3-genre.json"

# --- bundle the typographic cover templates --------------------------------
# CoverGenerator.swift renders a fallback book cover NATIVELY via an offscreen
# WKWebView from these 4 approved tokenized SVG templates (text substitution +
# width-fit happen in JS inside the webview). They must ship inside the bundle
# at Contents/Resources/cover-templates/ so Bundle.main can find them at runtime.
echo "==> copying cover templates into Resources/cover-templates"
COVER_TPL_SRC="$REPO_DIR/design/cover-templates/tokenized"
COVER_TPL_DST="$RES/cover-templates"
mkdir -p "$COVER_TPL_DST"
for n in 1 2 3 4; do
  case "$n" in
    1) f="tmpl-1-minimal.tok.svg" ;;
    2) f="tmpl-2-classic.tok.svg" ;;
    3) f="tmpl-3-modern.tok.svg" ;;
    4) f="tmpl-4-dark.tok.svg" ;;
  esac
  [[ -f "$COVER_TPL_SRC/$f" ]] || { echo "build-app: missing cover template $COVER_TPL_SRC/$f" >&2; exit 1; }
  install -m 0644 "$COVER_TPL_SRC/$f" "$COVER_TPL_DST/$f"
done

# --- icon: SVG -> PNG set -> .icns -----------------------------------------
echo "==> building AppIcon.icns from $(basename "$ICON_SVG")"
ICON_TMP="$(mktemp -d)"
ICONSET="$ICON_TMP/AppIcon.iconset"
mkdir -p "$ICONSET"

# Render the SVG once at high resolution with cairosvg (transparent background, exact
# 1024x1024). Then downscale with sips to each required size — sips preserves the alpha
# channel, so the squircle's rounded corners stay transparent all the way down.
BASE_PNG="$ICON_TMP/base-1024.png"
"${CAIROSVG[@]}" "$ICON_SVG" -o "$BASE_PNG" --output-width 1024 --output-height 1024 2>&1 | sed 's/^/    /' || true
if [[ ! -f "$BASE_PNG" ]]; then
  echo "build-app: failed to rasterize SVG to PNG via cairosvg" >&2
  rm -rf "$ICON_TMP"; exit 1
fi

# Apple iconset requires these named sizes (1x + 2x).
make_size() { sips -z "$2" "$2" "$BASE_PNG" --out "$ICONSET/$1" >/dev/null; }
make_size icon_16x16.png        16
make_size icon_16x16@2x.png     32
make_size icon_32x32.png        32
make_size icon_32x32@2x.png     64
make_size icon_128x128.png     128
make_size icon_128x128@2x.png  256
make_size icon_256x256.png     256
make_size icon_256x256@2x.png  512
make_size icon_512x512.png     512
make_size icon_512x512@2x.png 1024

iconutil -c icns "$ICONSET" -o "$RES/AppIcon.icns"
# Native bundle: the icon resolves straight from CFBundleIconFile=AppIcon → AppIcon.icns.
# No applet.icns / applet.rsrc legacy carriers here (those were osacompile artifacts).
rm -rf "$ICON_TMP"

# --- Info.plist: clean, written from scratch (native bundle) ---------------
# A native bundle has NO pre-seeded Info.plist (unlike osacompile, which dumped a
# pile of Carbon/AppleEvents keys). Write exactly the keys we want — stable id,
# native executable name, dark-capable windowed app. No LSUIElement (plain window).
# No CFBundleIconName (that key would override CFBundleIconFile and, without an
# Assets.car, fall back to the generic icon — see .patches/002).
echo "==> writing Info.plist (id=$BUNDLE_ID, exec=$APP_NAME, version=$VERSION)"
PLIST="$APP/Contents/Info.plist"
cat > "$PLIST" <<PLIST_EOF
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleIdentifier</key>
	<string>$BUNDLE_ID</string>
	<key>CFBundleExecutable</key>
	<string>$APP_NAME</string>
	<key>CFBundleName</key>
	<string>$APP_NAME</string>
	<key>CFBundleDisplayName</key>
	<string>$APP_NAME</string>
	<key>CFBundleShortVersionString</key>
	<string>$VERSION</string>
	<key>CFBundleVersion</key>
	<string>$VERSION</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>LSMinimumSystemVersion</key>
	<string>$MIN_MACOS</string>
	<key>NSHighResolutionCapable</key>
	<true/>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
</dict>
</plist>
PLIST_EOF
plutil -lint "$PLIST" >/dev/null

# --- strip xattrs + ad-hoc sign + verify (strict), with retry --------------
# cairosvg/sips/iconutil/touch leave com.apple.FinderInfo / quarantine xattrs that
# make `codesign --deep --strict` reject the bundle ("resource fork ... detritus
# not allowed"). Worse: when the repo lives in a synced folder (iCloud/fileprovider),
# the daemon re-stamps com.apple.FinderInfo onto the bundle ROOT directory
# ASYNCHRONOUSLY — sometimes between the strip and codesign, or between codesign and
# strict verify — so the failure is a RACE and reproduces only intermittently. That
# xattr sits on the wrapper DIRECTORY, not on any signed payload, so clearing it just
# before signing/verify is safe and does not invalidate the signature.
# Note: do NOT `touch` the bundle root — touch re-adds FinderInfo and re-breaks strict.
#
# Strategy: run the full strip→sign→clean→verify sequence inside a retry loop (up to 5
# attempts, ~1s pause between). Both codesign calls are guarded by `if` so a failed
# attempt RETRIES instead of killing the script under `set -euo pipefail`.
echo "==> ad-hoc codesign + strict verify (with retry)"
CODESIGN_OK=0
for attempt in 1 2 3 4 5; do
  echo "==> codesign attempt $attempt/5"
  # Full cleanup on every attempt — FinderInfo may have been re-stamped since last try.
  find "$APP" -name '._*' -delete 2>/dev/null || true
  find "$APP" -name '.DS_Store' -delete 2>/dev/null || true
  xattr -cr "$APP" 2>/dev/null || true
  xattr -d com.apple.FinderInfo "$APP" 2>/dev/null || true

  if ! codesign --force --deep -s - "$APP"; then
    echo "    codesign --force failed (attempt $attempt/5), retrying after 1s" >&2
    sleep 1
    continue
  fi

  # Dechunk FinderInfo again RIGHT before strict verify — this is the race guard.
  xattr -d com.apple.FinderInfo "$APP" 2>/dev/null || true

  if codesign --verify --deep --strict "$APP"; then
    CODESIGN_OK=1
    break
  fi
  echo "    strict verify failed (attempt $attempt/5), retrying after 1s" >&2
  sleep 1
done

if [[ "$CODESIGN_OK" -ne 1 ]]; then
  echo "build-app: codesign failed strict verify after 5 attempts (iCloud/fileprovider FinderInfo race)" >&2
  exit 1
fi
{ codesign --verify --deep --strict --verbose=2 "$APP" 2>&1 || true; } | sed 's/^/    /'

echo ""
echo "Built: $APP"
echo "  CFBundleIdentifier: $(plutil -extract CFBundleIdentifier raw -o - "$PLIST")"
echo "  CFBundleExecutable: $(plutil -extract CFBundleExecutable raw -o - "$PLIST")"
echo "  Version:            $(plutil -extract CFBundleShortVersionString raw -o - "$PLIST")"
echo "  Architectures:      $(lipo -archs "$MACOS/$APP_NAME")"
echo "  Icon:               $RES/AppIcon.icns"
// EngineClient — thin Swift bridge between the SwiftUI app and the existing
// shell engine (LaunchAgent + installer.sh + Calibre). M1.
//
// Design rules (from arch/plans-ui.md, "Инварианты" + "Контракты данных"):
//   - The engine logic is NOT changed here. We only *call* installer.sh and
//     read agent state via launchctl / plutil.
//   - runner.sh / watcher.sh are never invoked from Swift.
//   - The app is unsandboxed, no external Swift deps: Foundation only.
//
// Everything here shells out via Process. Arguments are passed as a real argv
// array (never spliced into a shell string), so paths with spaces / unicode are
// safe — same contract the installer relies on (plutil, not sed).

import Foundation

// MARK: - Public model

/// Resolved state of the LaunchAgent, as the UI needs to render it.
///
/// IMPORTANT (Codex's nuance, confirmed by `launchctl print` on a real agent):
/// the agent is *event-driven*. In the normal idle case `launchctl print`
/// reports `state = not running` — that is healthy, not an error. So "active"
/// is defined as: `launchctl print` returned 0  AND  the plist exists  AND  the
/// label is not disabled. A running pid is NOT required.
struct AgentStatus {
    /// `launchctl print gui/$UID/<label>` returned rc == 0 (agent is bootstrapped).
    var isLoaded: Bool
    /// Label is disabled in the user domain (`launchctl print-disabled` => disabled).
    var isDisabled: Bool
    /// `state = running` was seen in `launchctl print` (informational only).
    var isRunning: Bool
    /// Whether the LaunchAgent plist file exists on disk.
    var plistExists: Bool

    /// The UI's single source of truth: is the agent effectively active?
    /// active = bootstrapped (rc 0) AND plist present AND not disabled.
    /// Running pid is intentionally NOT part of this — the agent is event-driven.
    var isActive: Bool { isLoaded && plistExists && !isDisabled }
}

/// Result of the first-run / migration decision.
enum FirstRunOutcome: Equatable {
    /// No plist existed → installer was run on the given default folder.
    case installedDefault(watchDir: String)
    /// A plist already existed → we kept the user's existing WATCH_DIR untouched.
    case migratedExisting(watchDir: String)
    /// Движка НЕТ — installer даже не звали. UI предлагает поставить Calibre.
    /// (CAL-1: раньше это и следующий случай маскировались одним blockedNoCalibre.)
    case needsEngine
    /// Движок ЕСТЬ, но installer вернул ненулевой код — агент не поднялся.
    /// Это НЕ повод предлагать установку движка: чинить надо запуск агента.
    case agentSetupFailed
}

/// One-shot snapshot the UI reads to prove the bridge works (M1 placeholder
/// screen; the real Status screen lands at M2).
struct EngineSnapshot {
    var watchDir: String?
    var agent: AgentStatus
    var calibreInstalled: Bool
}

// MARK: - EngineClient

/// All paths are derived from `home` so the whole client can be pointed at a
/// throwaway HOME (and a throwaway label) for isolated tests — without ever
/// touching the user's real agent or watch folder.
struct EngineClient {

    // --- configuration -----------------------------------------------------

    /// LaunchAgent label. Default is the production label; tests override it
    /// with a throwaway label so they never collide with the real agent.
    let label: String

    /// Home directory root. Default is the real HOME; tests point it at a
    /// `mktemp -d` so the installer writes into a sandbox.
    let home: String

    /// Absolute path to the bundled installer.sh (in the .app Resources, or a
    /// checkout path for tests).
    let installerPath: String

    /// ЯВНЫЙ оверрайд пути к `ebook-convert`. По умолчанию nil → путь целиком
    /// определяет `CalibreLocator` (контракт детекта, инвариант 5).
    ///
    /// Оверрайд оставлен для тестов, которые подсовывают сюда `/bin/echo`: для
    /// него проверяется ТОЛЬКО сам файл (без meta/polish рядом) — прежняя
    /// M1-семантика «есть исполняемый файл = движок есть». Продакшен ходит
    /// через локатор, где валидность = все три CLI.
    let ebookConvertOverride: String?

    init(
        label: String = "com.arrivarus.fb2toepub.agent",
        home: String = EngineHome.resolve(),
        installerPath: String,
        ebookConvertPath: String? = nil
    ) {
        self.label = label
        self.home = home
        self.installerPath = installerPath
        self.ebookConvertOverride = ebookConvertPath
    }

    // --- derived paths -----------------------------------------------------

    /// `~/Library/LaunchAgents/<label>.plist`
    var plistPath: String {
        "\(home)/Library/LaunchAgents/\(label).plist"
    }

    /// `gui/<uid>/<label>` — the launchd service target.
    private var serviceTarget: String { "gui/\(getuid())/\(label)" }
    private var domainTarget: String { "gui/\(getuid())" }

    // MARK: - Process helper

    /// Result of running a child process.
    struct RunResult {
        let status: Int32
        let stdout: String
        let stderr: String
    }

    /// Run an executable with an explicit argv array. No shell, no string
    /// splicing. `env` is merged onto the current environment when provided
    /// (used to inject a throwaway HOME for the installer in tests).
    @discardableResult
    func run(_ launchPath: String, _ args: [String], env: [String: String]? = nil) -> RunResult {
        let proc = Process()
        proc.executableURL = URL(fileURLWithPath: launchPath)
        proc.arguments = args
        if let env = env {
            var merged = ProcessInfo.processInfo.environment
            for (k, v) in env { merged[k] = v }
            proc.environment = merged
        }

        let outPipe = Pipe()
        let errPipe = Pipe()
        proc.standardOutput = outPipe
        proc.standardError = errPipe

        do {
            try proc.run()
        } catch {
            return RunResult(status: -1, stdout: "", stderr: "spawn failed: \(error)")
        }

        // Read both pipes fully BEFORE waitUntilExit to avoid a deadlock if the
        // child fills a pipe buffer (launchctl print can be large).
        let outData = outPipe.fileHandleForReading.readDataToEndOfFile()
        let errData = errPipe.fileHandleForReading.readDataToEndOfFile()
        proc.waitUntilExit()

        return RunResult(
            status: proc.terminationStatus,
            stdout: String(data: outData, encoding: .utf8) ?? "",
            stderr: String(data: errData, encoding: .utf8) ?? ""
        )
    }

    // MARK: - Calibre probe (контракт детекта — CalibreLocator)

    /// Текущая локация движка по контракту (CAL-1). nil = движка нет.
    /// Все пути считаются от `home`, поэтому изолированный тест остаётся изолированным.
    func calibreLocation() -> CalibreLocation? {
        CalibreLocator.resolve(home: home)
    }

    /// Абсолютный путь к `ebook-convert`: явный оверрайд → локатор → "" (движка нет).
    /// Пустая строка недостижима для вызывающих: все они идут через
    /// `calibreInstalled()`, который в этом случае уже вернул false.
    var ebookConvertPath: String {
        ebookConvertOverride ?? calibreLocation()?.ebookConvert ?? ""
    }

    /// Движок есть?
    ///   • с явным оверрайдом — исполняем ли ИМЕННО он (прежняя M1-семантика);
    ///   • без оверрайда — нашёл ли локатор валидную локацию (все три CLI).
    func calibreInstalled() -> Bool {
        if let override = ebookConvertOverride {
            return CalibreLocator.isExecutableRegularFile(override)
        }
        return calibreLocation() != nil
    }

    // MARK: - Raw history (гибрид подачи D37)

    /// Есть ли у пользователя история конвертаций по СЫРОМУ снапшоту `state.json`
    /// (`converted_total > 0 || recent непуст || last_conversion != nil`).
    ///
    /// Определяет подачу онбординга без движка: история ЕСТЬ → баннер A (не прячем
    /// уже сконвертированные книги), НЕТ → блокер B (показывать нечего, ведём в одно
    /// действие). Считается по СЫРОМУ `StateStore.load()`, а НЕ по отфильтрованному
    /// `loadState()`: иначе «Сбросить статистику» / «Очистить» превратили бы баннер
    /// в блокер (D37, решение суда архитекторов). Тот же сигнал питает
    /// `shouldShowSetup` — единый helper, чтобы две ветки не разъехались.
    func hasRawHistory() -> Bool {
        let raw = StateStore(home: home).load()
        return raw.totals.convertedTotal > 0 || !raw.recent.isEmpty || raw.lastConversion != nil
    }

    // MARK: - Watch dir (read from installed plist)

    func plistExists() -> Bool {
        FileManager.default.fileExists(atPath: plistPath)
    }

    /// Read the installed WATCH_DIR straight from the plist's EnvironmentVariables
    /// via `plutil -extract` (the same typed value the installer wrote). Returns
    /// nil when the plist is absent or the key is missing.
    func readWatchDir() -> String? {
        guard plistExists() else { return nil }
        let r = run("/usr/bin/plutil",
                    ["-extract", "EnvironmentVariables.WATCH_DIR", "raw", "-o", "-", plistPath])
        guard r.status == 0 else { return nil }
        let value = r.stdout.trimmingCharacters(in: .whitespacesAndNewlines)
        return value.isEmpty ? nil : value
    }

    // MARK: - launchctl: status

    /// `launchctl print <target>` + `launchctl print-disabled <domain>` parsed
    /// into an AgentStatus. See AgentStatus doc for why running is not required.
    func agentStatus() -> AgentStatus {
        let printRes = run("/bin/launchctl", ["print", serviceTarget])
        let isLoaded = printRes.status == 0

        // `state = running` is informational only (event-driven agent idles as
        // "not running"). Match the exact token launchctl prints.
        let isRunning = Self.parseIsRunning(fromPrint: printRes.stdout)

        let disabledRes = run("/bin/launchctl", ["print-disabled", domainTarget])
        let isDisabled = Self.parseIsDisabled(fromPrintDisabled: disabledRes.stdout, label: label)

        return AgentStatus(
            isLoaded: isLoaded,
            isDisabled: isDisabled,
            isRunning: isRunning,
            plistExists: plistExists()
        )
    }

    /// Parse `state = running` from `launchctl print` output.
    /// launchctl prints e.g. `\tstate = running` or `\tstate = not running`.
    static func parseIsRunning(fromPrint out: String) -> Bool {
        for rawLine in out.split(separator: "\n") {
            let line = rawLine.trimmingCharacters(in: .whitespaces)
            guard line.hasPrefix("state") else { continue }
            // Only the top-level service `state = ...` line (there are nested
            // `state = active` lines for endpoints — those are not the pid state).
            // The service line has the form "state = running" / "state = not running".
            if let eq = line.firstIndex(of: "=") {
                let value = line[line.index(after: eq)...].trimmingCharacters(in: .whitespaces)
                if value == "running" { return true }
                if value == "not running" { return false }
            }
        }
        return false
    }

    /// Parse `"<label>" => disabled` / `=> enabled` from `launchctl print-disabled`.
    /// Default-absent label is treated as NOT disabled (launchctl omits labels
    /// it has no explicit disable record for).
    static func parseIsDisabled(fromPrintDisabled out: String, label: String) -> Bool {
        let needle = "\"\(label)\""
        for rawLine in out.split(separator: "\n") {
            let line = rawLine.trimmingCharacters(in: .whitespaces)
            guard line.contains(needle) else { continue }
            // Form: "com.x.y" => disabled  |  "com.x.y" => enabled
            if line.contains("=> disabled") { return true }
            if line.contains("=> enabled") { return false }
        }
        return false
    }

    // MARK: - launchctl: lifecycle wrappers (used by install/repair flows)

    @discardableResult
// FolderAccessCard — the FDA-onboarding component (v1.0.1, D46), rendered in four
// presentations, mirroring EngineSetupCard's visual grammar and tokens WITHOUT
// touching it (its Phase automat is a just-shipped v1.0.0 surface — coupling the two
// would blow up the "4 подачи × N states" matrix, arch/plan-fda-synthesis.md).
//
// One problem (the agent has no Full Disk Access, so the watch folder is unreadable
// and books silently don't convert), FOUR подачи (mirror the engine onboarding):
//   • .blocker     — full-screen: warn ring + title + steps + CTA (replaces Status
//                    content when there is NO conversion history).
//   • .banner      — warn card under the header, Status content stays visible+muted
//                    (there IS history — conversion is equally blocked either way).
//   • .setupStep   — amber «ДОСТУП К ПАПКЕ» step body on first-run Setup (display-only,
//                    hands off to Status where the live flow lives).
//   • .settingsRow — the compact warn row in Настройки (replaces the passive FDA row
//                    only while denied; ok/absent keeps today's byte-identical row).
//
// A tiny automat drives blocker/banner:  denied / checking / stillDenied / timeout.
// FDA-2 is DISPLAY-ONLY: every button defaults to a no-op; the real recheck/clipboard/
// panel pipeline is wired at FDA-3. Screenshots come via FB2_FORCE_FOLDER_ACCESS.

import SwiftUI
import AppKit

// MARK: - Local glyph helper (file-private; mirrors StatusView/EngineSetupCard)

private func faIcon(_ name: String, size: CGFloat, weight: Font.Weight = .regular) -> some View {
    Image(systemName: name)
        .font(.system(size: size, weight: weight))
}

// MARK: - FolderAccessCard

struct FolderAccessCard: View {

    /// The tiny recheck automat. `denied` is the resting problem state; the other
    /// three are transient states the host drives during «Проверить снова» (FDA-3).
    enum State: Equatable {
        case denied         // no access — the resting problem
        case checking       // «Проверить снова» pressed, waiting for a fresh probe
        case stillDenied    // the fresh probe still says denied
        case timeout        // the agent never answered (kickstart failed / agent off)

        /// The two RESTING outcomes the host may PIN after a recheck coordinator tears
        /// down. Distinguished from the transient `.checking` and the live-derived
        /// `.denied` so the refresh cycle can dissolve a stale terminal card once the
        /// live flag recovers (A1, via `FolderRecheck.terminalRecheckDissolves`).
        var isTerminal: Bool { self == .stillDenied || self == .timeout }
    }

    /// Which of the four surfaces to render.
    enum Presentation { case blocker, banner, setupStep, settingsRow }

    let state: State
    let presentation: Presentation

    // Actions — DRAWN but inert in FDA-2 (defaults are no-ops). Wired at FDA-3.
    /// Primary: open the Full Disk Access pane AND copy the runner path to the clipboard.
    var onOpenSettings: () -> Void = {}
    /// Secondary: kick the agent and wait for a fresh probe.
    var onRecheck: () -> Void = {}
    /// Setup step only: hand off to Status (where the live blocker/banner lives).
    var onFixFromSetup: () -> Void = {}

    var body: some View {
        switch presentation {
        case .blocker:     blocker
        case .banner:      banner
        case .setupStep:   setupStep
        case .settingsRow: settingsRow
        }
    }

    // MARK: - Shared texts (ты-форма, honest — never asserts "FDA запрещён")

    // The three numbered steps (same wording A/B; the badge style differs by direction).
    private static let step1 = "Нажми «+» под списком."
    private static let step2 = "Cmd-Shift-G и вставь путь — он уже в буфере."
    private static let step3lead = "Включи переключатель у "
    private static let step3accent = "fb2-to-epub-runner.sh"

    private var blockerTitleText: String {
        state == .timeout ? "Агент не ответил" : "Нет доступа к папке"
    }
    private var bodyText: String {
        state == .timeout
            ? "Фоновый агент не отозвался. Проверь, что он активен, и попробуй ещё раз."
            : "macOS прячет «Рабочий стол», «Документы» и «Загрузки» от фоновых программ. "
              + "Разреши доступ один раз — и конвертация заработает."
    }
    /// The status/feedback line above the buttons, per automat state (nil = none).
    private var feedbackText: String? {
        switch state {
        case .denied:      return nil
        case .checking:    return "Проверяю доступ…"
        case .stillDenied: return "Доступа пока нет — проверь шаги и попробуй ещё раз."
        case .timeout:     return nil    // the timeout message is the body itself
        }
    }
    private var isChecking: Bool { state == .checking }
}

// MARK: - Reusable buttons (tokens-exact; file-private mirrors of EngineSetupCard's)

/// `.cta` — the big blocker button (Direction B). Orange fill for FDA.
private struct FaCtaBig: View {
    let title: String
    var icon: String? = nil
    var disabled: Bool = false
    let action: () -> Void
    var body: some View {
        HStack(spacing: Tokens.CO.ctaGap) {
            if let icon = icon {
                faIcon(icon, size: 16, weight: .bold).foregroundColor(.white)
            }
            Text(title).font(Tokens.CS.cta_).foregroundColor(.white)
                .lineLimit(1).minimumScaleFactor(0.85)
        }
        .frame(maxWidth: .infinity)
        .padding(Tokens.CO.ctaPad)
        .background(
            RoundedRectangle(cornerRadius: Tokens.CO.ctaBigRadius, style: .continuous)
                .fill(Tokens.CO.retryOrangeGrad))
        .shadow(color: Tokens.CO.ctaShadowOrangeColor,
                radius: Tokens.CO.ctaShadowRadius, x: 0, y: Tokens.CO.ctaShadowY)
        .padding(.top, Tokens.CO.ctaTop)
        .opacity(disabled ? 0.5 : 1)
        .contentShape(Rectangle())
        .onTapGesture { if !disabled { action() } }
    }
}

/// `.cta-ghost` — secondary "Проверить снова" (Direction B).
private struct FaCtaGhost: View {
    let title: String
    var icon: String? = nil
    var disabled: Bool = false
    let action: () -> Void
    var body: some View {
        HStack(spacing: Tokens.CO.ctaGap) {
            if let icon = icon {
                faIcon(icon, size: 15, weight: .regular).foregroundColor(Tokens.C.textSoft)
            }
            Text(title).font(Tokens.F.stepTitle).foregroundColor(Tokens.C.textPrimary)
        }
        .frame(maxWidth: .infinity)
        .padding(Tokens.CO.ctaGhostPad)
        .background(
            RoundedRectangle(cornerRadius: Tokens.CO.ctaBigRadius, style: .continuous)
                .fill(Tokens.C.btnBg))
        .overlay(
            RoundedRectangle(cornerRadius: Tokens.CO.ctaBigRadius, style: .continuous)
                .stroke(Tokens.C.fieldBtnBorder, lineWidth: 1))
        .padding(.top, Tokens.CO.ctaTop)
        .opacity(disabled ? 0.5 : 1)
        .contentShape(Rectangle())
        .onTapGesture { if !disabled { action() } }
    }
}

/// `.cta-sm` — the small brand/orange button (banner A + Setup + Settings).
private struct FaCtaSmall: View {
    enum Size { case big, compact }
    enum Fill { case brand, orange }
    let title: String
    var icon: String? = nil
    var size: Size = .big
    var fill: Fill = .orange
    var disabled: Bool = false
    let action: () -> Void
    var body: some View {
        HStack(spacing: Tokens.CO.ctaSmGap) {
            if let icon = icon {
                faIcon(icon, size: size == .big ? 13 : 12, weight: .bold).foregroundColor(.white)
            }
            Text(title)
                .font(size == .big ? Tokens.CO.ctaSmFont : Tokens.CO.ctaSmFontSm)
                .foregroundColor(.white)
                .lineLimit(1).fixedSize(horizontal: true, vertical: false)
        }
        .padding(.vertical, size == .big ? Tokens.CO.ctaSmPadV : Tokens.CO.ctaSmPadVsm)
        .padding(.horizontal, size == .big ? Tokens.CO.ctaSmPadH : Tokens.CO.ctaSmPadHsm)
        .background(fillShape)
        .shadow(color: Tokens.CO.ctaSmShadowColor,
                radius: Tokens.CO.ctaSmShadowRadius, x: 0, y: Tokens.CO.ctaSmShadowY)
        .opacity(disabled ? 0.5 : 1)
        .contentShape(Rectangle())
        .onTapGesture { if !disabled { action() } }
    }

    // @ViewBuilder switch (not AnyShapeStyle, which is macOS 12+) — the app targets 11.0.
    @ViewBuilder private var fillShape: some View {
        switch fill {
        case .brand:
            RoundedRectangle(cornerRadius: Tokens.CO.ctaRadius, style: .continuous).fill(Tokens.G.brand135)
        case .orange:
            RoundedRectangle(cornerRadius: Tokens.CO.ctaRadius, style: .continuous).fill(Tokens.CO.retryOrangeGrad)
        }
    }
}

/// `.link-btn` — text-only secondary (12px everywhere).
private struct FaLinkBtn: View {
    let title: String
    var orange: Bool = false
    var disabled: Bool = false
    let action: () -> Void
    var body: some View {
        Text(title)
            .font(Tokens.F.link)
            .foregroundColor(orange ? Tokens.C.accentOrange : Tokens.C.textSecondary)
            .lineLimit(1).fixedSize(horizontal: true, vertical: false)
            .opacity(disabled ? 0.5 : 1)
            .contentShape(Rectangle())
            .onTapGesture { if !disabled { action() } }
    }
}

// MARK: - Numbered step (mirrors EngineSetupCard.ManualStep)

private struct FaStep: View {
    enum Direction { case a, b }
    let dir: Direction
    let n: Int
    let leading: String
    var accent: String? = nil
    var accentMono: Bool = false
    var trailing: String = ""

    var body: some View {
        HStack(alignment: .top, spacing: dir == .a ? Tokens.CO.stepsGapA : Tokens.CO.stepsGapB) {
            badge
            line.fixedSize(horizontal: false, vertical: true)
            Spacer(minLength: 0)
        }
    }

    private var badge: some View {
        Text("\(n)")
            .font(dir == .a ? Tokens.CO.stepBadgeFontA : Tokens.F.pill)
            .foregroundColor(dir == .a ? Tokens.C.textSoft : Tokens.C.accentOrange)
            .frame(width: dir == .a ? Tokens.CO.stepBadgeA : Tokens.CO.stepBadgeB,
                   height: dir == .a ? Tokens.CO.stepBadgeA : Tokens.CO.stepBadgeB)
            .background(Circle().fill(dir == .a ? Tokens.C.cardBorder : Tokens.C.stepCurBg))
            .overlay(dir == .b ? Circle().stroke(Tokens.CO.warnBorder30, lineWidth: 1) : nil)
            .padding(.top, 1)
    }

    private var line: some View {
        let bodyFont = dir == .a ? Tokens.CO.stepTextA : Tokens.F.headerSub
        let accentText: Text = accent.map { word in
            accentMono
                ? Text(word).font(Tokens.F.convSemibold).foregroundColor(Tokens.C.textPrimary)
                : Text(word).font(bodyFont).fontWeight(.semibold).foregroundColor(Tokens.C.textSoft)
        } ?? Text("")
        return (Text(leading) + accentText + Text(trailing))
            .font(bodyFont)
            .foregroundColor(Tokens.C.textSoft)
            .lineSpacing(dir == .a ? Tokens.CO.stepTextALineSpacing : Tokens.CO.stepTextBLineSpacing)
    }
}

// MARK: - Small caps label + warn ring + indeterminate bar (file-local mirrors)

private struct FaCapLabel: View {
    let text: String
    var body: some View {
        Text(text)
            .font(Tokens.F.cap)
            .foregroundColor(Tokens.C.textTertiary)
            .trackingCompat(Tokens.Track.cap)
    }
}

/// The 104×104 warn ring: dashed amber track + ⚠ (or a continuous spin while checking).
private struct FaWarnRing: View {
    var checking: Bool = false
    @State private var spin = false
    var body: some View {
        ZStack {
            Circle().stroke(Tokens.C.barTrack, style: StrokeStyle(lineWidth: Tokens.M.ringStroke))
            if checking {
                Circle()
                    .trim(from: 0, to: 70 / Tokens.CO.ringCircumference)
                    .stroke(Tokens.CO.ringInstallGrad,
                            style: StrokeStyle(lineWidth: Tokens.M.ringStroke, lineCap: .round))
                    .rotationEffect(.degrees(spin ? 360 : 0))
                faIcon("gearshape", size: Tokens.CO.ringIconGear, weight: .regular)
                    .foregroundColor(Tokens.C.accentOrange)
                    .rotationEffect(.degrees(spin ? 360 : 0))
            } else {
                Circle()
                    .stroke(Tokens.CO.ringWarnDash,
                            style: StrokeStyle(lineWidth: Tokens.M.ringStroke, lineCap: .round, dash: [4, 10]))
                faIcon("lock.shield", size: Tokens.CO.ringIconWarn, weight: .regular)
                    .foregroundColor(Tokens.C.accentOrange)
            }
        }
        .frame(width: Tokens.M.ringSize, height: Tokens.M.ringSize)
        .onAppear { syncSpin() }
        // A3: the ring is a PERSISTENT instance in the blocker (not conditionally
        // re-inserted), so a live denied→checking flip does NOT re-fire onAppear — the
        // gearshape would sit frozen. Restart/stop the spin on every `checking` flip.
        .onChange(of: checking) { _ in syncSpin() }
    }

    /// Drive the continuous spin from the current `checking` value: start the repeating
    /// rotation while checking, drop it (and reset the angle) otherwise so a later
    /// checking begins from 0. Called on appear AND on each `checking` transition (A3).
    private func syncSpin() {
        if checking {
            withAnimation(.linear(duration: Tokens.CO.spinDur).repeatForever(autoreverses: false)) {
                spin = true
            }
        } else {
            withAnimation(.linear(duration: 0)) { spin = false }
        }
    }
}

/// The 6px indeterminate progress bar (shown while checking), mirrors EngineSetupCard.

exec
/bin/zsh -lc "rg -n -C 10 'RUNNER|runner|ProgramArguments|codesign|Resources|quarantine|SMAppService|BundleProgram' packaging/installer.sh build/build-app.sh app/EngineClient.swift app/FolderAccessCard.swift; rg -n -C 8 'step3accent|runner|Full Disk|FDA|clipboard|Pasteboard|ProgramArguments' app/FolderAccessCard.swift app/EngineClient.swift; sed -n '220,520p' packaging/installer.sh; sed -n '90,260p' build/build-app.sh; sed -n '1,190p' app/EngineClient.swift" in /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
 succeeded in 0ms:
app/FolderAccessCard.swift-47-        var isTerminal: Bool { self == .stillDenied || self == .timeout }
app/FolderAccessCard.swift-48-    }
app/FolderAccessCard.swift-49-
app/FolderAccessCard.swift-50-    /// Which of the four surfaces to render.
app/FolderAccessCard.swift-51-    enum Presentation { case blocker, banner, setupStep, settingsRow }
app/FolderAccessCard.swift-52-
app/FolderAccessCard.swift-53-    let state: State
app/FolderAccessCard.swift-54-    let presentation: Presentation
app/FolderAccessCard.swift-55-
app/FolderAccessCard.swift-56-    // Actions — DRAWN but inert in FDA-2 (defaults are no-ops). Wired at FDA-3.
app/FolderAccessCard.swift:57:    /// Primary: open the Full Disk Access pane AND copy the runner path to the clipboard.
app/FolderAccessCard.swift-58-    var onOpenSettings: () -> Void = {}
app/FolderAccessCard.swift-59-    /// Secondary: kick the agent and wait for a fresh probe.
app/FolderAccessCard.swift-60-    var onRecheck: () -> Void = {}
app/FolderAccessCard.swift-61-    /// Setup step only: hand off to Status (where the live blocker/banner lives).
app/FolderAccessCard.swift-62-    var onFixFromSetup: () -> Void = {}
app/FolderAccessCard.swift-63-
app/FolderAccessCard.swift-64-    var body: some View {
app/FolderAccessCard.swift-65-        switch presentation {
app/FolderAccessCard.swift-66-        case .blocker:     blocker
app/FolderAccessCard.swift-67-        case .banner:      banner
--
app/FolderAccessCard.swift-69-        case .settingsRow: settingsRow
app/FolderAccessCard.swift-70-        }
app/FolderAccessCard.swift-71-    }
app/FolderAccessCard.swift-72-
app/FolderAccessCard.swift-73-    // MARK: - Shared texts (ты-форма, honest — never asserts "FDA запрещён")
app/FolderAccessCard.swift-74-
app/FolderAccessCard.swift-75-    // The three numbered steps (same wording A/B; the badge style differs by direction).
app/FolderAccessCard.swift-76-    private static let step1 = "Нажми «+» под списком."
app/FolderAccessCard.swift-77-    private static let step2 = "Cmd-Shift-G и вставь путь — он уже в буфере."
app/FolderAccessCard.swift-78-    private static let step3lead = "Включи переключатель у "
app/FolderAccessCard.swift:79:    private static let step3accent = "fb2-to-epub-runner.sh"
app/FolderAccessCard.swift-80-
app/FolderAccessCard.swift-81-    private var blockerTitleText: String {
app/FolderAccessCard.swift-82-        state == .timeout ? "Агент не ответил" : "Нет доступа к папке"
app/FolderAccessCard.swift-83-    }
app/FolderAccessCard.swift-84-    private var bodyText: String {
app/FolderAccessCard.swift-85-        state == .timeout
app/FolderAccessCard.swift-86-            ? "Фоновый агент не отозвался. Проверь, что он активен, и попробуй ещё раз."
app/FolderAccessCard.swift-87-            : "macOS прячет «Рабочий стол», «Документы» и «Загрузки» от фоновых программ. "
app/FolderAccessCard.swift-88-              + "Разреши доступ один раз — и конвертация заработает."
app/FolderAccessCard.swift-89-    }
--
app/EngineClient.swift-1-// EngineClient — thin Swift bridge between the SwiftUI app and the existing
app/EngineClient.swift-2-// shell engine (LaunchAgent + installer.sh + Calibre). M1.
app/EngineClient.swift-3-//
app/EngineClient.swift-4-// Design rules (from arch/plans-ui.md, "Инварианты" + "Контракты данных"):
app/EngineClient.swift-5-//   - The engine logic is NOT changed here. We only *call* installer.sh and
app/EngineClient.swift-6-//     read agent state via launchctl / plutil.
app/EngineClient.swift:7://   - runner.sh / watcher.sh are never invoked from Swift.
app/EngineClient.swift-8-//   - The app is unsandboxed, no external Swift deps: Foundation only.
app/EngineClient.swift-9-//
app/EngineClient.swift-10-// Everything here shells out via Process. Arguments are passed as a real argv
app/EngineClient.swift-11-// array (never spliced into a shell string), so paths with spaces / unicode are
app/EngineClient.swift-12-// safe — same contract the installer relies on (plutil, not sed).
app/EngineClient.swift-13-
app/EngineClient.swift-14-import Foundation
app/EngineClient.swift-15-
app/EngineClient.swift-16-// MARK: - Public model
app/EngineClient.swift-17-
--
app/EngineClient.swift-70-    // --- configuration -----------------------------------------------------
app/EngineClient.swift-71-
app/EngineClient.swift-72-    /// LaunchAgent label. Default is the production label; tests override it
app/EngineClient.swift-73-    /// with a throwaway label so they never collide with the real agent.
app/EngineClient.swift-74-    let label: String
app/EngineClient.swift-75-
app/EngineClient.swift-76-    /// Home directory root. Default is the real HOME; tests point it at a
app/EngineClient.swift-77-    /// `mktemp -d` so the installer writes into a sandbox.
app/EngineClient.swift-78-    let home: String
app/EngineClient.swift-79-
app/EngineClient.swift:80:    /// Absolute path to the bundled installer.sh (in the .app Resources, or a
app/EngineClient.swift-81-    /// checkout path for tests).
app/EngineClient.swift-82-    let installerPath: String
app/EngineClient.swift-83-
app/EngineClient.swift-84-    /// ЯВНЫЙ оверрайд пути к `ebook-convert`. По умолчанию nil → путь целиком
app/EngineClient.swift-85-    /// определяет `CalibreLocator` (контракт детекта, инвариант 5).
app/EngineClient.swift-86-    ///
app/EngineClient.swift-87-    /// Оверрайд оставлен для тестов, которые подсовывают сюда `/bin/echo`: для
app/EngineClient.swift-88-    /// него проверяется ТОЛЬКО сам файл (без meta/polish рядом) — прежняя
app/EngineClient.swift-89-    /// M1-семантика «есть исполняемый файл = движок есть». Продакшен ходит
app/EngineClient.swift-90-    /// через локатор, где валидность = все три CLI.
--
app/EngineClient.swift-295-    /// FDA (v1.0.1): kick the agent WITHOUT `-k`. `-k` kills a running instance to
app/EngineClient.swift-296-    /// restart it — fine for install/repair, but here we only want to *wake* the
app/EngineClient.swift-297-    /// (short-lived, event-driven) agent so it re-probes the watch folder. There is
app/EngineClient.swift-298-    /// no batch to interrupt while access is denied, but "no `-k`" is safe by
app/EngineClient.swift-299-    /// construction (arch/plan-fda-synthesis.md, В4). Used by «Проверить снова».
app/EngineClient.swift-300-    @discardableResult
app/EngineClient.swift-301-    func kickstartGentle() -> RunResult {
app/EngineClient.swift-302-        run("/bin/launchctl", ["kickstart", serviceTarget])
app/EngineClient.swift-303-    }
app/EngineClient.swift-304-
app/EngineClient.swift:305:    /// FDA (v1.0.1): the ACTUAL Full-Disk-Access target — the runner launchd spawns,
app/EngineClient.swift:306:    /// i.e. `ProgramArguments[0]` of the installed plist (read via plutil, the same
app/EngineClient.swift-307-    /// typed value the installer wrote). This is the path the user must add/enable in
app/EngineClient.swift-308-    /// the FDA pane; we copy it to the clipboard. Falls back to the canonical
app/EngineClient.swift:309:    /// app-owned runner path (derived from `home`, never a literal `~`) when the plist
app/EngineClient.swift-310-    /// is absent/unreadable. Returns an absolute, tilde-expanded path.
app/EngineClient.swift:311:    func runnerPath() -> String {
app/EngineClient.swift:312:        let fallback = "\(home)/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh"
app/EngineClient.swift-313-        guard plistExists() else { return fallback }
app/EngineClient.swift-314-        let r = run("/usr/bin/plutil",
app/EngineClient.swift:315:                    ["-extract", "ProgramArguments.0", "raw", "-o", "-", plistPath])
app/EngineClient.swift-316-        guard r.status == 0 else { return fallback }
app/EngineClient.swift-317-        let value = r.stdout.trimmingCharacters(in: .whitespacesAndNewlines)
app/EngineClient.swift-318-        return value.isEmpty ? fallback : value
app/EngineClient.swift-319-    }
app/EngineClient.swift-320-
app/EngineClient.swift-321-    // MARK: - installer.sh
app/EngineClient.swift-322-
app/EngineClient.swift-323-    /// Run installer.sh with WATCH_DIR as a single argv element (no shell glue).
app/EngineClient.swift-324-    /// installer.sh itself does the heavy lifting: Calibre detect, plist via
app/EngineClient.swift-325-    /// plutil, bootout→bootstrap→enable→kickstart.
--
packaging/installer.sh-1-#!/bin/bash
packaging/installer.sh-2-# fb2-to-epub installer (all install logic lives here).
packaging/installer.sh-3-#
packaging/installer.sh-4-# Responsibilities:
packaging/installer.sh-5-#   - detect Calibre (ebook-convert) and python3 (clear message if missing)
packaging/installer.sh-6-#   - accept a WATCH_DIR (arg or env); create it if absent
packaging/installer.sh:7:#   - copy watcher + cover-finder + runner into App Support/bin
packaging/installer.sh-8-#   - generate the LaunchAgent plist via `plutil` (NOT sed) so arbitrary paths
packaging/installer.sh-9-#     with spaces / unicode are encoded safely
packaging/installer.sh-10-#   - (re)load the agent idempotently: bootout -> bootstrap -> enable -> kickstart
packaging/installer.sh-11-#   - print Full Disk Access guidance when WATCH_DIR is in a TCC-protected zone
packaging/installer.sh-12-#
packaging/installer.sh-13-# Usage:
packaging/installer.sh-14-#   installer.sh ["/path/to/watch folder"]
packaging/installer.sh-15-#   WATCH_DIR="/path/to/folder" installer.sh
packaging/installer.sh-16-# Default WATCH_DIR: ~/Desktop/fb2-to-epub
packaging/installer.sh-17-#
--
packaging/installer.sh-91-fi
packaging/installer.sh-92-APP_SUPPORT="$HOME/Library/Application Support/fb2-to-epub"
packaging/installer.sh-93-BIN_DIR="$APP_SUPPORT/bin"
packaging/installer.sh-94-PLIST="$HOME/Library/LaunchAgents/$LABEL.plist"
packaging/installer.sh-95-LOG_FILE="$HOME/Library/Logs/fb2-to-epub.log"
packaging/installer.sh-96-AGENT_PATH="/usr/bin:/bin:/usr/sbin:/sbin"
packaging/installer.sh-97-# Cover apply-jobs (M5): the app drops jobs here; this dir is in the agent's
packaging/installer.sh-98-# WatchPaths so a dropped job fires the agent. It must exist before (re)load.
packaging/installer.sh-99-COVERS_JOBS_DIR="$APP_SUPPORT/covers/jobs"
packaging/installer.sh-100-
packaging/installer.sh:101:RUNNER_DST="$BIN_DIR/fb2-to-epub-runner.sh"
packaging/installer.sh-102-WATCHER_DST="$BIN_DIR/fb2-to-epub-watcher.sh"
packaging/installer.sh-103-COVER_DST="$BIN_DIR/fb2-to-epub-cover-finder.py"
packaging/installer.sh-104-# FB3 support: transform + its genre map, installed next to the other bin scripts
packaging/installer.sh-105-# so the watcher (which resolves them relative to itself) finds them after install.
packaging/installer.sh-106-FB3_DST="$BIN_DIR/fb2-to-epub-fb3.py"
packaging/installer.sh-107-FB3_GENRE_DST="$BIN_DIR/fb2-to-epub-fb3-genre.json"
packaging/installer.sh-108-
packaging/installer.sh-109-# Resolve where our source scripts are. Search order:
packaging/installer.sh-110-#   1) FB2_SRC_DIR override (used by build/tests)
packaging/installer.sh-111-#   2) a sibling layout when run from a checkout (packaging/.. -> bin/, packaging/)
packaging/installer.sh:112:#   3) the same directory as this installer (the .app Resources layout)
packaging/installer.sh-113-SELF_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
packaging/installer.sh-114-find_src() {
packaging/installer.sh-115-  local name="$1"
packaging/installer.sh-116-  local c
packaging/installer.sh-117-  for c in \
packaging/installer.sh-118-    "${FB2_SRC_DIR:-}/$name" \
packaging/installer.sh-119-    "$SELF_DIR/$name" \
packaging/installer.sh-120-    "$SELF_DIR/../bin/$name" \
packaging/installer.sh-121-    "$SELF_DIR/bin/$name"; do
packaging/installer.sh-122-    [[ -n "$c" && -f "$c" ]] && { printf '%s' "$c"; return 0; }
--
packaging/installer.sh-286-  "~"|"~/"*) WATCH_DIR="$HOME/${WATCH_DIR#\~/}" ;;
packaging/installer.sh-287-esac
packaging/installer.sh-288-mkdir -p "$WATCH_DIR"
packaging/installer.sh-289-WATCH_DIR="$(cd "$WATCH_DIR" && pwd)"
packaging/installer.sh-290-
packaging/installer.sh-291-# ---------------------------------------------------------------------------
packaging/installer.sh-292-# 3. Copy scripts into App Support/bin
packaging/installer.sh-293-# ---------------------------------------------------------------------------
packaging/installer.sh-294-mkdir -p "$BIN_DIR" "$(dirname "$PLIST")" "$(dirname "$LOG_FILE")" "$COVERS_JOBS_DIR"
packaging/installer.sh-295-
packaging/installer.sh:296:src_runner="$(find_src fb2-to-epub-runner.sh)"   || { echo "fb2-to-epub: missing fb2-to-epub-runner.sh source" >&2; exit 1; }
packaging/installer.sh-297-src_watcher="$(find_src fb2-to-epub-watcher.sh)" || { echo "fb2-to-epub: missing fb2-to-epub-watcher.sh source" >&2; exit 1; }
packaging/installer.sh-298-src_cover="$(find_src fb2-to-epub-cover-finder.py)" || { echo "fb2-to-epub: missing fb2-to-epub-cover-finder.py source" >&2; exit 1; }
packaging/installer.sh-299-src_fb3="$(find_src fb2-to-epub-fb3.py)"            || { echo "fb2-to-epub: missing fb2-to-epub-fb3.py source" >&2; exit 1; }
packaging/installer.sh-300-src_fb3_genre="$(find_src fb2-to-epub-fb3-genre.json)" || { echo "fb2-to-epub: missing fb2-to-epub-fb3-genre.json source" >&2; exit 1; }
packaging/installer.sh-301-
packaging/installer.sh:302:# runner.sh is the FDA-granted "responsible" target — the TCC grant is keyed to
packaging/installer.sh-303-# this file. On update, only (re)install it if missing or actually changed, so an
packaging/installer.sh-304-# idempotent re-run never churns the file and risks dropping the user's FDA grant.
packaging/installer.sh:305:if [[ ! -f "$RUNNER_DST" ]] || ! cmp -s "$src_runner" "$RUNNER_DST"; then
packaging/installer.sh:306:  install -m 0755 "$src_runner" "$RUNNER_DST"
packaging/installer.sh-307-fi
packaging/installer.sh-308-install -m 0755 "$src_watcher" "$WATCHER_DST"
packaging/installer.sh-309-install -m 0755 "$src_cover"   "$COVER_DST"
packaging/installer.sh-310-install -m 0755 "$src_fb3"       "$FB3_DST"
packaging/installer.sh-311-install -m 0644 "$src_fb3_genre" "$FB3_GENRE_DST"
packaging/installer.sh-312-
packaging/installer.sh-313-# ---------------------------------------------------------------------------
packaging/installer.sh-314-# 4. Generate the LaunchAgent plist via plutil (safe for spaces/unicode)
packaging/installer.sh-315-#    Build a minimal valid skeleton, then insert/replace typed values whose
packaging/installer.sh-316-#    contents are passed as separate argv -> never spliced into XML.
--
packaging/installer.sh-321-  cat > "$out" <<'PLIST'
packaging/installer.sh-322-<?xml version="1.0" encoding="UTF-8"?>
packaging/installer.sh-323-<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
packaging/installer.sh-324-<plist version="1.0">
packaging/installer.sh-325-<dict/>
packaging/installer.sh-326-</plist>
packaging/installer.sh-327-PLIST
packaging/installer.sh-328-
packaging/installer.sh-329-  plutil -replace Label -string "$LABEL" "$out"
packaging/installer.sh-330-
packaging/installer.sh:331:  # ProgramArguments -> [ runner ]
packaging/installer.sh:332:  plutil -replace ProgramArguments -json '[]' "$out"
packaging/installer.sh:333:  plutil -insert  ProgramArguments.0 -string "$RUNNER_DST" "$out"
packaging/installer.sh-334-
packaging/installer.sh-335-  # WatchPaths -> [ WATCH_DIR, COVERS_JOBS_DIR ]
packaging/installer.sh-336-  # The watch folder fires the agent on new books; covers/jobs fires it when the
packaging/installer.sh-337-  # app drops a cover apply-job (M5) so the agent applies the chosen cover under
packaging/installer.sh-338-  # its Full Disk Access. The dir must EXIST for launchd to watch it (created
packaging/installer.sh-339-  # below before (re)load).
packaging/installer.sh-340-  plutil -replace WatchPaths -json '[]' "$out"
packaging/installer.sh-341-  plutil -insert  WatchPaths.0 -string "$WATCH_DIR" "$out"
packaging/installer.sh-342-  plutil -insert  WatchPaths.1 -string "$COVERS_JOBS_DIR" "$out"
packaging/installer.sh-343-
--
packaging/installer.sh-422-needs_fda=0
packaging/installer.sh-423-case "$WATCH_DIR/" in
packaging/installer.sh-424-  "$HOME/Desktop/"*|"$HOME/Documents/"*|"$HOME/Downloads/"*) needs_fda=1 ;;
packaging/installer.sh-425-esac
packaging/installer.sh-426-
packaging/installer.sh-427-cat <<EOF
packaging/installer.sh-428-fb2-to-epub installed.
packaging/installer.sh-429-
packaging/installer.sh-430-  Watch folder: $WATCH_DIR
packaging/installer.sh-431-  Agent label:  $LABEL
packaging/installer.sh:432:  Runner:       $RUNNER_DST
packaging/installer.sh-433-  LaunchAgent:  $PLIST
packaging/installer.sh-434-  Log:          $LOG_FILE
packaging/installer.sh-435-
packaging/installer.sh-436-Drop .fb2 / .fb2.zip files (or folders of them) into the watch folder.
packaging/installer.sh-437-EOF
packaging/installer.sh-438-
packaging/installer.sh-439-if [[ "$needs_fda" -eq 1 ]]; then
packaging/installer.sh-440-  cat <<EOF
packaging/installer.sh-441-
packaging/installer.sh-442-NOTE: Your watch folder is inside a macOS-protected location. If conversions do
packaging/installer.sh:443:not start, grant Full Disk Access to the runner:
packaging/installer.sh-444-
packaging/installer.sh-445-  System Settings -> Privacy & Security -> Full Disk Access -> "+"
packaging/installer.sh:446:  Add: $RUNNER_DST
packaging/installer.sh-447-  (press Cmd-Shift-G in the picker and paste the path above)
packaging/installer.sh-448-
packaging/installer.sh-449-Then toggle it on. The grant is keyed to that file and persists across updates.
packaging/installer.sh-450-EOF
packaging/installer.sh-451-fi
--
build/build-app.sh-1-#!/bin/bash
build/build-app.sh-2-# Build fb2-to-epub.app — NATIVE SwiftUI app (M0+).
build/build-app.sh-3-#
build/build-app.sh-4-# Steps:
build/build-app.sh-5-#   1. compile app/main.swift for arm64 + x86_64 (xcrun swiftc) and lipo them
build/build-app.sh-6-#      into a universal Contents/MacOS/fb2-to-epub
build/build-app.sh:7:#   2. copy installer.sh + watcher + cover-finder + runner into Contents/Resources
build/build-app.sh-8-#   3. build AppIcon.icns from branding/icon-app.svg (svg->png->iconutil)
build/build-app.sh-9-#   4. write a clean Info.plist from scratch: CFBundleIdentifier=com.arrivarus.fb2toepub
build/build-app.sh-10-#      (stable! a drifting id breaks TCC grants on every rebuild),
build/build-app.sh-11-#      CFBundleExecutable=fb2-to-epub, version, icon, LSMinimumSystemVersion=11.0
build/build-app.sh:12:#   5. ad-hoc codesign (-s -) and strict verify, inside a retry loop (iCloud
build/build-app.sh-13-#      FinderInfo race — see .patches/003)
build/build-app.sh-14-#
build/build-app.sh-15-# Unsandboxed, no external Swift deps (SwiftUI/AppKit/Foundation), offline build.
build/build-app.sh-16-#
build/build-app.sh-17-# Output: build/dist/fb2-to-epub.app
build/build-app.sh-18-#
build/build-app.sh-19-# Usage: build/build-app.sh [version]   (default version below)
build/build-app.sh-20-
build/build-app.sh-21-set -euo pipefail
build/build-app.sh-22-
--
build/build-app.sh-48-  "$REPO_DIR/app/EngineSetupCard.swift"
build/build-app.sh-49-  "$REPO_DIR/app/FolderAccessCard.swift"
build/build-app.sh-50-  "$REPO_DIR/app/CoverSelectView.swift"
build/build-app.sh-51-  "$REPO_DIR/app/SettingsView.swift"
build/build-app.sh-52-  "$REPO_DIR/app/UpdateChecker.swift"
build/build-app.sh-53-  "$REPO_DIR/app/CoverGenerator.swift"
build/build-app.sh-54-)
build/build-app.sh-55-ICON_SVG="$REPO_DIR/branding/icon-app.svg"
build/build-app.sh-56-
build/build-app.sh-57-# --- tool checks -----------------------------------------------------------
build/build-app.sh:58:for t in xcrun lipo sips iconutil plutil codesign; do
build/build-app.sh-59-  command -v "$t" >/dev/null 2>&1 || { echo "build-app: required tool '$t' not found" >&2; exit 1; }
build/build-app.sh-60-done
build/build-app.sh-61-xcrun --find swiftc >/dev/null 2>&1 || { echo "build-app: swiftc not found (install Xcode)" >&2; exit 1; }
build/build-app.sh-62-for s in "${SWIFT_SRCS[@]}"; do
build/build-app.sh-63-  [[ -f "$s" ]] || { echo "build-app: missing $s" >&2; exit 1; }
build/build-app.sh-64-done
build/build-app.sh-65-[[ -f "$ICON_SVG"  ]] || { echo "build-app: missing $ICON_SVG" >&2; exit 1; }
build/build-app.sh-66-
build/build-app.sh-67-SDK_PATH="$(xcrun --show-sdk-path --sdk macosx)"
build/build-app.sh-68-[[ -d "$SDK_PATH" ]] || { echo "build-app: macOS SDK not found via xcrun" >&2; exit 1; }
--
build/build-app.sh-84-else
build/build-app.sh-85-  echo "build-app: cairosvg not found. Install it:" >&2
build/build-app.sh-86-  echo "    python3 -m venv build/.venv && build/.venv/bin/pip install cairosvg" >&2
build/build-app.sh-87-  echo "    (needs system cairo: brew install cairo)" >&2
build/build-app.sh-88-  exit 1
build/build-app.sh-89-fi
build/build-app.sh-90-
build/build-app.sh-91-# --- clean + build native universal binary ---------------------------------
build/build-app.sh-92-rm -rf "$APP"
build/build-app.sh-93-MACOS="$APP/Contents/MacOS"
build/build-app.sh:94:RES="$APP/Contents/Resources"
build/build-app.sh-95-mkdir -p "$MACOS" "$RES"
build/build-app.sh-96-
build/build-app.sh-97-echo "==> compiling native SwiftUI binary (arm64 + x86_64)"
build/build-app.sh-98-BIN_TMP="$(mktemp -d)"
build/build-app.sh-99-for arch in arm64 x86_64; do
build/build-app.sh-100-  echo "    swiftc -> $arch"
build/build-app.sh-101-  xcrun swiftc \
build/build-app.sh-102-    -sdk "$SDK_PATH" \
build/build-app.sh-103-    -target "${arch}-apple-macos${MIN_MACOS}" \
build/build-app.sh-104-    -O \
--
build/build-app.sh-110-done
build/build-app.sh-111-
build/build-app.sh-112-echo "==> lipo -> universal $MACOS/$APP_NAME"
build/build-app.sh-113-lipo -create "$BIN_TMP/$APP_NAME-arm64" "$BIN_TMP/$APP_NAME-x86_64" \
build/build-app.sh-114-  -output "$MACOS/$APP_NAME"
build/build-app.sh-115-chmod 0755 "$MACOS/$APP_NAME"
build/build-app.sh-116-rm -rf "$BIN_TMP"
build/build-app.sh-117-lipo -info "$MACOS/$APP_NAME" | sed 's/^/    /'
build/build-app.sh-118-
build/build-app.sh-119-# --- bundle the install logic + scripts ------------------------------------
build/build-app.sh:120:echo "==> copying scripts into Resources"
build/build-app.sh-121-install -m 0755 "$REPO_DIR/packaging/installer.sh"             "$RES/installer.sh"
build/build-app.sh:122:install -m 0755 "$REPO_DIR/packaging/fb2-to-epub-runner.sh"    "$RES/fb2-to-epub-runner.sh"
build/build-app.sh-123-install -m 0755 "$REPO_DIR/bin/fb2-to-epub-watcher.sh"         "$RES/fb2-to-epub-watcher.sh"
build/build-app.sh-124-install -m 0755 "$REPO_DIR/bin/fb2-to-epub-cover-finder.py"    "$RES/fb2-to-epub-cover-finder.py"
build/build-app.sh-125-# FB3 support: the transform (executable, like cover-finder) + its genre map (data,
build/build-app.sh-126-# 0644 like the cover templates). The watcher resolves both next to itself.
build/build-app.sh-127-install -m 0755 "$REPO_DIR/bin/fb2-to-epub-fb3.py"            "$RES/fb2-to-epub-fb3.py"
build/build-app.sh-128-install -m 0644 "$REPO_DIR/bin/fb2-to-epub-fb3-genre.json"    "$RES/fb2-to-epub-fb3-genre.json"
build/build-app.sh-129-
build/build-app.sh-130-# --- bundle the typographic cover templates --------------------------------
build/build-app.sh-131-# CoverGenerator.swift renders a fallback book cover NATIVELY via an offscreen
build/build-app.sh-132-# WKWebView from these 4 approved tokenized SVG templates (text substitution +
build/build-app.sh-133-# width-fit happen in JS inside the webview). They must ship inside the bundle
build/build-app.sh:134:# at Contents/Resources/cover-templates/ so Bundle.main can find them at runtime.
build/build-app.sh:135:echo "==> copying cover templates into Resources/cover-templates"
build/build-app.sh-136-COVER_TPL_SRC="$REPO_DIR/design/cover-templates/tokenized"
build/build-app.sh-137-COVER_TPL_DST="$RES/cover-templates"
build/build-app.sh-138-mkdir -p "$COVER_TPL_DST"
build/build-app.sh-139-for n in 1 2 3 4; do
build/build-app.sh-140-  case "$n" in
build/build-app.sh-141-    1) f="tmpl-1-minimal.tok.svg" ;;
build/build-app.sh-142-    2) f="tmpl-2-classic.tok.svg" ;;
build/build-app.sh-143-    3) f="tmpl-3-modern.tok.svg" ;;
build/build-app.sh-144-    4) f="tmpl-4-dark.tok.svg" ;;
build/build-app.sh-145-  esac
--
build/build-app.sh-219-	<key>NSHighResolutionCapable</key>
build/build-app.sh-220-	<true/>
build/build-app.sh-221-	<key>NSPrincipalClass</key>
build/build-app.sh-222-	<string>NSApplication</string>
build/build-app.sh-223-</dict>
build/build-app.sh-224-</plist>
build/build-app.sh-225-PLIST_EOF
build/build-app.sh-226-plutil -lint "$PLIST" >/dev/null
build/build-app.sh-227-
build/build-app.sh-228-# --- strip xattrs + ad-hoc sign + verify (strict), with retry --------------
build/build-app.sh:229:# cairosvg/sips/iconutil/touch leave com.apple.FinderInfo / quarantine xattrs that
build/build-app.sh:230:# make `codesign --deep --strict` reject the bundle ("resource fork ... detritus
build/build-app.sh-231-# not allowed"). Worse: when the repo lives in a synced folder (iCloud/fileprovider),
build/build-app.sh-232-# the daemon re-stamps com.apple.FinderInfo onto the bundle ROOT directory
build/build-app.sh:233:# ASYNCHRONOUSLY — sometimes between the strip and codesign, or between codesign and
build/build-app.sh-234-# strict verify — so the failure is a RACE and reproduces only intermittently. That
build/build-app.sh-235-# xattr sits on the wrapper DIRECTORY, not on any signed payload, so clearing it just
build/build-app.sh-236-# before signing/verify is safe and does not invalidate the signature.
build/build-app.sh-237-# Note: do NOT `touch` the bundle root — touch re-adds FinderInfo and re-breaks strict.
build/build-app.sh-238-#
build/build-app.sh-239-# Strategy: run the full strip→sign→clean→verify sequence inside a retry loop (up to 5
build/build-app.sh:240:# attempts, ~1s pause between). Both codesign calls are guarded by `if` so a failed
build/build-app.sh-241-# attempt RETRIES instead of killing the script under `set -euo pipefail`.
build/build-app.sh:242:echo "==> ad-hoc codesign + strict verify (with retry)"
build/build-app.sh-243-CODESIGN_OK=0
build/build-app.sh-244-for attempt in 1 2 3 4 5; do
build/build-app.sh:245:  echo "==> codesign attempt $attempt/5"
build/build-app.sh-246-  # Full cleanup on every attempt — FinderInfo may have been re-stamped since last try.
build/build-app.sh-247-  find "$APP" -name '._*' -delete 2>/dev/null || true
build/build-app.sh-248-  find "$APP" -name '.DS_Store' -delete 2>/dev/null || true
build/build-app.sh-249-  xattr -cr "$APP" 2>/dev/null || true
build/build-app.sh-250-  xattr -d com.apple.FinderInfo "$APP" 2>/dev/null || true
build/build-app.sh-251-
build/build-app.sh:252:  if ! codesign --force --deep -s - "$APP"; then
build/build-app.sh:253:    echo "    codesign --force failed (attempt $attempt/5), retrying after 1s" >&2
build/build-app.sh-254-    sleep 1
build/build-app.sh-255-    continue
build/build-app.sh-256-  fi
build/build-app.sh-257-
build/build-app.sh-258-  # Dechunk FinderInfo again RIGHT before strict verify — this is the race guard.
build/build-app.sh-259-  xattr -d com.apple.FinderInfo "$APP" 2>/dev/null || true
build/build-app.sh-260-
build/build-app.sh:261:  if codesign --verify --deep --strict "$APP"; then
build/build-app.sh-262-    CODESIGN_OK=1
build/build-app.sh-263-    break
build/build-app.sh-264-  fi
build/build-app.sh-265-  echo "    strict verify failed (attempt $attempt/5), retrying after 1s" >&2
build/build-app.sh-266-  sleep 1
build/build-app.sh-267-done
build/build-app.sh-268-
build/build-app.sh-269-if [[ "$CODESIGN_OK" -ne 1 ]]; then
build/build-app.sh:270:  echo "build-app: codesign failed strict verify after 5 attempts (iCloud/fileprovider FinderInfo race)" >&2
build/build-app.sh-271-  exit 1
build/build-app.sh-272-fi
build/build-app.sh:273:{ codesign --verify --deep --strict --verbose=2 "$APP" 2>&1 || true; } | sed 's/^/    /'
build/build-app.sh-274-
build/build-app.sh-275-echo ""
build/build-app.sh-276-echo "Built: $APP"
build/build-app.sh-277-echo "  CFBundleIdentifier: $(plutil -extract CFBundleIdentifier raw -o - "$PLIST")"
build/build-app.sh-278-echo "  CFBundleExecutable: $(plutil -extract CFBundleExecutable raw -o - "$PLIST")"
build/build-app.sh-279-echo "  Version:            $(plutil -extract CFBundleShortVersionString raw -o - "$PLIST")"
build/build-app.sh-280-echo "  Architectures:      $(lipo -archs "$MACOS/$APP_NAME")"
build/build-app.sh-281-echo "  Icon:               $RES/AppIcon.icns"
app/EngineClient.swift-1-// EngineClient — thin Swift bridge between the SwiftUI app and the existing
app/EngineClient.swift-2-// shell engine (LaunchAgent + installer.sh + Calibre). M1.
app/EngineClient.swift-3-//
app/EngineClient.swift-4-// Design rules (from arch/plans-ui.md, "Инварианты" + "Контракты данных"):
app/EngineClient.swift-5-//   - The engine logic is NOT changed here. We only *call* installer.sh and
app/EngineClient.swift-6-//     read agent state via launchctl / plutil.
app/EngineClient.swift:7://   - runner.sh / watcher.sh are never invoked from Swift.
app/EngineClient.swift-8-//   - The app is unsandboxed, no external Swift deps: Foundation only.
app/EngineClient.swift-9-//
app/EngineClient.swift-10-// Everything here shells out via Process. Arguments are passed as a real argv
app/EngineClient.swift-11-// array (never spliced into a shell string), so paths with spaces / unicode are
app/EngineClient.swift-12-// safe — same contract the installer relies on (plutil, not sed).
app/EngineClient.swift-13-
app/EngineClient.swift-14-import Foundation
app/EngineClient.swift-15-
--
app/EngineClient.swift-287-        run("/bin/launchctl", ["bootstrap", domainTarget, plistPath])
app/EngineClient.swift-288-    }
app/EngineClient.swift-289-
app/EngineClient.swift-290-    @discardableResult
app/EngineClient.swift-291-    func kickstart() -> RunResult {
app/EngineClient.swift-292-        run("/bin/launchctl", ["kickstart", "-k", serviceTarget])
app/EngineClient.swift-293-    }
app/EngineClient.swift-294-
app/EngineClient.swift:295:    /// FDA (v1.0.1): kick the agent WITHOUT `-k`. `-k` kills a running instance to
app/EngineClient.swift-296-    /// restart it — fine for install/repair, but here we only want to *wake* the
app/EngineClient.swift-297-    /// (short-lived, event-driven) agent so it re-probes the watch folder. There is
app/EngineClient.swift-298-    /// no batch to interrupt while access is denied, but "no `-k`" is safe by
app/EngineClient.swift-299-    /// construction (arch/plan-fda-synthesis.md, В4). Used by «Проверить снова».
app/EngineClient.swift-300-    @discardableResult
app/EngineClient.swift-301-    func kickstartGentle() -> RunResult {
app/EngineClient.swift-302-        run("/bin/launchctl", ["kickstart", serviceTarget])
app/EngineClient.swift-303-    }
app/EngineClient.swift-304-
app/EngineClient.swift:305:    /// FDA (v1.0.1): the ACTUAL Full-Disk-Access target — the runner launchd spawns,
app/EngineClient.swift:306:    /// i.e. `ProgramArguments[0]` of the installed plist (read via plutil, the same
app/EngineClient.swift-307-    /// typed value the installer wrote). This is the path the user must add/enable in
app/EngineClient.swift:308:    /// the FDA pane; we copy it to the clipboard. Falls back to the canonical
app/EngineClient.swift:309:    /// app-owned runner path (derived from `home`, never a literal `~`) when the plist
app/EngineClient.swift-310-    /// is absent/unreadable. Returns an absolute, tilde-expanded path.
app/EngineClient.swift:311:    func runnerPath() -> String {
app/EngineClient.swift:312:        let fallback = "\(home)/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh"
app/EngineClient.swift-313-        guard plistExists() else { return fallback }
app/EngineClient.swift-314-        let r = run("/usr/bin/plutil",
app/EngineClient.swift:315:                    ["-extract", "ProgramArguments.0", "raw", "-o", "-", plistPath])
app/EngineClient.swift-316-        guard r.status == 0 else { return fallback }
app/EngineClient.swift-317-        let value = r.stdout.trimmingCharacters(in: .whitespacesAndNewlines)
app/EngineClient.swift-318-        return value.isEmpty ? fallback : value
app/EngineClient.swift-319-    }
app/EngineClient.swift-320-
app/EngineClient.swift-321-    // MARK: - installer.sh
app/EngineClient.swift-322-
app/EngineClient.swift-323-    /// Run installer.sh with WATCH_DIR as a single argv element (no shell glue).
--
app/FolderAccessCard.swift:1:// FolderAccessCard — the FDA-onboarding component (v1.0.1, D46), rendered in four
app/FolderAccessCard.swift-2-// presentations, mirroring EngineSetupCard's visual grammar and tokens WITHOUT
app/FolderAccessCard.swift-3-// touching it (its Phase automat is a just-shipped v1.0.0 surface — coupling the two
app/FolderAccessCard.swift-4-// would blow up the "4 подачи × N states" matrix, arch/plan-fda-synthesis.md).
app/FolderAccessCard.swift-5-//
app/FolderAccessCard.swift:6:// One problem (the agent has no Full Disk Access, so the watch folder is unreadable
app/FolderAccessCard.swift-7-// and books silently don't convert), FOUR подачи (mirror the engine onboarding):
app/FolderAccessCard.swift-8-//   • .blocker     — full-screen: warn ring + title + steps + CTA (replaces Status
app/FolderAccessCard.swift-9-//                    content when there is NO conversion history).
app/FolderAccessCard.swift-10-//   • .banner      — warn card under the header, Status content stays visible+muted
app/FolderAccessCard.swift-11-//                    (there IS history — conversion is equally blocked either way).
app/FolderAccessCard.swift-12-//   • .setupStep   — amber «ДОСТУП К ПАПКЕ» step body on first-run Setup (display-only,
app/FolderAccessCard.swift-13-//                    hands off to Status where the live flow lives).
app/FolderAccessCard.swift:14://   • .settingsRow — the compact warn row in Настройки (replaces the passive FDA row
app/FolderAccessCard.swift-15-//                    only while denied; ok/absent keeps today's byte-identical row).
app/FolderAccessCard.swift-16-//
app/FolderAccessCard.swift-17-// A tiny automat drives blocker/banner:  denied / checking / stillDenied / timeout.
app/FolderAccessCard.swift:18:// FDA-2 is DISPLAY-ONLY: every button defaults to a no-op; the real recheck/clipboard/
app/FolderAccessCard.swift:19:// panel pipeline is wired at FDA-3. Screenshots come via FB2_FORCE_FOLDER_ACCESS.
app/FolderAccessCard.swift-20-
app/FolderAccessCard.swift-21-import SwiftUI
app/FolderAccessCard.swift-22-import AppKit
app/FolderAccessCard.swift-23-
app/FolderAccessCard.swift-24-// MARK: - Local glyph helper (file-private; mirrors StatusView/EngineSetupCard)
app/FolderAccessCard.swift-25-
app/FolderAccessCard.swift-26-private func faIcon(_ name: String, size: CGFloat, weight: Font.Weight = .regular) -> some View {
app/FolderAccessCard.swift-27-    Image(systemName: name)
app/FolderAccessCard.swift-28-        .font(.system(size: size, weight: weight))
app/FolderAccessCard.swift-29-}
app/FolderAccessCard.swift-30-
app/FolderAccessCard.swift-31-// MARK: - FolderAccessCard
app/FolderAccessCard.swift-32-
app/FolderAccessCard.swift-33-struct FolderAccessCard: View {
app/FolderAccessCard.swift-34-
app/FolderAccessCard.swift-35-    /// The tiny recheck automat. `denied` is the resting problem state; the other
app/FolderAccessCard.swift:36:    /// three are transient states the host drives during «Проверить снова» (FDA-3).
app/FolderAccessCard.swift-37-    enum State: Equatable {
app/FolderAccessCard.swift-38-        case denied         // no access — the resting problem
app/FolderAccessCard.swift-39-        case checking       // «Проверить снова» pressed, waiting for a fresh probe
app/FolderAccessCard.swift-40-        case stillDenied    // the fresh probe still says denied
app/FolderAccessCard.swift-41-        case timeout        // the agent never answered (kickstart failed / agent off)
app/FolderAccessCard.swift-42-
app/FolderAccessCard.swift-43-        /// The two RESTING outcomes the host may PIN after a recheck coordinator tears
app/FolderAccessCard.swift-44-        /// down. Distinguished from the transient `.checking` and the live-derived
--
app/FolderAccessCard.swift-48-    }
app/FolderAccessCard.swift-49-
app/FolderAccessCard.swift-50-    /// Which of the four surfaces to render.
app/FolderAccessCard.swift-51-    enum Presentation { case blocker, banner, setupStep, settingsRow }
app/FolderAccessCard.swift-52-
app/FolderAccessCard.swift-53-    let state: State
app/FolderAccessCard.swift-54-    let presentation: Presentation
app/FolderAccessCard.swift-55-
app/FolderAccessCard.swift:56:    // Actions — DRAWN but inert in FDA-2 (defaults are no-ops). Wired at FDA-3.
app/FolderAccessCard.swift:57:    /// Primary: open the Full Disk Access pane AND copy the runner path to the clipboard.
app/FolderAccessCard.swift-58-    var onOpenSettings: () -> Void = {}
app/FolderAccessCard.swift-59-    /// Secondary: kick the agent and wait for a fresh probe.
app/FolderAccessCard.swift-60-    var onRecheck: () -> Void = {}
app/FolderAccessCard.swift-61-    /// Setup step only: hand off to Status (where the live blocker/banner lives).
app/FolderAccessCard.swift-62-    var onFixFromSetup: () -> Void = {}
app/FolderAccessCard.swift-63-
app/FolderAccessCard.swift-64-    var body: some View {
app/FolderAccessCard.swift-65-        switch presentation {
app/FolderAccessCard.swift-66-        case .blocker:     blocker
app/FolderAccessCard.swift-67-        case .banner:      banner
app/FolderAccessCard.swift-68-        case .setupStep:   setupStep
app/FolderAccessCard.swift-69-        case .settingsRow: settingsRow
app/FolderAccessCard.swift-70-        }
app/FolderAccessCard.swift-71-    }
app/FolderAccessCard.swift-72-
app/FolderAccessCard.swift:73:    // MARK: - Shared texts (ты-форма, honest — never asserts "FDA запрещён")
app/FolderAccessCard.swift-74-
app/FolderAccessCard.swift-75-    // The three numbered steps (same wording A/B; the badge style differs by direction).
app/FolderAccessCard.swift-76-    private static let step1 = "Нажми «+» под списком."
app/FolderAccessCard.swift-77-    private static let step2 = "Cmd-Shift-G и вставь путь — он уже в буфере."
app/FolderAccessCard.swift-78-    private static let step3lead = "Включи переключатель у "
app/FolderAccessCard.swift:79:    private static let step3accent = "fb2-to-epub-runner.sh"
app/FolderAccessCard.swift-80-
app/FolderAccessCard.swift-81-    private var blockerTitleText: String {
app/FolderAccessCard.swift-82-        state == .timeout ? "Агент не ответил" : "Нет доступа к папке"
app/FolderAccessCard.swift-83-    }
app/FolderAccessCard.swift-84-    private var bodyText: String {
app/FolderAccessCard.swift-85-        state == .timeout
app/FolderAccessCard.swift-86-            ? "Фоновый агент не отозвался. Проверь, что он активен, и попробуй ещё раз."
app/FolderAccessCard.swift-87-            : "macOS прячет «Рабочий стол», «Документы» и «Загрузки» от фоновых программ. "
--
app/FolderAccessCard.swift-96-        case .timeout:     return nil    // the timeout message is the body itself
app/FolderAccessCard.swift-97-        }
app/FolderAccessCard.swift-98-    }
app/FolderAccessCard.swift-99-    private var isChecking: Bool { state == .checking }
app/FolderAccessCard.swift-100-}
app/FolderAccessCard.swift-101-
app/FolderAccessCard.swift-102-// MARK: - Reusable buttons (tokens-exact; file-private mirrors of EngineSetupCard's)
app/FolderAccessCard.swift-103-
app/FolderAccessCard.swift:104:/// `.cta` — the big blocker button (Direction B). Orange fill for FDA.
app/FolderAccessCard.swift-105-private struct FaCtaBig: View {
app/FolderAccessCard.swift-106-    let title: String
app/FolderAccessCard.swift-107-    var icon: String? = nil
app/FolderAccessCard.swift-108-    var disabled: Bool = false
app/FolderAccessCard.swift-109-    let action: () -> Void
app/FolderAccessCard.swift-110-    var body: some View {
app/FolderAccessCard.swift-111-        HStack(spacing: Tokens.CO.ctaGap) {
app/FolderAccessCard.swift-112-            if let icon = icon {
--
app/FolderAccessCard.swift-366-                .padding(.top, Tokens.CO.blBodyTop)
app/FolderAccessCard.swift-367-
app/FolderAccessCard.swift-368-            // Steps hidden while actively checking (the spinner + status carry it).
app/FolderAccessCard.swift-369-            if !isChecking {
app/FolderAccessCard.swift-370-                VStack(alignment: .leading, spacing: Tokens.CO.stepsGapB) {
app/FolderAccessCard.swift-371-                    FaStep(dir: .b, n: 1, leading: Self.step1)
app/FolderAccessCard.swift-372-                    FaStep(dir: .b, n: 2, leading: Self.step2)
app/FolderAccessCard.swift-373-                    FaStep(dir: .b, n: 3, leading: Self.step3lead,
app/FolderAccessCard.swift:374:                           accent: Self.step3accent, accentMono: true, trailing: ".")
app/FolderAccessCard.swift-375-                }
app/FolderAccessCard.swift-376-                .frame(maxWidth: .infinity, alignment: .leading)
app/FolderAccessCard.swift-377-                .padding(.top, Tokens.CO.stepsTopB)
app/FolderAccessCard.swift-378-            } else {
app/FolderAccessCard.swift-379-                FaProgressBar().padding(.top, Tokens.CO.stepsTopB)
app/FolderAccessCard.swift-380-            }
app/FolderAccessCard.swift-381-
app/FolderAccessCard.swift-382-            if let fb = feedbackText {
--
app/FolderAccessCard.swift-431-
app/FolderAccessCard.swift-432-            if isChecking {
app/FolderAccessCard.swift-433-                FaProgressBar().padding(.top, Tokens.CO.bActionsTop)
app/FolderAccessCard.swift-434-            } else {
app/FolderAccessCard.swift-435-                VStack(alignment: .leading, spacing: Tokens.CO.stepsGapA) {
app/FolderAccessCard.swift-436-                    FaStep(dir: .a, n: 1, leading: Self.step1)
app/FolderAccessCard.swift-437-                    FaStep(dir: .a, n: 2, leading: Self.step2)
app/FolderAccessCard.swift-438-                    FaStep(dir: .a, n: 3, leading: Self.step3lead,
app/FolderAccessCard.swift:439:                           accent: Self.step3accent, accentMono: true, trailing: ".")
app/FolderAccessCard.swift-440-                }
app/FolderAccessCard.swift-441-                .padding(.top, Tokens.CO.stepsTopA)
app/FolderAccessCard.swift-442-
app/FolderAccessCard.swift-443-                HStack(spacing: Tokens.CO.bActionsGap) {
app/FolderAccessCard.swift-444-                    FaCtaSmall(title: "Открыть настройки", icon: "arrow.up.right.square",
app/FolderAccessCard.swift-445-                               size: .big, fill: .orange, action: onOpenSettings)
app/FolderAccessCard.swift-446-                    FaLinkBtn(title: "Проверить снова", orange: true, action: onRecheck)
app/FolderAccessCard.swift-447-                }
--
app/FolderAccessCard.swift-496-    }
app/FolderAccessCard.swift-497-}
app/FolderAccessCard.swift-498-
app/FolderAccessCard.swift-499-// MARK: - Presentation: SETTINGS ROW (compact warn row)
app/FolderAccessCard.swift-500-
app/FolderAccessCard.swift-501-extension FolderAccessCard {
app/FolderAccessCard.swift-502-
app/FolderAccessCard.swift-503-    /// The warn Настройки row shown ONLY while denied (SettingsView keeps its own
app/FolderAccessCard.swift:504:    /// passive "Full Disk Access" row for ok/absent — byte-identical to today).
app/FolderAccessCard.swift-505-    var settingsRow: some View {
app/FolderAccessCard.swift-506-        HStack(spacing: Tokens.M.rowGap) {
app/FolderAccessCard.swift-507-            RoundedRectangle(cornerRadius: Tokens.M.rowIconRadius, style: .continuous)
app/FolderAccessCard.swift-508-                .fill(Tokens.C.tintOrange)
app/FolderAccessCard.swift-509-                .overlay(faIcon("lock.shield", size: 15, weight: .semibold)
app/FolderAccessCard.swift-510-                    .foregroundColor(Tokens.C.accentOrange))
app/FolderAccessCard.swift-511-                .frame(width: Tokens.M.rowIcon, height: Tokens.M.rowIcon)
app/FolderAccessCard.swift-512-            VStack(alignment: .leading, spacing: 1) {
    exit 1
  fi
fi

# ebook-convert/meta/polish — производные ОДНОГО CALIBRE_MACOS_DIR (их и пишем в
# plist). Env-оверрайды остаются рабочими для нестандартных раскладок.
EBOOK_CONVERT="${EBOOK_CONVERT:-$CALIBRE_MACOS_DIR/ebook-convert}"
EBOOK_META="${EBOOK_META:-$CALIBRE_MACOS_DIR/ebook-meta}"
EBOOK_POLISH="${EBOOK_POLISH:-$CALIBRE_MACOS_DIR/ebook-polish}"

missing=()
[[ -x "$EBOOK_META" ]]   || missing+=("ebook-meta   ($EBOOK_META)")
[[ -x "$EBOOK_POLISH" ]] || missing+=("ebook-polish ($EBOOK_POLISH)")
if [[ ${#missing[@]} -gt 0 ]]; then
  if calibre_detect_only; then printf 'CALIBRE_MACOS_DIR=NONE\n'; exit 0; fi
  {
    echo "fb2-to-epub: Calibre is installed but some required tools are missing:"
    echo
    for m in "${missing[@]}"; do echo "  - $m"; done
    echo
    echo "These ship with a normal Calibre install. Update Calibre, then re-run:"
    echo "  - Download: https://calibre-ebook.com/download_osx"
    echo "  - or:       brew upgrade --cask calibre"
  } >&2
  exit 1
fi

# Детект завершён и валиден. В detect-only выходим ЗДЕСЬ — до создания папок,
# копирования скриптов, генерации plist и launchctl (parity-тест ничего не пишет).
if calibre_detect_only; then
  printf 'CALIBRE_MACOS_DIR=%s\n' "$CALIBRE_MACOS_DIR"
  printf 'EBOOK_CONVERT=%s\n' "$EBOOK_CONVERT"
  printf 'EBOOK_META=%s\n' "$EBOOK_META"
  printf 'EBOOK_POLISH=%s\n' "$EBOOK_POLISH"
  exit 0
fi

detect_python3() {
  local cand
  for cand in /usr/bin/python3 /opt/homebrew/bin/python3 /usr/local/bin/python3; do
    [[ -x "$cand" ]] && { printf '%s' "$cand"; return 0; }
  done
  cand="$(command -v python3 2>/dev/null || true)"
  [[ -n "$cand" ]] && { printf '%s' "$cand"; return 0; }
  return 1
}
if ! PYTHON3="$(detect_python3)"; then
  cat >&2 <<EOF
fb2-to-epub: python3 not found.

Install the Xcode Command Line Tools (provides /usr/bin/python3):
  xcode-select --install

Then run this installer again.
EOF
  exit 1
fi

# ---------------------------------------------------------------------------
# 2. Resolve WATCH_DIR
# ---------------------------------------------------------------------------
WATCH_DIR="${1:-${WATCH_DIR:-$HOME/Desktop/fb2-to-epub}}"
# Normalize a literal leading tilde from user input (it would not expand inside
# the quoted arg/env). We match the literal '~' on purpose, then expand via HOME.
# shellcheck disable=SC2088
case "$WATCH_DIR" in
  "~"|"~/"*) WATCH_DIR="$HOME/${WATCH_DIR#\~/}" ;;
esac
mkdir -p "$WATCH_DIR"
WATCH_DIR="$(cd "$WATCH_DIR" && pwd)"

# ---------------------------------------------------------------------------
# 3. Copy scripts into App Support/bin
# ---------------------------------------------------------------------------
mkdir -p "$BIN_DIR" "$(dirname "$PLIST")" "$(dirname "$LOG_FILE")" "$COVERS_JOBS_DIR"

src_runner="$(find_src fb2-to-epub-runner.sh)"   || { echo "fb2-to-epub: missing fb2-to-epub-runner.sh source" >&2; exit 1; }
src_watcher="$(find_src fb2-to-epub-watcher.sh)" || { echo "fb2-to-epub: missing fb2-to-epub-watcher.sh source" >&2; exit 1; }
src_cover="$(find_src fb2-to-epub-cover-finder.py)" || { echo "fb2-to-epub: missing fb2-to-epub-cover-finder.py source" >&2; exit 1; }
src_fb3="$(find_src fb2-to-epub-fb3.py)"            || { echo "fb2-to-epub: missing fb2-to-epub-fb3.py source" >&2; exit 1; }
src_fb3_genre="$(find_src fb2-to-epub-fb3-genre.json)" || { echo "fb2-to-epub: missing fb2-to-epub-fb3-genre.json source" >&2; exit 1; }

# runner.sh is the FDA-granted "responsible" target — the TCC grant is keyed to
# this file. On update, only (re)install it if missing or actually changed, so an
# idempotent re-run never churns the file and risks dropping the user's FDA grant.
if [[ ! -f "$RUNNER_DST" ]] || ! cmp -s "$src_runner" "$RUNNER_DST"; then
  install -m 0755 "$src_runner" "$RUNNER_DST"
fi
install -m 0755 "$src_watcher" "$WATCHER_DST"
install -m 0755 "$src_cover"   "$COVER_DST"
install -m 0755 "$src_fb3"       "$FB3_DST"
install -m 0644 "$src_fb3_genre" "$FB3_GENRE_DST"

# ---------------------------------------------------------------------------
# 4. Generate the LaunchAgent plist via plutil (safe for spaces/unicode)
#    Build a minimal valid skeleton, then insert/replace typed values whose
#    contents are passed as separate argv -> never spliced into XML.
# ---------------------------------------------------------------------------
gen_plist() {
  local out="$1"
  # Minimal valid empty-dict plist to seed plutil edits.
  cat > "$out" <<'PLIST'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict/>
</plist>
PLIST

  plutil -replace Label -string "$LABEL" "$out"

  # ProgramArguments -> [ runner ]
  plutil -replace ProgramArguments -json '[]' "$out"
  plutil -insert  ProgramArguments.0 -string "$RUNNER_DST" "$out"

  # WatchPaths -> [ WATCH_DIR, COVERS_JOBS_DIR ]
  # The watch folder fires the agent on new books; covers/jobs fires it when the
  # app drops a cover apply-job (M5) so the agent applies the chosen cover under
  # its Full Disk Access. The dir must EXIST for launchd to watch it (created
  # below before (re)load).
  plutil -replace WatchPaths -json '[]' "$out"
  plutil -insert  WatchPaths.0 -string "$WATCH_DIR" "$out"
  plutil -insert  WatchPaths.1 -string "$COVERS_JOBS_DIR" "$out"

  # EnvironmentVariables -> { WATCH_DIR, PATH, CALIBRE_MACOS_DIR, EBOOK_CONVERT,
  #                           EBOOK_META, EBOOK_POLISH, PYTHON3 }
  #
  # CALIBRE_MACOS_DIR (CAL-1) — ОДИН источник правды о том, где движок: агент
  # переживает переезд Calibre (наша папка ↔ /Applications) без гадания по путям.
  # Три EBOOK_* остаются абсолютами и производными от него — старый watcher,
  # который читает только их, продолжает работать без изменений.
  plutil -replace EnvironmentVariables -json '{}' "$out"
  plutil -insert  EnvironmentVariables.WATCH_DIR     -string "$WATCH_DIR"     "$out"
  plutil -insert  EnvironmentVariables.PATH          -string "$AGENT_PATH"    "$out"
  plutil -insert  EnvironmentVariables.CALIBRE_MACOS_DIR -string "$CALIBRE_MACOS_DIR" "$out"
  plutil -insert  EnvironmentVariables.EBOOK_CONVERT -string "$EBOOK_CONVERT" "$out"
  plutil -insert  EnvironmentVariables.EBOOK_META    -string "$EBOOK_META"    "$out"
  plutil -insert  EnvironmentVariables.EBOOK_POLISH  -string "$EBOOK_POLISH"  "$out"
  plutil -insert  EnvironmentVariables.PYTHON3       -string "$PYTHON3"       "$out"

  # H1 (ре-ревью, цикл 2): ТОЛЬКО под тест-защёлкой мутации закрепляем в plist HOME и
  # пути state/covers/log, ПРОИЗВОДНЫЕ от throwaway-HOME теста. Иначе агент, который
  # РЕАЛЬНЫЙ launchd поднимает по этому plist, не наследует env теста и пишет боевой
  # state.json / дренирует боевые covers-jobs / пишет боевой лог (launchd НЕ пиннит
  # throwaway-HOME). Значения — ровно по формуле дефолтов watcher
  # (bin/fb2-to-epub-watcher.sh §env: LOG_FILE/STATE_DIR/COVERS_DIR); явный env-оверрайд
  # уважается (за throwaway-корректность оверрайдов отвечает харнесс). БЕЗ защёлки
  # (боевая установка) блок пропускается → plist БАЙТ-В-БАЙТ прежний (обязательный
  # негатив-тест). Swift-семантика защёлки — app/CalibreLocator.swift → CalibreTestLatch.
  if latch_allows_mutation; then
    plutil -insert EnvironmentVariables.HOME           -string "$HOME"                                                                      "$out"
    plutil -insert EnvironmentVariables.FB2_STATE_DIR  -string "${FB2_STATE_DIR:-$HOME/Library/Application Support/fb2-to-epub/state}"       "$out"
    plutil -insert EnvironmentVariables.FB2_COVERS_DIR -string "${FB2_COVERS_DIR:-$HOME/Library/Application Support/fb2-to-epub/covers}"     "$out"
    plutil -insert EnvironmentVariables.FB2_LOG_FILE   -string "${FB2_LOG_FILE:-$HOME/Library/Logs/fb2-to-epub.log}"                         "$out"
  fi

  plutil -replace RunAtLoad       -bool true "$out"
  plutil -replace ThrottleInterval -integer 5 "$out"
  plutil -replace StandardOutPath  -string "$LOG_FILE" "$out"
  plutil -replace StandardErrorPath -string "$LOG_FILE" "$out"

  # Final sanity: must be a valid plist.
  plutil -lint "$out" >/dev/null
}

# Generate into a temp file, then atomically move it into place. mktemp gives a
# bare name; appending .plist keeps `plutil` happy without orphaning the base
# file. The trap cleans up the temp on any early exit (set -e).
tmp_plist_base="$(mktemp -t fb2plist)"
tmp_plist="$tmp_plist_base.plist"
trap 'rm -f "$tmp_plist_base" "$tmp_plist"' EXIT
gen_plist "$tmp_plist"
mv -f "$tmp_plist" "$PLIST"
rm -f "$tmp_plist_base"
trap - EXIT

# ---------------------------------------------------------------------------
# 5. (Re)load the agent idempotently
# ---------------------------------------------------------------------------
domain="gui/$(id -u)"

# Migration: remove the legacy hand-installed agent (com.user.fb2-to-epub).
# Earlier manual/CLI installs left a SEPARATE agent watching the same folder,
# which double-converts alongside ours. Remove it (idempotent) so exactly one
# agent (ours) remains. Touches ONLY this specific legacy label + its plist.
LEGACY_LABEL="com.user.fb2-to-epub"
LEGACY_PLIST="$HOME/Library/LaunchAgents/$LEGACY_LABEL.plist"
if [[ -f "$LEGACY_PLIST" ]] || launchctl print "$domain/$LEGACY_LABEL" >/dev/null 2>&1; then
  launchctl bootout "$domain/$LEGACY_LABEL" 2>/dev/null || true
  rm -f "$LEGACY_PLIST"
  echo "migration: removed legacy agent $LEGACY_LABEL"
fi

# bootout is best-effort (agent may not be loaded yet); ignore its failure.
launchctl bootout "$domain/$LABEL" 2>/dev/null || true
launchctl bootstrap "$domain" "$PLIST"
launchctl enable "$domain/$LABEL" 2>/dev/null || true
launchctl kickstart -k "$domain/$LABEL" 2>/dev/null || true

# ---------------------------------------------------------------------------
# 6. Full Disk Access guidance for TCC-protected zones
# ---------------------------------------------------------------------------
needs_fda=0
case "$WATCH_DIR/" in
  "$HOME/Desktop/"*|"$HOME/Documents/"*|"$HOME/Downloads/"*) needs_fda=1 ;;
esac

cat <<EOF
fb2-to-epub installed.

  Watch folder: $WATCH_DIR
  Agent label:  $LABEL
  Runner:       $RUNNER_DST
  LaunchAgent:  $PLIST
  Log:          $LOG_FILE

Drop .fb2 / .fb2.zip files (or folders of them) into the watch folder.
EOF

if [[ "$needs_fda" -eq 1 ]]; then
  cat <<EOF

NOTE: Your watch folder is inside a macOS-protected location. If conversions do
not start, grant Full Disk Access to the runner:

  System Settings -> Privacy & Security -> Full Disk Access -> "+"
  Add: $RUNNER_DST
  (press Cmd-Shift-G in the picker and paste the path above)

Then toggle it on. The grant is keyed to that file and persists across updates.
EOF
fi

# --- clean + build native universal binary ---------------------------------
rm -rf "$APP"
MACOS="$APP/Contents/MacOS"
RES="$APP/Contents/Resources"
mkdir -p "$MACOS" "$RES"

echo "==> compiling native SwiftUI binary (arm64 + x86_64)"
BIN_TMP="$(mktemp -d)"
for arch in arm64 x86_64; do
  echo "    swiftc -> $arch"
  xcrun swiftc \
    -sdk "$SDK_PATH" \
    -target "${arch}-apple-macos${MIN_MACOS}" \
    -O \
    "${SWIFT_SRCS[@]}" \
    -o "$BIN_TMP/$APP_NAME-$arch" 2>&1 | sed 's/^/    /'
  # swiftc exit code is hidden by the pipe to sed — verify the artifact exists.
  [[ -f "$BIN_TMP/$APP_NAME-$arch" ]] || {
    echo "build-app: swiftc failed to produce $arch binary" >&2; rm -rf "$BIN_TMP"; exit 1; }
done

echo "==> lipo -> universal $MACOS/$APP_NAME"
lipo -create "$BIN_TMP/$APP_NAME-arm64" "$BIN_TMP/$APP_NAME-x86_64" \
  -output "$MACOS/$APP_NAME"
chmod 0755 "$MACOS/$APP_NAME"
rm -rf "$BIN_TMP"
lipo -info "$MACOS/$APP_NAME" | sed 's/^/    /'

# --- bundle the install logic + scripts ------------------------------------
echo "==> copying scripts into Resources"
install -m 0755 "$REPO_DIR/packaging/installer.sh"             "$RES/installer.sh"
install -m 0755 "$REPO_DIR/packaging/fb2-to-epub-runner.sh"    "$RES/fb2-to-epub-runner.sh"
install -m 0755 "$REPO_DIR/bin/fb2-to-epub-watcher.sh"         "$RES/fb2-to-epub-watcher.sh"
install -m 0755 "$REPO_DIR/bin/fb2-to-epub-cover-finder.py"    "$RES/fb2-to-epub-cover-finder.py"
# FB3 support: the transform (executable, like cover-finder) + its genre map (data,
# 0644 like the cover templates). The watcher resolves both next to itself.
install -m 0755 "$REPO_DIR/bin/fb2-to-epub-fb3.py"            "$RES/fb2-to-epub-fb3.py"
install -m 0644 "$REPO_DIR/bin/fb2-to-epub-fb3-genre.json"    "$RES/fb2-to-epub-fb3-genre.json"

# --- bundle the typographic cover templates --------------------------------
# CoverGenerator.swift renders a fallback book cover NATIVELY via an offscreen
# WKWebView from these 4 approved tokenized SVG templates (text substitution +
# width-fit happen in JS inside the webview). They must ship inside the bundle
# at Contents/Resources/cover-templates/ so Bundle.main can find them at runtime.
echo "==> copying cover templates into Resources/cover-templates"
COVER_TPL_SRC="$REPO_DIR/design/cover-templates/tokenized"
COVER_TPL_DST="$RES/cover-templates"
mkdir -p "$COVER_TPL_DST"
for n in 1 2 3 4; do
  case "$n" in
    1) f="tmpl-1-minimal.tok.svg" ;;
    2) f="tmpl-2-classic.tok.svg" ;;
    3) f="tmpl-3-modern.tok.svg" ;;
    4) f="tmpl-4-dark.tok.svg" ;;
  esac
  [[ -f "$COVER_TPL_SRC/$f" ]] || { echo "build-app: missing cover template $COVER_TPL_SRC/$f" >&2; exit 1; }
  install -m 0644 "$COVER_TPL_SRC/$f" "$COVER_TPL_DST/$f"
done

# --- icon: SVG -> PNG set -> .icns -----------------------------------------
echo "==> building AppIcon.icns from $(basename "$ICON_SVG")"
ICON_TMP="$(mktemp -d)"
ICONSET="$ICON_TMP/AppIcon.iconset"
mkdir -p "$ICONSET"

# Render the SVG once at high resolution with cairosvg (transparent background, exact
# 1024x1024). Then downscale with sips to each required size — sips preserves the alpha
# channel, so the squircle's rounded corners stay transparent all the way down.
BASE_PNG="$ICON_TMP/base-1024.png"
"${CAIROSVG[@]}" "$ICON_SVG" -o "$BASE_PNG" --output-width 1024 --output-height 1024 2>&1 | sed 's/^/    /' || true
if [[ ! -f "$BASE_PNG" ]]; then
  echo "build-app: failed to rasterize SVG to PNG via cairosvg" >&2
  rm -rf "$ICON_TMP"; exit 1
fi

# Apple iconset requires these named sizes (1x + 2x).
make_size() { sips -z "$2" "$2" "$BASE_PNG" --out "$ICONSET/$1" >/dev/null; }
make_size icon_16x16.png        16
make_size icon_16x16@2x.png     32
make_size icon_32x32.png        32
make_size icon_32x32@2x.png     64
make_size icon_128x128.png     128
make_size icon_128x128@2x.png  256
make_size icon_256x256.png     256
make_size icon_256x256@2x.png  512
make_size icon_512x512.png     512
make_size icon_512x512@2x.png 1024

iconutil -c icns "$ICONSET" -o "$RES/AppIcon.icns"
# Native bundle: the icon resolves straight from CFBundleIconFile=AppIcon → AppIcon.icns.
# No applet.icns / applet.rsrc legacy carriers here (those were osacompile artifacts).
rm -rf "$ICON_TMP"

# --- Info.plist: clean, written from scratch (native bundle) ---------------
# A native bundle has NO pre-seeded Info.plist (unlike osacompile, which dumped a
# pile of Carbon/AppleEvents keys). Write exactly the keys we want — stable id,
# native executable name, dark-capable windowed app. No LSUIElement (plain window).
# No CFBundleIconName (that key would override CFBundleIconFile and, without an
# Assets.car, fall back to the generic icon — see .patches/002).
echo "==> writing Info.plist (id=$BUNDLE_ID, exec=$APP_NAME, version=$VERSION)"
PLIST="$APP/Contents/Info.plist"
cat > "$PLIST" <<PLIST_EOF
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleIdentifier</key>
	<string>$BUNDLE_ID</string>
	<key>CFBundleExecutable</key>
	<string>$APP_NAME</string>
	<key>CFBundleName</key>
	<string>$APP_NAME</string>
	<key>CFBundleDisplayName</key>
	<string>$APP_NAME</string>
	<key>CFBundleShortVersionString</key>
	<string>$VERSION</string>
	<key>CFBundleVersion</key>
	<string>$VERSION</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>LSMinimumSystemVersion</key>
	<string>$MIN_MACOS</string>
	<key>NSHighResolutionCapable</key>
	<true/>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
</dict>
</plist>
PLIST_EOF
plutil -lint "$PLIST" >/dev/null

# --- strip xattrs + ad-hoc sign + verify (strict), with retry --------------
# cairosvg/sips/iconutil/touch leave com.apple.FinderInfo / quarantine xattrs that
# make `codesign --deep --strict` reject the bundle ("resource fork ... detritus
# not allowed"). Worse: when the repo lives in a synced folder (iCloud/fileprovider),
# the daemon re-stamps com.apple.FinderInfo onto the bundle ROOT directory
# ASYNCHRONOUSLY — sometimes between the strip and codesign, or between codesign and
# strict verify — so the failure is a RACE and reproduces only intermittently. That
# xattr sits on the wrapper DIRECTORY, not on any signed payload, so clearing it just
# before signing/verify is safe and does not invalidate the signature.
# Note: do NOT `touch` the bundle root — touch re-adds FinderInfo and re-breaks strict.
#
# Strategy: run the full strip→sign→clean→verify sequence inside a retry loop (up to 5
# attempts, ~1s pause between). Both codesign calls are guarded by `if` so a failed
# attempt RETRIES instead of killing the script under `set -euo pipefail`.
echo "==> ad-hoc codesign + strict verify (with retry)"
CODESIGN_OK=0
for attempt in 1 2 3 4 5; do
  echo "==> codesign attempt $attempt/5"
  # Full cleanup on every attempt — FinderInfo may have been re-stamped since last try.
  find "$APP" -name '._*' -delete 2>/dev/null || true
  find "$APP" -name '.DS_Store' -delete 2>/dev/null || true
  xattr -cr "$APP" 2>/dev/null || true
  xattr -d com.apple.FinderInfo "$APP" 2>/dev/null || true

  if ! codesign --force --deep -s - "$APP"; then
    echo "    codesign --force failed (attempt $attempt/5), retrying after 1s" >&2
    sleep 1
    continue
  fi

  # Dechunk FinderInfo again RIGHT before strict verify — this is the race guard.
  xattr -d com.apple.FinderInfo "$APP" 2>/dev/null || true

// EngineClient — thin Swift bridge between the SwiftUI app and the existing
// shell engine (LaunchAgent + installer.sh + Calibre). M1.
//
// Design rules (from arch/plans-ui.md, "Инварианты" + "Контракты данных"):
//   - The engine logic is NOT changed here. We only *call* installer.sh and
//     read agent state via launchctl / plutil.
//   - runner.sh / watcher.sh are never invoked from Swift.
//   - The app is unsandboxed, no external Swift deps: Foundation only.
//
// Everything here shells out via Process. Arguments are passed as a real argv
// array (never spliced into a shell string), so paths with spaces / unicode are
// safe — same contract the installer relies on (plutil, not sed).

import Foundation

// MARK: - Public model

/// Resolved state of the LaunchAgent, as the UI needs to render it.
///
/// IMPORTANT (Codex's nuance, confirmed by `launchctl print` on a real agent):
/// the agent is *event-driven*. In the normal idle case `launchctl print`
/// reports `state = not running` — that is healthy, not an error. So "active"
/// is defined as: `launchctl print` returned 0  AND  the plist exists  AND  the
/// label is not disabled. A running pid is NOT required.
struct AgentStatus {
    /// `launchctl print gui/$UID/<label>` returned rc == 0 (agent is bootstrapped).
    var isLoaded: Bool
    /// Label is disabled in the user domain (`launchctl print-disabled` => disabled).
    var isDisabled: Bool
    /// `state = running` was seen in `launchctl print` (informational only).
    var isRunning: Bool
    /// Whether the LaunchAgent plist file exists on disk.
    var plistExists: Bool

    /// The UI's single source of truth: is the agent effectively active?
    /// active = bootstrapped (rc 0) AND plist present AND not disabled.
    /// Running pid is intentionally NOT part of this — the agent is event-driven.
    var isActive: Bool { isLoaded && plistExists && !isDisabled }
}

/// Result of the first-run / migration decision.
enum FirstRunOutcome: Equatable {
    /// No plist existed → installer was run on the given default folder.
    case installedDefault(watchDir: String)
    /// A plist already existed → we kept the user's existing WATCH_DIR untouched.
    case migratedExisting(watchDir: String)
    /// Движка НЕТ — installer даже не звали. UI предлагает поставить Calibre.
    /// (CAL-1: раньше это и следующий случай маскировались одним blockedNoCalibre.)
    case needsEngine
    /// Движок ЕСТЬ, но installer вернул ненулевой код — агент не поднялся.
    /// Это НЕ повод предлагать установку движка: чинить надо запуск агента.
    case agentSetupFailed
}

/// One-shot snapshot the UI reads to prove the bridge works (M1 placeholder
/// screen; the real Status screen lands at M2).
struct EngineSnapshot {
    var watchDir: String?
    var agent: AgentStatus
    var calibreInstalled: Bool
}

// MARK: - EngineClient

/// All paths are derived from `home` so the whole client can be pointed at a
/// throwaway HOME (and a throwaway label) for isolated tests — without ever
/// touching the user's real agent or watch folder.
struct EngineClient {

    // --- configuration -----------------------------------------------------

    /// LaunchAgent label. Default is the production label; tests override it
    /// with a throwaway label so they never collide with the real agent.
    let label: String

    /// Home directory root. Default is the real HOME; tests point it at a
    /// `mktemp -d` so the installer writes into a sandbox.
    let home: String

    /// Absolute path to the bundled installer.sh (in the .app Resources, or a
    /// checkout path for tests).
    let installerPath: String

    /// ЯВНЫЙ оверрайд пути к `ebook-convert`. По умолчанию nil → путь целиком
    /// определяет `CalibreLocator` (контракт детекта, инвариант 5).
    ///
    /// Оверрайд оставлен для тестов, которые подсовывают сюда `/bin/echo`: для
    /// него проверяется ТОЛЬКО сам файл (без meta/polish рядом) — прежняя
    /// M1-семантика «есть исполняемый файл = движок есть». Продакшен ходит
    /// через локатор, где валидность = все три CLI.
    let ebookConvertOverride: String?

    init(
        label: String = "com.arrivarus.fb2toepub.agent",
        home: String = EngineHome.resolve(),
        installerPath: String,
        ebookConvertPath: String? = nil
    ) {
        self.label = label
        self.home = home
        self.installerPath = installerPath
        self.ebookConvertOverride = ebookConvertPath
    }

    // --- derived paths -----------------------------------------------------

    /// `~/Library/LaunchAgents/<label>.plist`
    var plistPath: String {
        "\(home)/Library/LaunchAgents/\(label).plist"
    }

    /// `gui/<uid>/<label>` — the launchd service target.
    private var serviceTarget: String { "gui/\(getuid())/\(label)" }
    private var domainTarget: String { "gui/\(getuid())" }

    // MARK: - Process helper

    /// Result of running a child process.
    struct RunResult {
        let status: Int32
        let stdout: String
        let stderr: String
    }

    /// Run an executable with an explicit argv array. No shell, no string
    /// splicing. `env` is merged onto the current environment when provided
    /// (used to inject a throwaway HOME for the installer in tests).
    @discardableResult
    func run(_ launchPath: String, _ args: [String], env: [String: String]? = nil) -> RunResult {
        let proc = Process()
        proc.executableURL = URL(fileURLWithPath: launchPath)
        proc.arguments = args
        if let env = env {
            var merged = ProcessInfo.processInfo.environment
            for (k, v) in env { merged[k] = v }
            proc.environment = merged
        }

        let outPipe = Pipe()
        let errPipe = Pipe()
        proc.standardOutput = outPipe
        proc.standardError = errPipe

        do {
            try proc.run()
        } catch {
            return RunResult(status: -1, stdout: "", stderr: "spawn failed: \(error)")
        }

        // Read both pipes fully BEFORE waitUntilExit to avoid a deadlock if the
        // child fills a pipe buffer (launchctl print can be large).
        let outData = outPipe.fileHandleForReading.readDataToEndOfFile()
        let errData = errPipe.fileHandleForReading.readDataToEndOfFile()
        proc.waitUntilExit()

        return RunResult(
            status: proc.terminationStatus,
            stdout: String(data: outData, encoding: .utf8) ?? "",
            stderr: String(data: errData, encoding: .utf8) ?? ""
        )
    }

    // MARK: - Calibre probe (контракт детекта — CalibreLocator)

    /// Текущая локация движка по контракту (CAL-1). nil = движка нет.
    /// Все пути считаются от `home`, поэтому изолированный тест остаётся изолированным.
    func calibreLocation() -> CalibreLocation? {
        CalibreLocator.resolve(home: home)
    }

    /// Абсолютный путь к `ebook-convert`: явный оверрайд → локатор → "" (движка нет).
    /// Пустая строка недостижима для вызывающих: все они идут через
    /// `calibreInstalled()`, который в этом случае уже вернул false.
    var ebookConvertPath: String {
        ebookConvertOverride ?? calibreLocation()?.ebookConvert ?? ""
    }

    /// Движок есть?
    ///   • с явным оверрайдом — исполняем ли ИМЕННО он (прежняя M1-семантика);
    ///   • без оверрайда — нашёл ли локатор валидную локацию (все три CLI).
    func calibreInstalled() -> Bool {
        if let override = ebookConvertOverride {
            return CalibreLocator.isExecutableRegularFile(override)
        }
        return calibreLocation() != nil
    }

    // MARK: - Raw history (гибрид подачи D37)

    /// Есть ли у пользователя история конвертаций по СЫРОМУ снапшоту `state.json`

web search: 
web search: site:developer.apple.com code signing designated requirement ad hoc cdhash identifier requirement macOS ...
exec
/bin/zsh -lc "rg --files | rg '("'^|/)(fb2-to-epub'"\\.app|fb2-to-epub-runner|.*\\.plist"'$|README)'"' | head -80; find build -maxdepth 5 -type f -perm -111 -print 2>/dev/null | head -30; codesign -d -r- build/dist/fb2-to-epub.app 2>&1 || true; codesign -dvvv build/dist/fb2-to-epub.app 2>&1 | sed -n '1,80p' || true; codesign -d -r- build/dist/fb2-to-epub.app/Contents/MacOS/fb2-to-epub 2>&1 || true" in /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
 succeeded in 0ms:
README.md
design/cover-templates/README.md
README.en.md
packaging/fb2-to-epub-runner.sh
build/dist/fb2-to-epub.app/Contents/MacOS/fb2-to-epub
build/dist/fb2-to-epub.app/Contents/Resources/fb2-to-epub-watcher.sh
build/dist/fb2-to-epub.app/Contents/Resources/installer.sh
build/dist/fb2-to-epub.app/Contents/Resources/fb2-to-epub-runner.sh
build/dist/fb2-to-epub.app/Contents/Resources/fb2-to-epub-fb3.py
build/dist/fb2-to-epub.app/Contents/Resources/fb2-to-epub-cover-finder.py
build/build-app.sh
build/.venv/bin/pip3.9
build/.venv/bin/mac_alias
build/.venv/bin/dmgbuild
build/.venv/bin/pip3
build/.venv/bin/pip
build/.venv/bin/ds_store
build/.venv/bin/cairosvg
build/.venv/lib/python3.9/site-packages/_cffi_backend.cpython-39-darwin.so
Executable=/Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub/build/dist/fb2-to-epub.app/Contents/MacOS/fb2-to-epub
# designated => cdhash H"d58de915e1e7f261c7576c924731e4493bedf22b" or cdhash H"e5bcf0335f4605a2ba29e77125d3ca18b3bf7a06"
Executable=/Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub/build/dist/fb2-to-epub.app/Contents/MacOS/fb2-to-epub
Identifier=com.arrivarus.fb2toepub
Format=app bundle with Mach-O universal (x86_64 arm64)
CodeDirectory v=20400 size=6576 flags=0x2(adhoc) hashes=199+3 location=embedded
Hash type=sha256 size=32
CandidateCDHash sha256=d58de915e1e7f261c7576c924731e4493bedf22b
CandidateCDHashFull sha256=d58de915e1e7f261c7576c924731e4493bedf22b5a5cf9456870a602c57cfde6
Hash choices=sha256
CMSDigest=d58de915e1e7f261c7576c924731e4493bedf22b5a5cf9456870a602c57cfde6
CMSDigestType=2
CDHash=d58de915e1e7f261c7576c924731e4493bedf22b
Signature=adhoc
Info.plist entries=13
TeamIdentifier=not set
Sealed Resources version=2 rules=13 files=11
Internal requirements count=0 size=12
Executable=/Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub/build/dist/fb2-to-epub.app/Contents/MacOS/fb2-to-epub
# designated => cdhash H"d58de915e1e7f261c7576c924731e4493bedf22b" or cdhash H"e5bcf0335f4605a2ba29e77125d3ca18b3bf7a06"

web search: 
web search: site:developer.apple.com/forums "Full Disk Access" "ad hoc" cdhash ...
web search: 
web search: site:support.apple.com/guide/deployment Privacy Preferences Policy Control payload CodeRequirement IdentifierType path bundleID ...
codex
По локальной сборке уже есть ключевое подтверждение: текущий ad-hoc `.app` имеет designated requirement вида `cdhash … or cdhash …`, а не стабильную identity по bundle ID/TeamID. Значит вариант B действительно теряет TCC-совпадение при любом изменении подписанного бандла. Сейчас уточняю цену и ограничения SMAppService/BundleProgram и затем сведу план.
exec
/bin/zsh -lc 'SDK_ROOT="$(xcrun --show-sdk-path --sdk macosx)"; rg -n -C 6 '"'BundleProgram|SMAppService|Contents/Library/LaunchAgents|plistName' \""'$SDK_ROOT"/System/Library/Frameworks/ServiceManagement.framework/Headers "$SDK_ROOT"/usr/share/man/man5/launchd.plist.5 2>/dev/null | head -240; mdls -name kMDItemVersion -name kMDItemCFBundleIdentifier /System/Applications/System'"\\ Settings.app 2>/dev/null | head -20" in /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
 succeeded in 1208ms:
2026-07-23 07:43:48.947 xcodebuild[8329:100360]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-23 07:43:49.047 xcodebuild[8329:100359] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
2026-07-23 07:43:49.337 xcodebuild[8330:100368]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-23 07:43:49.438 xcodebuild[8330:100367] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
xcrun: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
xcrun: error: couldn't create cache file '/tmp/xcrun_db-CoFUR30T' (errno=Operation not permitted)
2026-07-23 07:43:49.742 xcodebuild[8332:100376]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-23 07:43:49.841 xcodebuild[8332:100375] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-184-and indicates the absolute path to the executable for the job. If this key is
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-185-missing, then the first element of the array of strings provided to the
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-186-.Nm ProgramArguments
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-187-will be used instead. This key is required in the absence of the
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-188-.Nm ProgramArguments
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-189-and
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5:190:.Nm BundleProgram
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-191-keys.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5:192:.It Sy BundleProgram <string>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-193-This key maps to the first argument of
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-194-.Xr execv 3
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-195-and is an app-bundle relative path to the executable for the job. This key is
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5:196:only supported for plists that are installed using SMAppService.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-197-.It Sy ProgramArguments <array of strings>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-198-This key maps to the second argument of
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-199-.Xr execvp 3
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-200-and specifies the argument vector to be passed to the job when a process is
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-201-spawned. This key is required in the absence of the Program key.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/usr/share/man/man5/launchd.plist.5-202-.Em IMPORTANT:
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-5-#include <sys/cdefs.h>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-6-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-7-#include <xpc/xpc.h>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-8-#include <CoreFoundation/CoreFoundation.h>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-9-#include <ServiceManagement/SMErrors.h>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-10-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h:11:#include <ServiceManagement/SMAppService.h>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-12-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-13-#if !TARGET_OS_IPHONE
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-14-#include <Security/Authorization.h>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-15-#include <ServiceManagement/SMLoginItem.h>
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-16-#else // !TARGET_OS_IPHONE
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-17-// Other projects typedef AuthorizationRef, so let's do a #define here to avoid
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-247- * It is the responsibility of the application to release the error reference.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-248- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-249- * @result
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-250- * True if the helper tool was successfully bootstrapped, otherwise false.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-251- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-252- * @discussion
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h:253: * SMJobBless has been deprecated in favor of using SMAppService APIs
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h:254: * to install a LaunchDaemon. See SMAppService:daemonServiceWithPlistName
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-255- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-256- * SMJobBless submits the executable for the given label as a launchd job.  This
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-257- * routine obviates the need for a setuid helper invoked via
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-258- * AuthorizationExecuteWithPrivileges() in order to install a launchd plist.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-259- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h:260:__OSX_DEPRECATED(10.6, 13.0, "Please use SMAppService instead")
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-261-XPC_EXPORT XPC_NONNULL2
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-262-Boolean
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-263-SMJobBless(CFStringRef domain, CFStringRef executableLabel,
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-264-	AuthorizationRef auth, CFErrorRef *outError);
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-265-#endif // !TARGET_OS_IPHONE
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/ServiceManagement.h-266-
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-7-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-8-NS_ASSUME_NONNULL_BEGIN
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-9-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-10-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-11- * @enum ServiceManagement App Service Status
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-12- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:13: * @abstract The values returned by SMAppService:status
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-14- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:15: * @const SMAppServiceNotRegistered
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-16- * A service has not been registered with ServiceManagement or that the service was unregistered
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-17- * after it was already registered.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-18- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:19: * @const SMAppServiceEnabled
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-20- * A service has been successfully registered and is eligible to run
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-21- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:22: * @const SMAppServiceRequiresApproval
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-23- * A service has been successfully registered, but the user needs to take action in System Settings
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-24- * before the service is eligible to run. This status will be returned if the user revokes consent for the service
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-25- * to run in System Settings
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-26- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:27: * @const SMAppServiceNotFound
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-28- * An error occurred and no such service could be found
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-29- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:30:typedef NS_ENUM(NSInteger, SMAppServiceStatus) {
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:31:	SMAppServiceStatusNotRegistered,
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:32:	SMAppServiceStatusEnabled,
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:33:	SMAppServiceStatusRequiresApproval,
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:34:	SMAppServiceStatusNotFound,
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:35:} NS_SWIFT_NAME(SMAppService.Status);
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-36-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-37-XPC_EXPORT
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-38-API_AVAILABLE(macos(15.0), macCatalyst(18.0)) API_UNAVAILABLE(ios, tvos, watchos)
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:39:NSString * const SMAppServiceErrorDomain;
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-40-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-41-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-42- * @abstract
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:43: * An SMAppService is used to control helper executables that live inside of an app's main bundle.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-44- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-45- * @discussion
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:46: * For SMAppServices initialized as LoginItems, the register and unregister APIs provide a replacement for
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-47- * SMLoginItemSetEnabled.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-48- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:49: * Apps that use SMAppService APIs must be code signed.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-50- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:51: * For SMAppServices initialized as LaunchAgents, the register and unregister APIs provide a replacement
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-52- * for installing plists in ~/Library/LaunchAgents or /Library/LaunchAgents.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-53- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:54: * For SMAppServices initialized as LaunchDaemons, the register and unregister APIs provide a replacement
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-55- * for installing plists in /Library/LaunchDaemons. Apps that contain LaunchDaemons must be notarized.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-56- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-57- * Legacy LaunchDaemons installed in /Library/LaunchDaemons will continue to be bootstrapped without
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-58- * explicit approval in System Settings since writing to /Library is protected  with filesystem permissions.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-59- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:60: * If an app updates either the plist or the executable for a LaunchAgent or LaunchDaemon, the SMAppService
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-61- * must be re-registered or it may not launch. It is recommended to also call unregister before
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-62- * re-registering if the executable has been changed.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-63- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-64-XPC_EXPORT
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-65-API_AVAILABLE(macos(13.0), macCatalyst(16.0)) API_UNAVAILABLE(ios, tvos, watchos)
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:66:@interface SMAppService : NSObject
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-67-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-68-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-69- * @method loginItemServiceWithIdentifier
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-70- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-71- * @abstract
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:72: * Initializes a SMAppService for a LoginItem corresponding to the bundle with the specified identifier.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-73- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-74- * @param identifier
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-75- * The bundle identifier of the helper application
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-76- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-77- * @discussion
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-78- * The identifier must correspond to the bundle identifier for a LoginItem that lives in the calling app's
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-81-+ (instancetype)loginItemServiceWithIdentifier:(NSString *)identifier NS_SWIFT_NAME(loginItem(identifier:)) API_AVAILABLE(macos(13.0), macCatalyst(16.0));
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-82-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-83-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-84- * @property mainAppService
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-85- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-86- * @abstract
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:87: * A SMAppService corresponding to the main application as a LoginItem
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-88- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:89: * This SMAppService can be used to configure the main app to be launched at login
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-90- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:91:@property (class, readonly) SMAppService *mainAppService NS_SWIFT_NAME(mainApp) API_AVAILABLE(macos(13.0), macCatalyst(16.0));
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-92-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-93-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-94- * @method agentServiceWithPlistName
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-95- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-96- * @abstract
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:97: * Initializes a SMAppService with a LaunchAgent with the specified plist name.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-98- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:99: * @param plistName
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:100: * The name of the plist corresponding to the SMAppService.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-101- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-102- * @discussion
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:103: * The plistName must correspond to a plist in the calling app's Contents/Library/LaunchAgents directory
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-104- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:105: * In addition to the standard launchd.plist keys, plists registered with SMAppService may use the
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:106: * BundleProgram launchd plist key to specify an app bundle relative path for the executable. This key allows
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-107- * apps to support a user relocating the app bundle after installation.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-108- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:109:+ (instancetype)agentServiceWithPlistName:(NSString *)plistName NS_SWIFT_NAME(agent(plistName:)) API_AVAILABLE(macos(13.0), macCatalyst(16.0));
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-110-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-111-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-112- * @method daemonServiceWithPlistName
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-113- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-114- * @abstract
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:115: * Initializes a SMAppService with a LaunchDaemon with the specified plist name.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-116- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:117: * @param plistName
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:118: * The name of the plist corresponding to the SMAppService.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-119- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-120- * @discussion
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:121: * The plistName must correspond to a plist in the calling app's Contents/Library/LaunchDaemons directory
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-122- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:123: * In addition to the standard launchd.plist keys, plists registered with SMAppService may use the
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:124: * BundleProgram launchd plist key to specify an app bundle relative path for the executable. This key allows
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-125- * apps to support a user relocating the app bundle after installation.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-126- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-127- * For a LaunchDaemon to be bootstrapped during boot, the containing application must be accessible before
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-128- * a user logs in. For applications that intend to register LaunchDaemons, it is recommended that the
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-129- * application bundle live in /Applications
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-130- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:131:+ (instancetype)daemonServiceWithPlistName:(NSString *)plistName NS_SWIFT_NAME(daemon(plistName:)) API_AVAILABLE(macos(13.0), macCatalyst(16.0));
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-132-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-133-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-134- * @method registerAndReturnError
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-135- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-136- * @abstract
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-137- * Registers the service such that it may begin launching subject to user consent
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-164- * If the service is already registered, this API will return error kSMErrorAlreadyRegistered
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-165- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-166- * If the service is not approved by the user, this API will return error kSMErrorLaunchDeniedByUser
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-167- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-168- * If the app bundle is not properly code signed, this API will return error kSMErrorInvalidSignature 
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-169- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:170: * @see SMAppService:unregisterAndReturnError
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-171- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-172-- (BOOL)registerAndReturnError:(NSError * _Nullable __autoreleasing * _Nullable)error API_AVAILABLE(macos(13.0), macCatalyst(16.0));
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-173-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-174-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-175- * @method unregisterAndReturnError
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-176- *
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-188- * If the service corresponds to a LoginItem, LaunchAgent, or LaunchDaemon and the service is currently
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-189- * running it will be killed. The unregister call will not wait for the service to be reaped.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-190- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-191- * If the service corresponds to the main application, it will continue running, but will still be unregistered to
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-192- * prevent future launches at login.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-193- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:194: * This is the opposite operation of SMAppService:register
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-195- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-196- * If the service is already unregistered, this API will return error kSMErrorJobNotFound
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-197- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:198: * @see SMAppService:registerAndReturnError
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-199- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-200- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-201-- (BOOL)unregisterAndReturnError:(NSError * _Nullable __autoreleasing * _Nullable)error API_AVAILABLE(macos(13.0), macCatalyst(16.0));
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-202-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-203-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-204- * @method unregisterWithCompletionHandler
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-217- * invoked whenever an error occurs. After the completion handler has been invoked it is safe to re-register the
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-218- * service.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-219- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-220- * If the service corresponds to the main application, it will continue running, but will still be unregistered to
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-221- * prevent future launches at login.
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-222- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:223: * This is the opposite operation of SMAppService:register
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-224- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:225: * This is the asynchronous variant of SMAppService:unregisterAndReturnError
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-226- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-227- * If the service is already unregistered, this API will return error kSMErrorJobNotFound
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-228- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h:229: * @see SMAppService:unregisterAndReturnError
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-230- *
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-231- */
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-232-- (void)unregisterWithCompletionHandler:(void (^)(NSError * _Nullable error))handler API_AVAILABLE(macos(13.0), macCatalyst(16.0));
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-233-
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-234-/*!
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-235- * @property status
--
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-238- * Returns the status for the service
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk/System/Library/Frameworks/ServiceManagement.framework/Headers/SMAppService.h-239- *
/System/Applications/System Settings.app: could not find /System/Applications/System Settings.app.

exec
/bin/zsh -lc "rg -n -C 5 'runner|Full Disk|полный доступ|FDA|LaunchAgent|launchd|Privacy|Конфиденциальность' README.md README.en.md; rg -n 'runnerPath\\(|step3accent|onOpenSettings|NSPasteboard|Privacy_AllFiles|x-apple.systempreferences' app/*.swift tests -g '*.swift' -g '*.sh' 2>/dev/null | head -160; rg -n 'runner\\.sh|RUNNER_DST|ProgramArguments\\.0' tests packaging build app README* 2>/dev/null | head -240" in /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
 succeeded in 0ms:
README.en.md-11-<p align="center">
README.en.md-12-  <img src="docs/screenshots/status.png" height="380" alt="Status screen — watching, stats, recent conversions">
README.en.md-13-  &nbsp;&nbsp;
README.en.md-14-  <img src="docs/screenshots/setup.png" height="380" alt="Setup screen — Calibre check and folder selection">
README.en.md-15-  &nbsp;&nbsp;
README.en.md:16:  <img src="docs/screenshots/settings.png" height="380" alt="Settings — reset stats, Full Disk Access, check for updates">
README.en.md-17-  &nbsp;&nbsp;
README.en.md-18-  <img src="docs/screenshots/cover-select.png" height="380" alt="Cover selection — online candidates with previews and generated fallbacks">
README.en.md-19-</p>
README.en.md-20-
README.en.md-21-<p align="center"><sub><b>Status</b> · <b>Setup</b> · <b>Settings</b> · <b>Cover selection</b></sub></p>
--
README.en.md-27-- **Folder-based auto-conversion.** Point it at a single folder — the background agent catches new files and converts them on its own, with no manual runs. A single `*.fb2` / `*.fb2.zip` / `*.fb3` file → an `.epub` appears next to it; a book folder (any nesting depth) → a mirror folder with the `-epub` suffix is created next to it. Source files are left untouched, and repeated runs are idempotent (anything already up to date is skipped).
README.en.md-28-- **Live status.** A progress ring fills clockwise as a batch converts and shows 100% once everything is done (its center shows a "done / total" counter while a batch is running). Next to it: "total" and "today" counters, the background agent's status (running / paused), the watched folder, a list of recent conversions, and an **Open Folder** button. Everything updates event-driven — no manual refresh.
README.en.md-29-- **Auto bring-to-front.** When a new conversion batch starts, the window comes to the front on its own, so you can see the process kick off.
README.en.md-30-- **The conversion engine installs itself.** If Calibre isn't installed, the app downloads and installs it in one click — with progress, an integrity check, and no admin password; an already-installed Calibre is used as is. Details in the [The conversion engine installs itself](#the-conversion-engine-installs-itself) section.
README.en.md-31-- **Built-in auto-update.** **⚙ Settings → Check for updates** downloads and installs the new version of the app; the background agent is updated automatically along with it.
README.en.md:32:- **Settings screen.** Reset statistics · Full Disk Access · app version + Check for updates. Authorship and a GitHub link sit at the bottom of the screen.
README.en.md-33-- **Reset statistics.** Clears "total", "today", and the recent-conversions list (it does not touch the `.epub` files themselves).
README.en.md-34-- **Smart covers.** If the FB2 has no cover, the app looks one up online, lets you pick from the matches, guards against the wrong cover by title, and — if nothing suitable is found online — draws a nice typographic cover itself (details in the [Cover handling](#cover-handling) section).
README.en.md-35-
README.en.md-36-## Installation (recommended path — DMG)
README.en.md-37-
README.en.md-38-1. Download `fb2-to-epub-<version>.dmg` from the **[Releases](https://github.com/ArrivaRUS/fb2-to-epub/releases)** page.
README.en.md-39-2. Open the `.dmg` and drag the **fb2-to-epub** app into the **Applications** folder — the arrow on the window background shows where it goes.
README.en.md-40-3. **First launch** (one time only): open the Applications folder, **right-click** `fb2-to-epub` → **Open** → confirm **Open** in the dialog.
README.en.md:41:   *Alternative:* launch the app, then go to **System Settings → Privacy & Security → Open Anyway**.
README.en.md-42-   The app is built without a paid Apple signing certificate, so macOS asks for confirmation — this is a one-time step, after which it launches with a normal double-click.
README.en.md-43-4. The app checks the conversion engine (**Calibre**). If it's missing, it offers to **install it in one click** (downloading and installing it itself) or uses an already-installed copy; see the [The conversion engine installs itself](#the-conversion-engine-installs-itself) section. Then it prompts you to **choose a folder** to watch. The default is `~/Desktop/fb2-to-epub` (created automatically).
README.en.md-44-5. Done. A "Ready to go" screen appears showing the chosen folder and an **Open Folder** button.
README.en.md-45-
README.en.md-46-From now on, drop into the chosen folder:
--
README.en.md-65-
README.en.md-66-<!-- TODO (Yurka): add the engine-onboarding screenshot — docs/screenshots/onboarding.png (verify light/dark theme). Example:
README.en.md-67-<p align="center"><img src="docs/screenshots/onboarding.png" height="380" alt="Engine onboarding — Install Calibre in one click, download with progress"></p>
README.en.md-68--->
README.en.md-69-
README.en.md:70:## Full Disk Access (if the folder is in Desktop / Documents / Downloads)
README.en.md-71-
README.en.md-72-`~/Desktop`, `~/Documents`, and `~/Downloads` are macOS-protected zones (TCC). On a fresh Mac, the background agent may need **one-time** access to them.
README.en.md-73-
README.en.md:74:**The app notices this and walks you through it.** If the agent can't read the folder, the app shows a "No access to the folder" card: the **"Open Settings & copy path"** button opens the right System Settings pane and puts the exact runner path on your clipboard, and **"Check again"** picks up the granted access on its own (the card disappears, conversion resumes). Nothing to type by hand.
README.en.md-75-
README.en.md-76-If you'd rather do it manually — the steps are the same (they're on the card too):
README.en.md-77-
README.en.md:78:1. **System Settings → Privacy & Security → Full Disk Access**.
README.en.md-79-2. Click **+**, then in the picker press **Cmd-Shift-G** and paste the path (it's already on your clipboard):
README.en.md-80-   ```
README.en.md:81:   ~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh
README.en.md-82-   ```
README.en.md-83-3. Add it and **turn the toggle on**.
README.en.md-84-
README.en.md:85:The access is bound to this specific file and persists across app updates. There is also a quick jump to the right pane inside the app: **⚙ Settings → Full Disk Access**.
README.en.md-86-
README.en.md:87:> Why the runner specifically: macOS binds file-access permissions not to the script but to the executable named in the agent's `ProgramArguments`. This gives the agent a stable "responsible" target at a fixed path (`fb2-to-epub-runner.sh`) that you can grant access to once.
README.en.md-88-
README.en.md-89-## Requirements
README.en.md-90-
README.en.md-91-- **macOS** (11.0+). Auto-installing the engine requires **macOS 14+**; on 11–13 Calibre is installed manually.
README.en.md-92-- **[Calibre](https://calibre-ebook.com)** — the conversion engine. **You don't have to install it up front:** the app installs it itself with a button (see [The conversion engine installs itself](#the-conversion-engine-installs-itself)) or uses an already-installed copy. If you'd rather install it yourself — `brew install --cask calibre` or [download it from the website](https://calibre-ebook.com/download_osx).
--
README.en.md-114-
README.en.md-115-| Action | Command |
README.en.md-116-| --- | --- |
README.en.md-117-| Live logs | `tail -f ~/Library/Logs/fb2-to-epub.log` |
README.en.md-118-| Stop the agent | `launchctl bootout gui/$(id -u)/com.arrivarus.fb2toepub.agent` |
README.en.md:119:| Start the agent | `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.arrivarus.fb2toepub.agent.plist` |
README.en.md-120-| Restart / run manually | `launchctl kickstart -k gui/$(id -u)/com.arrivarus.fb2toepub.agent` |
README.en.md-121-| Change the watched folder | Click **Change** in **⚙ Settings** (or launch the app again) and pick a new folder — reinstalling is idempotent |
README.en.md-122-| Uninstall | Use `./uninstall.sh` from the repository (CLI path below) |
README.en.md-123-
README.en.md-124-## How it works
README.en.md-125-
README.en.md:126:- macOS `launchd` watches the chosen folder via `WatchPaths`. Agent: `com.arrivarus.fb2toepub.agent`.
README.en.md:127:- When new files appear, the agent runs the **runner** (`fb2-to-epub-runner.sh`, the FDA target), which exec's the **watcher** (`fb2-to-epub-watcher.sh`).
README.en.md-128-- The watcher converts books through [Calibre](https://calibre-ebook.com) (`ebook-convert`, `ebook-meta`).
README.en.md-129-- The Calibre engine is looked up in three places in order: the **app's own copy** (`~/Library/Application Support/fb2-to-epub/calibre.app`, where auto-install puts it) → `/Applications/calibre.app` → `~/Applications/calibre.app` (manual install). The first working one wins. Auto-install is handled by `CalibreInstaller` (download → SHA-512 check → `codesign --verify` → unpack into the app's own folder → reinstall the agent with the correct path to `ebook-convert`).
README.en.md-130-- Cover lookups are handled by `fb2-to-epub-cover-finder.py` (Python 3, no third-party dependencies). Fallback covers are rendered by the app itself, natively (no Python).
README.en.md-131-- **FB3** (`.fb3` — a ZIP/OPC container, newer than FB2) is first translated into FB2 by `fb2-to-epub-fb3.py` (Python 3, stdlib only, no third-party dependencies) — preserving text, images, metadata, and the native cover — then follows the usual path through Calibre. For an FB3 with an embedded cover, the online search and the generated fallback cover aren't needed.
README.en.md-132-- The app and the agent communicate through state files: the agent writes `state.json` atomically (batch progress, statistics, the cover queue), and the app reads it and reacts event-driven.
README.en.md-133-- The absolute paths to `ebook-convert` and `python3` are passed to the agent via `EnvironmentVariables` (the agent starts with a bare `PATH`).
README.en.md-134-- `ThrottleInterval=5s` smooths out batch copying; a lock directory in `/tmp` serializes parallel runs.
README.en.md-135-
README.en.md:136:The scripts live in `~/Library/Application Support/fb2-to-epub/bin/`, the data (state, the cover queue, and previews) lives in `~/Library/Application Support/fb2-to-epub/`, the agent config is at `~/Library/LaunchAgents/com.arrivarus.fb2toepub.agent.plist`, and the log is at `~/Library/Logs/fb2-to-epub.log`.
README.en.md-137-
README.en.md-138-## Advanced path — install from the CLI
README.en.md-139-
README.en.md-140-For developers and anyone who prefers the terminal. This bypasses the app and the DMG — it installs the same agent with the same installer (`packaging/installer.sh`).
README.en.md-141-
--
README.en.md-167-```
README.en.md-168-
README.en.md-169-`build-app.sh`:
README.en.md-170-
README.en.md-171-- compiles `app/*.swift` (`main.swift`, `StatusView`, `SetupView`, `CoverSelectView`, `SettingsView`, `CoverGenerator`, `UpdateChecker`, `CalibreLocator`, `CalibreInstaller`, `EngineSetupCard`, and others) with `xcrun swiftc` for **arm64 and x86_64** and joins them into a universal binary (`lipo`);
README.en.md:172:- places the agent scripts — `installer.sh`, `fb2-to-epub-runner.sh`, the watcher, the cover-finder, `fb2-to-epub-fb3.py`, and `fb2-to-epub-fb3-genre.json` (the FB3 genre map) — into `Contents/Resources`;
README.en.md-173-- bundles the 4 cover templates (`cover-templates/`) and the icon (from `branding/icon-app.svg` → `.icns`);
README.en.md-174-- writes a clean `Info.plist` with a fixed `CFBundleIdentifier=com.arrivarus.fb2toepub` (a stable id matters so that TCC grants don't get dropped on a rebuild);
README.en.md-175-- runs an ad-hoc `codesign` with a strict verify (with retries against the FinderInfo race in iCloud-synced folders).
README.en.md-176-
README.en.md-177-`make-dmg.sh` packages the `.app` + a `/Applications` symlink + a background with first-launch instructions.
--
README.md-11-<p align="center">
README.md-12-  <img src="docs/screenshots/status.png" height="380" alt="Экран статуса — отслеживание, статистика, последние конвертации">
README.md-13-  &nbsp;&nbsp;
README.md-14-  <img src="docs/screenshots/setup.png" height="380" alt="Экран установки — проверка Calibre и выбор папки">
README.md-15-  &nbsp;&nbsp;
README.md:16:  <img src="docs/screenshots/settings.png" height="380" alt="Настройки — сброс статистики, Full Disk Access, проверка обновления">
README.md-17-  &nbsp;&nbsp;
README.md-18-  <img src="docs/screenshots/cover-select.png" height="380" alt="Выбор обложки — кандидаты с превью из сети и запасные сгенерированные">
README.md-19-</p>
README.md-20-
README.md-21-<p align="center"><sub><b>Статус</b> · <b>Установка</b> · <b>Настройки</b> · <b>Выбор обложки</b></sub></p>
--
README.md-27-- **Авто-конвертация по папке.** Указываешь одну папку — фоновый агент сам ловит новые файлы и конвертирует их, без ручных запусков. Отдельный файл `*.fb2` / `*.fb2.zip` / `*.fb3` → рядом появляется `.epub`; папка с книгами (любая вложенность) → рядом создаётся папка-зеркало с суффиксом `-epub`. Исходники не трогаются, повторные срабатывания идемпотентны (актуальное пропускается).
README.md-28-- **Live-статус.** Кольцо-прогресс заливается по часовой по мере конвертации пачки и показывает 100%, когда всё готово (в центре — счётчик «сделано / всего», пока пачка идёт). Рядом — счётчики «всего» и «за сегодня», статус фонового агента (работает / на паузе), отслеживаемая папка, список последних конвертаций и кнопка **«Открыть папку»**. Всё обновляется событийно, без ручного «обновить».
README.md-29-- **Авто-всплытие окна.** Когда стартует новая пачка конвертаций, окно само выходит на передний план — видно, что процесс пошёл.
README.md-30-- **Движок конвертации ставится сам.** Если Calibre не установлен, приложение скачает и поставит его в один клик — с прогрессом, проверкой целостности и без пароля администратора; уже установленный Calibre используется как есть. Подробнее — в разделе [«Движок конвертации ставится сам»](#движок-конвертации-ставится-сам).
README.md-31-- **Встроенный авто-апдейт.** **⚙ Настройки → «Проверить обновление»** скачивает и ставит новую версию приложения; фоновый агент при этом обновляется автоматически.
README.md:32:- **Экран настроек.** Сбросить статистику · Full Disk Access · версия приложения + «Проверить обновление». Авторство и ссылка на GitHub — внизу экрана.
README.md-33-- **Сброс статистики.** Обнуляет «всего», «за сегодня» и список последних конвертаций (сами `.epub`-файлы не трогает).
README.md-34-- **Умные обложки.** Если в FB2 нет обложки — приложение ищет её онлайн, даёт выбрать из найденных вариантов, защищает от чужих обложек по названию и, если в сети ничего не нашлось, рисует красивую типографскую обложку само (подробно — в разделе [«Обработка обложек»](#обработка-обложек)).
README.md-35-
README.md-36-## Установка (основной путь — DMG)
README.md-37-
README.md-38-1. Скачай `fb2-to-epub-<версия>.dmg` со страницы **[Releases](https://github.com/ArrivaRUS/fb2-to-epub/releases)**.
README.md-39-2. Открой `.dmg` и перетащи приложение **fb2-to-epub** в папку **«Программы»** (Applications) — стрелка на фоне окна показывает куда.
README.md-40-3. **Первый запуск** (один раз): открой папку «Программы», **правый клик** на `fb2-to-epub` → **«Открыть»** → в диалоге подтверди **«Открыть»**.
README.md:41:   *Альтернатива:* запусти приложение, затем **Системные настройки → Конфиденциальность и безопасность → «Открыть всё равно»**.
README.md-42-   Приложение собрано без платной подписи Apple, поэтому macOS просит подтверждение — это разовый шаг, дальше оно запускается обычным двойным кликом.
README.md-43-4. Приложение проверит движок конвертации (**Calibre**). Если его нет — предложит **установить в один клик** (скачает и поставит сам) или использует уже установленный; подробнее — в разделе [«Движок конвертации ставится сам»](#движок-конвертации-ставится-сам). Затем предложит **выбрать папку** для отслеживания. По умолчанию — `~/Desktop/fb2-to-epub` (создаётся автоматически).
README.md-44-5. Готово. Появится экран «Готово к работе» с указанием выбранной папки и кнопкой **«Открыть папку»**.
README.md-45-
README.md-46-Дальше кидай в выбранную папку:
--
README.md-65-
README.md-66-<!-- TODO (Юрка): вставить скриншот онбординга движка — docs/screenshots/onboarding.png (сверить светлую/тёмную тему). Пример:
README.md-67-<p align="center"><img src="docs/screenshots/onboarding.png" height="380" alt="Онбординг движка — «Установить Calibre» в один клик, скачивание с прогрессом"></p>
README.md-68--->
README.md-69-
README.md:70:## Full Disk Access (если папка в Desktop / Documents / Downloads)
README.md-71-
README.md-72-`~/Desktop`, `~/Documents` и `~/Downloads` — защищённые macOS зоны (TCC). На чистом Mac фоновому агенту может понадобиться **разовый** доступ к ним.
README.md-73-
README.md-74-**Приложение само это заметит и проведёт за руку.** Если агент не может прочитать папку, приложение показывает карточку «Нет доступа к папке»: кнопка **«Открыть настройки и скопировать путь»** открывает нужную панель системных настроек и кладёт точный путь раннера в буфер обмена, а **«Проверить снова»** сама подхватывает выданный доступ (карточка исчезает, конвертация продолжается). Ничего вводить вручную не нужно.
README.md-75-
README.md-76-Если хочется сделать всё руками — шаги те же (они и на карточке):
README.md-77-
README.md:78:1. **Системные настройки → Конфиденциальность и безопасность → Полный доступ к диску** (Full Disk Access).
README.md-79-2. Нажми **«+»**, в открывшемся выборе нажми **Cmd-Shift-G** и вставь путь (он уже в буфере):
README.md-80-   ```
README.md:81:   ~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh
README.md-82-   ```
README.md-83-3. Добавь его и **включи переключатель**.
README.md-84-
README.md:85:Доступ привязан именно к этому файлу и сохраняется при обновлениях приложения. Быстрый переход к нужной панели есть и в самом приложении: **⚙ Настройки → Full Disk Access**.
README.md-86-
README.md:87:> Почему именно раннер: macOS привязывает право доступа к файлам не к скрипту, а к исполняемому файлу, указанному в `ProgramArguments` агента. Поэтому у агента есть стабильная «ответственная» цель по фиксированному пути (`fb2-to-epub-runner.sh`), которой можно выдать доступ один раз.
README.md-88-
README.md-89-## Требования
README.md-90-
README.md-91-- **macOS** (11.0+). Автоустановка движка требует **macOS 14+**; на 11–13 Calibre ставится вручную.
README.md-92-- **[Calibre](https://calibre-ebook.com)** — движок конвертации. **Ставить заранее не обязательно:** приложение поставит его само по кнопке (см. [«Движок конвертации ставится сам»](#движок-конвертации-ставится-сам)) или использует уже установленный. Если предпочитаешь поставить сам — `brew install --cask calibre` или [скачать с сайта](https://calibre-ebook.com/download_osx).
--
README.md-114-
README.md-115-| Действие | Команда |
README.md-116-| --- | --- |
README.md-117-| Логи в реальном времени | `tail -f ~/Library/Logs/fb2-to-epub.log` |
README.md-118-| Остановить агент | `launchctl bootout gui/$(id -u)/com.arrivarus.fb2toepub.agent` |
README.md:119:| Запустить агент | `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.arrivarus.fb2toepub.agent.plist` |
README.md-120-| Перезапустить / прогнать вручную | `launchctl kickstart -k gui/$(id -u)/com.arrivarus.fb2toepub.agent` |
README.md-121-| Сменить отслеживаемую папку | Нажми **«Сменить»** в **⚙ Настройках** (или запусти приложение ещё раз) и выбери новую папку — переустановка идемпотентна |
README.md-122-| Удалить | Используй `./uninstall.sh` из репозитория (CLI-путь ниже) |
README.md-123-
README.md-124-## Как работает
README.md-125-
README.md:126:- macOS `launchd` отслеживает выбранную папку через `WatchPaths`. Агент: `com.arrivarus.fb2toepub.agent`.
README.md:127:- При появлении новых файлов агент запускает **runner** (`fb2-to-epub-runner.sh`, FDA-цель), который exec'ает **watcher** (`fb2-to-epub-watcher.sh`).
README.md-128-- Watcher конвертирует книги через [Calibre](https://calibre-ebook.com) (`ebook-convert`, `ebook-meta`).
README.md-129-- Движок Calibre ищется в трёх местах по порядку: **своя копия** (`~/Library/Application Support/fb2-to-epub/calibre.app`, куда его кладёт автоустановка) → `/Applications/calibre.app` → `~/Applications/calibre.app` (ручная установка). Побеждает первый рабочий. Автоустановку выполняет `CalibreInstaller` (скачивание → сверка SHA-512 → `codesign --verify` → распаковка в свою папку → переустановка агента с верным путём к `ebook-convert`).
README.md-130-- **FB3** (`.fb3` — ZIP/OPC-контейнер, новее FB2) сначала транслируется в FB2 скриптом `fb2-to-epub-fb3.py` (Python 3, только stdlib, без сторонних зависимостей) — с сохранением текста, картинок, метаданных и родной обложки, — после чего идёт обычным путём через Calibre. Для FB3 со встроенной обложкой онлайн-поиск и генерация запасной обложки не нужны.
README.md-131-- Поиск обложек делает `fb2-to-epub-cover-finder.py` (Python 3, без сторонних зависимостей). Запасные обложки приложение рендерит само, нативно (без Python).
README.md-132-- Приложение и агент общаются через файлы состояния: агент атомарно пишет `state.json` (прогресс пачки, статистика, очередь обложек), приложение его читает и реагирует событийно.
README.md-133-- Абсолютные пути к `ebook-convert` и `python3` передаются агенту через `EnvironmentVariables` (агент стартует с голым `PATH`).
README.md-134-- `ThrottleInterval=5s` сглаживает пакетное копирование; lock-каталог в `/tmp` сериализует параллельные запуски.
README.md-135-
README.md:136:Скрипты живут в `~/Library/Application Support/fb2-to-epub/bin/`, данные (состояние, очередь и превью обложек) — в `~/Library/Application Support/fb2-to-epub/`, конфиг агента — в `~/Library/LaunchAgents/com.arrivarus.fb2toepub.agent.plist`, лог — в `~/Library/Logs/fb2-to-epub.log`.
README.md-137-
README.md-138-## Продвинутый путь — установка из CLI
README.md-139-
README.md-140-Для разработчиков и тех, кто предпочитает терминал. Минует приложение и DMG — ставит тот же агент тем же установщиком (`packaging/installer.sh`).
README.md-141-
--
README.md-167-```
README.md-168-
README.md-169-`build-app.sh`:
README.md-170-
README.md-171-- компилирует `app/*.swift` (`main.swift`, `StatusView`, `SetupView`, `CoverSelectView`, `SettingsView`, `CoverGenerator`, `UpdateChecker`, `CalibreLocator`, `CalibreInstaller`, `EngineSetupCard` и др.) через `xcrun swiftc` под **arm64 и x86_64** и склеивает их в универсальный бинарь (`lipo`);
README.md:172:- кладёт скрипты агента — `installer.sh`, `fb2-to-epub-runner.sh`, watcher, cover-finder, `fb2-to-epub-fb3.py` и `fb2-to-epub-fb3-genre.json` (таблица жанров FB3) — в `Contents/Resources`;
README.md-173-- бандлит 4 шаблона обложек (`cover-templates/`) и иконку (из `branding/icon-app.svg` → `.icns`);
README.md-174-- пишет чистый `Info.plist` с фиксированным `CFBundleIdentifier=com.arrivarus.fb2toepub` (стабильный id важен, чтобы TCC-гранты не слетали при пересборке);
README.md-175-- выполняет ad-hoc `codesign` со строгой проверкой (с ретраями против гонки FinderInfo в iCloud-папках).
README.md-176-
README.md-177-`make-dmg.sh` упаковывает `.app` + симлинк `/Applications` + фон с инструкцией первого запуска.
app/main.swift:1041:    /// Settings so the user can grant access to the agent. The `Privacy_AllFiles`
app/main.swift:1048:            "x-apple.systempreferences:com.apple.preference.security?Privacy_AllFiles"),
app/main.swift:1053:            "x-apple.systempreferences:com.apple.preference.security?Privacy") {
app/main.swift:1290:        let path = engine.runnerPath()
app/main.swift:1291:        let pb = NSPasteboard.general
app/StatusView.swift:577:            onOpenSettings: onOpenFolderAccess,
app/FolderAccessCard.swift:58:    var onOpenSettings: () -> Void = {}
app/FolderAccessCard.swift:79:    private static let step3accent = "fb2-to-epub-runner.sh"
app/FolderAccessCard.swift:374:                           accent: Self.step3accent, accentMono: true, trailing: ".")
app/FolderAccessCard.swift:393:                     icon: "arrow.up.right.square", disabled: isChecking, action: onOpenSettings)
app/FolderAccessCard.swift:439:                           accent: Self.step3accent, accentMono: true, trailing: ".")
app/FolderAccessCard.swift:445:                               size: .big, fill: .orange, action: onOpenSettings)
app/FolderAccessCard.swift:522:            FaCtaSmall(title: "Исправить", size: .compact, fill: .brand, action: onOpenSettings)
app/SettingsView.swift:134:                                     onOpenSettings: onFixFolderAccess)
app/EngineClient.swift:311:    func runnerPath() -> String {
tests/ClearHistoryTests/FDAContractTests.swift:7://   • EngineClient.runnerPath() — the clipboard target: plist ProgramArguments[0] via
tests/ClearHistoryTests/FDAContractTests.swift:101:    T.eq(ec.runnerPath(), runner, "runnerPath() reads ProgramArguments[0] from the plist")
tests/ClearHistoryTests/FDAContractTests.swift:108:    T.eq(ec.runnerPath(), want, "no plist → canonical app-owned runner path (derived from home, not literal ~)")
README.en.md:81:   ~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh
README.en.md:87:> Why the runner specifically: macOS binds file-access permissions not to the script but to the executable named in the agent's `ProgramArguments`. This gives the agent a stable "responsible" target at a fixed path (`fb2-to-epub-runner.sh`) that you can grant access to once.
README.en.md:127:- When new files appear, the agent runs the **runner** (`fb2-to-epub-runner.sh`, the FDA target), which exec's the **watcher** (`fb2-to-epub-watcher.sh`).
README.en.md:172:- places the agent scripts — `installer.sh`, `fb2-to-epub-runner.sh`, the watcher, the cover-finder, `fb2-to-epub-fb3.py`, and `fb2-to-epub-fb3-genre.json` (the FB3 genre map) — into `Contents/Resources`;
README.md:81:   ~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh
README.md:87:> Почему именно раннер: macOS привязывает право доступа к файлам не к скрипту, а к исполняемому файлу, указанному в `ProgramArguments` агента. Поэтому у агента есть стабильная «ответственная» цель по фиксированному пути (`fb2-to-epub-runner.sh`), которой можно выдать доступ один раз.
README.md:127:- При появлении новых файлов агент запускает **runner** (`fb2-to-epub-runner.sh`, FDA-цель), который exec'ает **watcher** (`fb2-to-epub-watcher.sh`).
README.md:172:- кладёт скрипты агента — `installer.sh`, `fb2-to-epub-runner.sh`, watcher, cover-finder, `fb2-to-epub-fb3.py` и `fb2-to-epub-fb3-genre.json` (таблица жанров FB3) — в `Contents/Resources`;
build/build-app.sh:122:install -m 0755 "$REPO_DIR/packaging/fb2-to-epub-runner.sh"    "$RES/fb2-to-epub-runner.sh"
tests/ClearHistoryTests/FDAContractTests.swift:91:    let runner = "\(home)/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh"
tests/ClearHistoryTests/FDAContractTests.swift:107:    let want = "\(home)/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh"
app/FolderAccessCard.swift:79:    private static let step3accent = "fb2-to-epub-runner.sh"
packaging/installer.sh:101:RUNNER_DST="$BIN_DIR/fb2-to-epub-runner.sh"
packaging/installer.sh:296:src_runner="$(find_src fb2-to-epub-runner.sh)"   || { echo "fb2-to-epub: missing fb2-to-epub-runner.sh source" >&2; exit 1; }
packaging/installer.sh:302:# runner.sh is the FDA-granted "responsible" target — the TCC grant is keyed to
packaging/installer.sh:305:if [[ ! -f "$RUNNER_DST" ]] || ! cmp -s "$src_runner" "$RUNNER_DST"; then
packaging/installer.sh:306:  install -m 0755 "$src_runner" "$RUNNER_DST"
packaging/installer.sh:333:  plutil -insert  ProgramArguments.0 -string "$RUNNER_DST" "$out"
packaging/installer.sh:432:  Runner:       $RUNNER_DST
packaging/installer.sh:446:  Add: $RUNNER_DST
tests/ClearHistoryTests/UpdateCheckerTests.swift:53:        "fb2-to-epub-runner.sh",
tests/ClearHistoryTests/UpdateCheckerTests.swift:276:    try? "OLD\n".write(toFile: "\(bundled)/fb2-to-epub-runner.sh", atomically: true, encoding: .utf8)
tests/ClearHistoryTests/UpdateCheckerTests.swift:303:    try? "SAME\n".write(toFile: "\(installedDir)/fb2-to-epub-runner.sh", atomically: true, encoding: .utf8)
packaging/fb2-to-epub-runner.sh:14:#      ~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh
app/EngineClient+Status.swift:590:        "fb2-to-epub-runner.sh",
app/EngineClient.swift:7://   - runner.sh / watcher.sh are never invoked from Swift.
app/EngineClient.swift:312:        let fallback = "\(home)/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh"
app/EngineClient.swift:315:                    ["-extract", "ProgramArguments.0", "raw", "-o", "-", plistPath])
tests/run-calibre-live-e2e.sh:521:RUNNER="$APP_SUPPORT/bin/fb2-to-epub-runner.sh"
packaging/applet.applescript:77:			"~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh"

exec
/bin/zsh -lc "rg -n -C 8 'installer|installAgent|migrat|firstRun|installedDir|bundled|Resources|resource' app/UpdateChecker.swift app/main.swift app/EngineClient.swift app/EngineClient+Status.swift | head -360" in /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
 succeeded in 0ms:
app/UpdateChecker.swift-2-//
app/UpdateChecker.swift-3-// PART 1: ask the GitHub "latest release" API which version is published and
app/UpdateChecker.swift-4-// whether it is newer than the running build (`checkLatest` → `UpdateInfo`).
app/UpdateChecker.swift-5-//
app/UpdateChecker.swift-6-// PART 2 (this file, below `MARK: - Download + install`): download the .dmg to
app/UpdateChecker.swift-7-// a temp file with a cheap size sanity-check, then hand off to a tiny detached
app/UpdateChecker.swift-8-// bash script that — AFTER the app quits (its leading `sleep` covers the gap) —
app/UpdateChecker.swift-9-// mounts the dmg, replaces the running bundle in place, strips quarantine, and
app/UpdateChecker.swift:10:// relaunches. This mirrors the proven, minimal installer from the sibling
app/UpdateChecker.swift-11-// "Claude Codex Limits" app: no checksum sidecar, no Application-Support
app/UpdateChecker.swift-12-// staging, no backup-rename dance — those added moving parts that hung
app/UpdateChecker.swift-13-// (`hdiutil detach`) and raced. Any failure before the hand-off surfaces as
app/UpdateChecker.swift-14-// `.failure` so the caller can fall back to opening the releases page.
app/UpdateChecker.swift-15-//
app/UpdateChecker.swift-16-// SECURITY INVARIANT: the install target is ALWAYS the running bundle
app/UpdateChecker.swift-17-// (`Bundle.main.bundlePath`), never a hardcoded /Applications. A test copy run
app/UpdateChecker.swift-18-// from build/dist therefore updates *itself*, not a real install. The dmg URL
app/UpdateChecker.swift-19-// is still gated through `isTrustedSource` (https on a GitHub release origin).
app/UpdateChecker.swift-20-
app/UpdateChecker.swift-21-import Foundation
app/UpdateChecker.swift:22:import AppKit   // NSApp.terminate after the installer hand-off
app/UpdateChecker.swift-23-
app/UpdateChecker.swift-24-enum UpdateChecker {
app/UpdateChecker.swift-25-
app/UpdateChecker.swift-26-    // MARK: - Current build version
app/UpdateChecker.swift-27-
app/UpdateChecker.swift-28-    /// The running app's marketing version (CFBundleShortVersionString), e.g.
app/UpdateChecker.swift-29-    /// "0.2.2". Falls back to "0" when the key is absent (dev/CLI runs) so semver
app/UpdateChecker.swift-30-    /// comparison still has a valid operand.
--
app/UpdateChecker.swift-35-    // MARK: - Result model
app/UpdateChecker.swift-36-
app/UpdateChecker.swift-37-    /// What the latest-release lookup found.
app/UpdateChecker.swift-38-    struct UpdateInfo {
app/UpdateChecker.swift-39-        /// Latest published version, leading "v" stripped (e.g. "0.2.3").
app/UpdateChecker.swift-40-        let latestVersion: String
app/UpdateChecker.swift-41-        /// Direct download URL of the release's .dmg asset, if present. Consumed by
app/UpdateChecker.swift-42-        /// `downloadAndInstall` (part 2), which downloads it to a temp file and
app/UpdateChecker.swift:43:        /// hands it to the installer script.
app/UpdateChecker.swift-44-        let dmgURL: URL?
app/UpdateChecker.swift-45-        /// True when `latestVersion` is strictly newer than `currentVersion`.
app/UpdateChecker.swift-46-        let isNewer: Bool
app/UpdateChecker.swift-47-    }
app/UpdateChecker.swift-48-
app/UpdateChecker.swift-49-    // MARK: - Semver compare
app/UpdateChecker.swift-50-
app/UpdateChecker.swift-51-    /// Strict "is `latest` a newer version than `current`?" by numeric, per-component
--
app/UpdateChecker.swift-172-
app/UpdateChecker.swift-173-    /// Errors raised by the download/install path. All of them mean
app/UpdateChecker.swift-174-    /// "auto-update failed" → the caller falls back to the releases page.
app/UpdateChecker.swift-175-    enum InstallError: LocalizedError {
app/UpdateChecker.swift-176-        case noDMG                         // the release had no .dmg asset
app/UpdateChecker.swift-177-        case untrustedSource               // dmg URL not https on an allowed host
app/UpdateChecker.swift-178-        case downloadFailed                // dmg GET failed / non-2xx / too small
app/UpdateChecker.swift-179-        case scriptWriteFailed             // couldn't write the install script
app/UpdateChecker.swift:180:        case launchFailed                  // couldn't spawn the detached installer
app/UpdateChecker.swift-181-
app/UpdateChecker.swift-182-        var errorDescription: String? {
app/UpdateChecker.swift-183-            switch self {
app/UpdateChecker.swift-184-            case .noDMG:             return "В релизе нет .dmg для автоматической установки."
app/UpdateChecker.swift-185-            case .untrustedSource:   return "Источник обновления не прошёл проверку безопасности."
app/UpdateChecker.swift-186-            case .downloadFailed:    return "Не удалось скачать обновление."
app/UpdateChecker.swift-187-            case .scriptWriteFailed: return "Не удалось подготовить установщик."
app/UpdateChecker.swift-188-            case .launchFailed:      return "Не удалось запустить установщик."
--
app/UpdateChecker.swift-200-              let host = url.host?.lowercased()
app/UpdateChecker.swift-201-        else { return false }
app/UpdateChecker.swift-202-        return host == "github.com"
app/UpdateChecker.swift-203-            || host == "objects.githubusercontent.com"
app/UpdateChecker.swift-204-            || host.hasSuffix(".githubusercontent.com")
app/UpdateChecker.swift-205-    }
app/UpdateChecker.swift-206-
app/UpdateChecker.swift-207-    /// Download `info.dmgURL` to a temp file, sanity-check its size, then kick off
app/UpdateChecker.swift:208:    /// the detached installer and quit the app. On any failure BEFORE the hand-off,
app/UpdateChecker.swift-209-    /// `completion(.failure)` is called and the app stays running.
app/UpdateChecker.swift-210-    ///
app/UpdateChecker.swift-211-    /// Deliberately minimal — modeled on the sibling "Claude Codex Limits" updater:
app/UpdateChecker.swift-212-    ///   1. trust-gate the dmg URL (https on a GitHub origin),
app/UpdateChecker.swift-213-    ///   2. download it to `NSTemporaryDirectory()/fb2-update.dmg`,
app/UpdateChecker.swift-214-    ///   3. cheap sanity check: a real dmg is far bigger than 100 KB (a 404/error
app/UpdateChecker.swift-215-    ///      body or truncated transfer is not), so reject anything smaller,
app/UpdateChecker.swift:216:    ///   4. write a tiny bash installer to `NSTemporaryDirectory()/fb2-update.sh`,
app/UpdateChecker.swift-217-    ///   5. spawn it via `/bin/bash <script>` and ask the app to quit.
app/UpdateChecker.swift-218-    ///
app/UpdateChecker.swift-219-    /// `completion` may run on a background thread — the caller hops to main for UI.
app/UpdateChecker.swift:220:    /// On SUCCESS the installer has been launched and `NSApp.terminate` requested;
app/UpdateChecker.swift-221-    /// `completion` is NOT called in that case (the process is on its way out). The
app/UpdateChecker.swift-222-    /// script's leading `sleep 1.5` covers the brief window until this process exits.
app/UpdateChecker.swift-223-    static func downloadAndInstall(
app/UpdateChecker.swift-224-        _ info: UpdateInfo,
app/UpdateChecker.swift-225-        completion: @escaping (Result<Void, Error>) -> Void
app/UpdateChecker.swift-226-    ) {
app/UpdateChecker.swift-227-        guard let dmgURL = info.dmgURL else {
app/UpdateChecker.swift-228-            completion(.failure(InstallError.noDMG))
--
app/UpdateChecker.swift-283-                completion(.failure(error))
app/UpdateChecker.swift-284-            }
app/UpdateChecker.swift-285-        }
app/UpdateChecker.swift-286-        task.resume()
app/UpdateChecker.swift-287-    }
app/UpdateChecker.swift-288-
app/UpdateChecker.swift-289-    // MARK: Install hand-off
app/UpdateChecker.swift-290-
app/UpdateChecker.swift:291:    /// Write the tiny detached installer to `NSTemporaryDirectory()/fb2-update.sh`,
app/UpdateChecker.swift-292-    /// spawn it via `/bin/bash <script>`, then ask the app to quit.
app/UpdateChecker.swift-293-    ///
app/UpdateChecker.swift:294:    /// The installer is intentionally minimal (mirrors the sibling app): it sleeps
app/UpdateChecker.swift-295-    /// briefly to let this process exit, mounts the dmg, copies the new bundle over
app/UpdateChecker.swift-296-    /// the RUNNING one (`Bundle.main.bundlePath` — never a hardcoded /Applications),
app/UpdateChecker.swift-297-    /// strips quarantine, detaches, and relaunches. Paths are interpolated inside
app/UpdateChecker.swift-298-    /// double quotes, so spaces in the bundle/temp path are handled by the shell.
app/UpdateChecker.swift-299-    private static func launchInstaller(dmgPath: String) throws {
app/UpdateChecker.swift-300-        let appPath = Bundle.main.bundlePath
app/UpdateChecker.swift-301-        let scriptPath = NSTemporaryDirectory() + "fb2-update.sh"
app/UpdateChecker.swift-302-        let script = """
--
app/EngineClient.swift-1-// EngineClient — thin Swift bridge between the SwiftUI app and the existing
app/EngineClient.swift:2:// shell engine (LaunchAgent + installer.sh + Calibre). M1.
app/EngineClient.swift-3-//
app/EngineClient.swift-4-// Design rules (from arch/plans-ui.md, "Инварианты" + "Контракты данных"):
app/EngineClient.swift:5://   - The engine logic is NOT changed here. We only *call* installer.sh and
app/EngineClient.swift-6-//     read agent state via launchctl / plutil.
app/EngineClient.swift-7-//   - runner.sh / watcher.sh are never invoked from Swift.
app/EngineClient.swift-8-//   - The app is unsandboxed, no external Swift deps: Foundation only.
app/EngineClient.swift-9-//
app/EngineClient.swift-10-// Everything here shells out via Process. Arguments are passed as a real argv
app/EngineClient.swift-11-// array (never spliced into a shell string), so paths with spaces / unicode are
app/EngineClient.swift:12:// safe — same contract the installer relies on (plutil, not sed).
app/EngineClient.swift-13-
app/EngineClient.swift-14-import Foundation
app/EngineClient.swift-15-
app/EngineClient.swift-16-// MARK: - Public model
app/EngineClient.swift-17-
app/EngineClient.swift-18-/// Resolved state of the LaunchAgent, as the UI needs to render it.
app/EngineClient.swift-19-///
app/EngineClient.swift-20-/// IMPORTANT (Codex's nuance, confirmed by `launchctl print` on a real agent):
--
app/EngineClient.swift-33-    var plistExists: Bool
app/EngineClient.swift-34-
app/EngineClient.swift-35-    /// The UI's single source of truth: is the agent effectively active?
app/EngineClient.swift-36-    /// active = bootstrapped (rc 0) AND plist present AND not disabled.
app/EngineClient.swift-37-    /// Running pid is intentionally NOT part of this — the agent is event-driven.
app/EngineClient.swift-38-    var isActive: Bool { isLoaded && plistExists && !isDisabled }
app/EngineClient.swift-39-}
app/EngineClient.swift-40-
app/EngineClient.swift:41:/// Result of the first-run / migration decision.
app/EngineClient.swift-42-enum FirstRunOutcome: Equatable {
app/EngineClient.swift:43:    /// No plist existed → installer was run on the given default folder.
app/EngineClient.swift-44-    case installedDefault(watchDir: String)
app/EngineClient.swift-45-    /// A plist already existed → we kept the user's existing WATCH_DIR untouched.
app/EngineClient.swift:46:    case migratedExisting(watchDir: String)
app/EngineClient.swift:47:    /// Движка НЕТ — installer даже не звали. UI предлагает поставить Calibre.
app/EngineClient.swift-48-    /// (CAL-1: раньше это и следующий случай маскировались одним blockedNoCalibre.)
app/EngineClient.swift-49-    case needsEngine
app/EngineClient.swift:50:    /// Движок ЕСТЬ, но installer вернул ненулевой код — агент не поднялся.
app/EngineClient.swift-51-    /// Это НЕ повод предлагать установку движка: чинить надо запуск агента.
app/EngineClient.swift-52-    case agentSetupFailed
app/EngineClient.swift-53-}
app/EngineClient.swift-54-
app/EngineClient.swift-55-/// One-shot snapshot the UI reads to prove the bridge works (M1 placeholder
app/EngineClient.swift-56-/// screen; the real Status screen lands at M2).
app/EngineClient.swift-57-struct EngineSnapshot {
app/EngineClient.swift-58-    var watchDir: String?
--
app/EngineClient.swift-69-
app/EngineClient.swift-70-    // --- configuration -----------------------------------------------------
app/EngineClient.swift-71-
app/EngineClient.swift-72-    /// LaunchAgent label. Default is the production label; tests override it
app/EngineClient.swift-73-    /// with a throwaway label so they never collide with the real agent.
app/EngineClient.swift-74-    let label: String
app/EngineClient.swift-75-
app/EngineClient.swift-76-    /// Home directory root. Default is the real HOME; tests point it at a
app/EngineClient.swift:77:    /// `mktemp -d` so the installer writes into a sandbox.
app/EngineClient.swift-78-    let home: String
app/EngineClient.swift-79-
app/EngineClient.swift:80:    /// Absolute path to the bundled installer.sh (in the .app Resources, or a
app/EngineClient.swift-81-    /// checkout path for tests).
app/EngineClient.swift:82:    let installerPath: String
app/EngineClient.swift-83-
app/EngineClient.swift-84-    /// ЯВНЫЙ оверрайд пути к `ebook-convert`. По умолчанию nil → путь целиком
app/EngineClient.swift-85-    /// определяет `CalibreLocator` (контракт детекта, инвариант 5).
app/EngineClient.swift-86-    ///
app/EngineClient.swift-87-    /// Оверрайд оставлен для тестов, которые подсовывают сюда `/bin/echo`: для
app/EngineClient.swift-88-    /// него проверяется ТОЛЬКО сам файл (без meta/polish рядом) — прежняя
app/EngineClient.swift-89-    /// M1-семантика «есть исполняемый файл = движок есть». Продакшен ходит
app/EngineClient.swift-90-    /// через локатор, где валидность = все три CLI.
app/EngineClient.swift-91-    let ebookConvertOverride: String?
app/EngineClient.swift-92-
app/EngineClient.swift-93-    init(
app/EngineClient.swift-94-        label: String = "com.arrivarus.fb2toepub.agent",
app/EngineClient.swift-95-        home: String = EngineHome.resolve(),
app/EngineClient.swift:96:        installerPath: String,
app/EngineClient.swift-97-        ebookConvertPath: String? = nil
app/EngineClient.swift-98-    ) {
app/EngineClient.swift-99-        self.label = label
app/EngineClient.swift-100-        self.home = home
app/EngineClient.swift:101:        self.installerPath = installerPath
app/EngineClient.swift-102-        self.ebookConvertOverride = ebookConvertPath
app/EngineClient.swift-103-    }
app/EngineClient.swift-104-
app/EngineClient.swift-105-    // --- derived paths -----------------------------------------------------
app/EngineClient.swift-106-
app/EngineClient.swift-107-    /// `~/Library/LaunchAgents/<label>.plist`
app/EngineClient.swift-108-    var plistPath: String {
app/EngineClient.swift-109-        "\(home)/Library/LaunchAgents/\(label).plist"
--
app/EngineClient.swift-119-    struct RunResult {
app/EngineClient.swift-120-        let status: Int32
app/EngineClient.swift-121-        let stdout: String
app/EngineClient.swift-122-        let stderr: String
app/EngineClient.swift-123-    }
app/EngineClient.swift-124-
app/EngineClient.swift-125-    /// Run an executable with an explicit argv array. No shell, no string
app/EngineClient.swift-126-    /// splicing. `env` is merged onto the current environment when provided
app/EngineClient.swift:127:    /// (used to inject a throwaway HOME for the installer in tests).
app/EngineClient.swift-128-    @discardableResult
app/EngineClient.swift-129-    func run(_ launchPath: String, _ args: [String], env: [String: String]? = nil) -> RunResult {
app/EngineClient.swift-130-        let proc = Process()
app/EngineClient.swift-131-        proc.executableURL = URL(fileURLWithPath: launchPath)
app/EngineClient.swift-132-        proc.arguments = args
app/EngineClient.swift-133-        if let env = env {
app/EngineClient.swift-134-            var merged = ProcessInfo.processInfo.environment
app/EngineClient.swift-135-            for (k, v) in env { merged[k] = v }
--
app/EngineClient.swift-203-
app/EngineClient.swift-204-    // MARK: - Watch dir (read from installed plist)
app/EngineClient.swift-205-
app/EngineClient.swift-206-    func plistExists() -> Bool {
app/EngineClient.swift-207-        FileManager.default.fileExists(atPath: plistPath)
app/EngineClient.swift-208-    }
app/EngineClient.swift-209-
app/EngineClient.swift-210-    /// Read the installed WATCH_DIR straight from the plist's EnvironmentVariables
app/EngineClient.swift:211:    /// via `plutil -extract` (the same typed value the installer wrote). Returns
app/EngineClient.swift-212-    /// nil when the plist is absent or the key is missing.
app/EngineClient.swift-213-    func readWatchDir() -> String? {
app/EngineClient.swift-214-        guard plistExists() else { return nil }
app/EngineClient.swift-215-        let r = run("/usr/bin/plutil",
app/EngineClient.swift-216-                    ["-extract", "EnvironmentVariables.WATCH_DIR", "raw", "-o", "-", plistPath])
app/EngineClient.swift-217-        guard r.status == 0 else { return nil }
app/EngineClient.swift-218-        let value = r.stdout.trimmingCharacters(in: .whitespacesAndNewlines)
app/EngineClient.swift-219-        return value.isEmpty ? nil : value
--
app/EngineClient.swift-299-    /// construction (arch/plan-fda-synthesis.md, В4). Used by «Проверить снова».
app/EngineClient.swift-300-    @discardableResult
app/EngineClient.swift-301-    func kickstartGentle() -> RunResult {
app/EngineClient.swift-302-        run("/bin/launchctl", ["kickstart", serviceTarget])
app/EngineClient.swift-303-    }
app/EngineClient.swift-304-
app/EngineClient.swift-305-    /// FDA (v1.0.1): the ACTUAL Full-Disk-Access target — the runner launchd spawns,
app/EngineClient.swift-306-    /// i.e. `ProgramArguments[0]` of the installed plist (read via plutil, the same
app/EngineClient.swift:307:    /// typed value the installer wrote). This is the path the user must add/enable in
app/EngineClient.swift-308-    /// the FDA pane; we copy it to the clipboard. Falls back to the canonical
app/EngineClient.swift-309-    /// app-owned runner path (derived from `home`, never a literal `~`) when the plist
app/EngineClient.swift-310-    /// is absent/unreadable. Returns an absolute, tilde-expanded path.
app/EngineClient.swift-311-    func runnerPath() -> String {
app/EngineClient.swift-312-        let fallback = "\(home)/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner.sh"
app/EngineClient.swift-313-        guard plistExists() else { return fallback }
app/EngineClient.swift-314-        let r = run("/usr/bin/plutil",
app/EngineClient.swift-315-                    ["-extract", "ProgramArguments.0", "raw", "-o", "-", plistPath])
app/EngineClient.swift-316-        guard r.status == 0 else { return fallback }
app/EngineClient.swift-317-        let value = r.stdout.trimmingCharacters(in: .whitespacesAndNewlines)
app/EngineClient.swift-318-        return value.isEmpty ? fallback : value
app/EngineClient.swift-319-    }
app/EngineClient.swift-320-
app/EngineClient.swift:321:    // MARK: - installer.sh
app/EngineClient.swift-322-
app/EngineClient.swift:323:    /// Run installer.sh with WATCH_DIR as a single argv element (no shell glue).
app/EngineClient.swift:324:    /// installer.sh itself does the heavy lifting: Calibre detect, plist via
app/EngineClient.swift-325-    /// plutil, bootout→bootstrap→enable→kickstart.
app/EngineClient.swift-326-    ///
app/EngineClient.swift-327-    /// `extraEnv` lets tests inject HOME (so the plist lands in a throwaway tree)
app/EngineClient.swift:328:    /// and FB2_SRC_DIR (so the installer finds the engine scripts from a checkout).
app/EngineClient.swift-329-    @discardableResult
app/EngineClient.swift-330-    func runInstaller(watchDir: String, extraEnv: [String: String]? = nil) -> RunResult {
app/EngineClient.swift:331:        run("/bin/bash", [installerPath, watchDir], env: extraEnv)
app/EngineClient.swift-332-    }
app/EngineClient.swift-333-
app/EngineClient.swift:334:    // MARK: - First-run / migration logic
app/EngineClient.swift-335-
app/EngineClient.swift-336-    /// Decide what to do on launch:
app/EngineClient.swift:337:    ///   - plist exists  → READ existing WATCH_DIR, do NOT overwrite (migration
app/EngineClient.swift-338-    ///                      from an older applet/version).
app/EngineClient.swift-339-    ///   - no plist      → install onto `~/Desktop/fb2-to-epub` (the default).
app/EngineClient.swift-340-    ///   - Calibre absent and we'd need to install → report blocked, let the UI
app/EngineClient.swift-341-    ///                      surface guidance (the install button lands at M2/M3).
app/EngineClient.swift-342-    ///
app/EngineClient.swift-343-    /// `defaultWatchDir` defaults to `<home>/Desktop/fb2-to-epub`.
app/EngineClient.swift:344:    /// `extraEnv` is forwarded to the installer (tests inject HOME/FB2_SRC_DIR).
app/EngineClient.swift-345-    @discardableResult
app/EngineClient.swift:346:    func firstRunSetupIfNeeded(defaultWatchDir: String? = nil,
app/EngineClient.swift-347-                               extraEnv: [String: String]? = nil) -> FirstRunOutcome {
app/EngineClient.swift-348-        // Migration branch: never clobber an existing user folder.
app/EngineClient.swift-349-        if plistExists() {
app/EngineClient.swift-350-            let existing = readWatchDir() ?? "\(home)/Desktop/fb2-to-epub"
app/EngineClient.swift:351:            return .migratedExisting(watchDir: existing)
app/EngineClient.swift-352-        }
app/EngineClient.swift-353-
app/EngineClient.swift:354:        // Fresh install branch — needs Calibre (installer would exit 1 otherwise).
app/EngineClient.swift-355-        guard calibreInstalled() else {
app/EngineClient.swift-356-            return .needsEngine
app/EngineClient.swift-357-        }
app/EngineClient.swift-358-
app/EngineClient.swift-359-        let target = defaultWatchDir ?? "\(home)/Desktop/fb2-to-epub"
app/EngineClient.swift-360-        let res = runInstaller(watchDir: target, extraEnv: extraEnv)
app/EngineClient.swift-361-        if res.status == 0 {
app/EngineClient.swift:362:            // Read back what the installer actually wrote (it normalizes the path).
app/EngineClient.swift-363-            return .installedDefault(watchDir: readWatchDir() ?? target)
app/EngineClient.swift-364-        }
app/EngineClient.swift:365:        // Движок на месте, а installer упал — честно называем это провалом
app/EngineClient.swift-366-        // ЗАПУСКА АГЕНТА (UI не должен предлагать «поставить Calibre»).
app/EngineClient.swift-367-        return .agentSetupFailed
app/EngineClient.swift-368-    }
app/EngineClient.swift-369-
app/EngineClient.swift-370-    // MARK: - Snapshot for UI
app/EngineClient.swift-371-
app/EngineClient.swift-372-    /// One read pass for the placeholder screen: watch dir + agent state + Calibre.
app/EngineClient.swift-373-    func snapshot() -> EngineSnapshot {
--
app/EngineClient+Status.swift-190-        try? FileManager.default.createDirectory(atPath: dir,
app/EngineClient+Status.swift-191-                                                 withIntermediateDirectories: true)
app/EngineClient+Status.swift-192-        let r = run("/usr/bin/open", [dir])
app/EngineClient+Status.swift-193-        return r.status == 0
app/EngineClient+Status.swift-194-    }
app/EngineClient+Status.swift-195-
app/EngineClient+Status.swift-196-    // --- Mutating actions ------------------------------------------------------
app/EngineClient+Status.swift-197-
app/EngineClient+Status.swift:198:    /// Re-point the watched folder at `newDir` by re-running installer.sh, which
app/EngineClient+Status.swift-199-    /// idempotently rewrites the plist's WATCH_DIR and re-bootstraps the agent
app/EngineClient+Status.swift:200:    /// (bootout→bootstrap→kickstart). Returns true on success (installer rc == 0).
app/EngineClient+Status.swift-201-    ///
app/EngineClient+Status.swift-202-    /// Scope: ONLY re-targets the agent. We do NOT move existing files and do NOT
app/EngineClient+Status.swift:203:    /// re-scan — no other side effects. Calibre is required (the installer exits
app/EngineClient+Status.swift-204-    /// non-zero without it), so we short-circuit to false when it's absent and let
app/EngineClient+Status.swift-205-    /// the UI surface guidance.
app/EngineClient+Status.swift-206-    @discardableResult
app/EngineClient+Status.swift-207-    func changeWatchFolder(to newDir: String) -> Bool {
app/EngineClient+Status.swift-208-        guard calibreInstalled() else { return false }
app/EngineClient+Status.swift-209-        let res = runInstaller(watchDir: newDir)
app/EngineClient+Status.swift-210-        return res.status == 0
app/EngineClient+Status.swift-211-    }
--
app/EngineClient+Status.swift-576-        if ProcessInfo.processInfo.environment["FB2_SKIP_KICKSTART"] != "1" {
app/EngineClient+Status.swift-577-            kickstart()
app/EngineClient+Status.swift-578-        }
app/EngineClient+Status.swift-579-        return true
app/EngineClient+Status.swift-580-    }
app/EngineClient+Status.swift-581-
app/EngineClient+Status.swift-582-    // MARK: - Engine refresh on update (fix #2)
app/EngineClient+Status.swift-583-
app/EngineClient+Status.swift:584:    /// The three engine scripts that installer.sh copies into App Support/bin.
app/EngineClient+Status.swift:585:    /// Exact names verified against packaging/installer.sh (RUNNER/WATCHER/COVER
app/EngineClient+Status.swift:586:    /// _DST) and build/build-app.sh (what it stages into Contents/Resources).
app/EngineClient+Status.swift-587-    /// Both sides use these identical filenames, so a name maps 1:1 from the
app/EngineClient+Status.swift:588:    /// bundled copy to the installed copy.
app/EngineClient+Status.swift-589-    private static let engineScriptNames = [
app/EngineClient+Status.swift-590-        "fb2-to-epub-runner.sh",
app/EngineClient+Status.swift-591-        "fb2-to-epub-watcher.sh",
app/EngineClient+Status.swift-592-        "fb2-to-epub-cover-finder.py",
app/EngineClient+Status.swift-593-    ]

2026-07-23T04:44:50.725210Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 88 column 5
exec
/bin/zsh -lc "sed -n '576,680p' app/EngineClient+Status.swift; sed -n '286,370p' app/UpdateChecker.swift; rg -n -C 5 'refreshEngine|engineScriptsAreCurrent|firstRunSetupIfNeeded' app/main.swift app/EngineClient+Status.swift" in /Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub
 succeeded in 0ms:
        if ProcessInfo.processInfo.environment["FB2_SKIP_KICKSTART"] != "1" {
            kickstart()
        }
        return true
    }

    // MARK: - Engine refresh on update (fix #2)

    /// The three engine scripts that installer.sh copies into App Support/bin.
    /// Exact names verified against packaging/installer.sh (RUNNER/WATCHER/COVER
    /// _DST) and build/build-app.sh (what it stages into Contents/Resources).
    /// Both sides use these identical filenames, so a name maps 1:1 from the
    /// bundled copy to the installed copy.
    private static let engineScriptNames = [
        "fb2-to-epub-runner.sh",
        "fb2-to-epub-watcher.sh",
        "fb2-to-epub-cover-finder.py",
    ]

    /// Directory holding the bundled engine scripts. In a real .app this is
    /// `Contents/Resources` (Bundle.main.resourcePath). Tests (which run as a
    /// bare CLI, NOT inside a bundle) point it at a throwaway dir via
    /// FB2_BUNDLED_RES_DIR — same override style as FB2_COVERS_DIR/FB2_SRC_DIR.
    /// nil when neither is available (e.g. a dev binary run outside a bundle and
    /// without the override) → refresh then safely no-ops.
    private var bundledResourceDir: String? {
        if let override = ProcessInfo.processInfo.environment["FB2_BUNDLED_RES_DIR"],
           !override.isEmpty {
            return override
        }
        return Bundle.main.resourcePath
    }

    /// Installed engine bin dir, rooted at THIS client's home (so throwaway-HOME
    /// tests stay isolated): `~/Library/Application Support/fb2-to-epub/bin`.
    private var installedBinDir: String {
        "\(home)/Library/Application Support/fb2-to-epub/bin"
    }

    /// Byte-for-byte compare a bundled script against its installed counterpart.
    /// Treats "installed file missing" as DIFFERENT (the engine must be (re)laid
    /// down). A missing BUNDLED file is treated as "same" (fail-safe: we can't
    /// prove an update, so we don't churn the agent over a packaging gap).
    private func scriptDiffers(name: String, bundledDir: String) -> Bool {
        let bundledPath = "\(bundledDir)/\(name)"
        let installedPath = "\(installedBinDir)/\(name)"
        guard let bundled = try? Data(contentsOf: URL(fileURLWithPath: bundledPath)) else {
            return false // can't read the bundled source → don't trigger a refresh
        }
        guard let installed = try? Data(contentsOf: URL(fileURLWithPath: installedPath)) else {
            return true  // installed copy absent → engine needs to be laid down
        }
        return bundled != installed
    }

    /// True when ANY bundled engine script differs from its installed copy, i.e.
    /// this app build carries a newer (or just changed) engine than what the
    /// agent is currently running. nil bundled dir → false (nothing to compare).
    func bundledEngineDiffersFromInstalled() -> Bool {
        guard let bundledDir = bundledResourceDir else { return false }
        return Self.engineScriptNames.contains { scriptDiffers(name: $0, bundledDir: bundledDir) }
    }

    /// Result of the on-launch engine-refresh check (fix #2), for tests/logging.
    enum EngineRefreshOutcome: Equatable {
        /// No plist → fresh install, not our job (firstRunSetupIfNeeded owns it).
        case skippedNoPlist
        /// Engine scripts already match → agent left completely untouched.
        case upToDate
        /// Engine changed → installer.sh re-ran successfully (bin+plist refreshed).
        case refreshed(watchDir: String)
        /// Engine changed but the installer refresh failed (swallowed, not fatal —
        /// the agent is left as-is and the next launch retries).
        case refreshFailed
    }

    /// Idempotent on-launch repair for the "stale agent after update" bug.
    ///
    /// Runs AFTER firstRunSetupIfNeeded(). It mutates the agent ONLY when the
    /// engine genuinely changed:
    ///   - no plist           → skip (a fresh install is handled elsewhere);
    ///   - plist + scripts same → no-op (e.g. an app-only update like v0.2.2:
    ///                            ZERO agent mutation);
    ///   - plist + any script differs → re-run installer.sh ONCE against the
    ///                            user's EXISTING WATCH_DIR (read from the plist).
    ///                            installer.sh idempotently refreshes bin + plist;
    ///                            its runner-preserve (cmp) keeps the FDA grant.
    ///
    /// The watch folder is NEVER changed. Any installer failure is swallowed and
    /// reported as `.refreshFailed` (we do NOT crash or brick the agent — the
    /// next launch simply retries). Calibre absence ⇒ installer would exit non-
    /// zero ⇒ `.refreshFailed`, which is the correct "leave it as-is" behavior.
    ///
    /// `extraEnv` is forwarded to the installer (tests inject HOME/FB2_SRC_DIR so
    /// the throwaway agent is touched, never the real one).
    @discardableResult
    func refreshEngineIfBundledChanged(extraEnv: [String: String]? = nil) -> EngineRefreshOutcome {
        // Fresh install (no plist) is firstRunSetupIfNeeded's job, not ours.
        guard plistExists() else { return .skippedNoPlist }

        // Engine unchanged → do not touch the agent at all.
        guard bundledEngineDiffersFromInstalled() else { return .upToDate }

        // Engine changed → idempotent refresh against the user's existing folder.
        let watchDir = readWatchDir() ?? "\(home)/Desktop/fb2-to-epub"
        task.resume()
    }

    // MARK: Install hand-off

    /// Write the tiny detached installer to `NSTemporaryDirectory()/fb2-update.sh`,
    /// spawn it via `/bin/bash <script>`, then ask the app to quit.
    ///
    /// The installer is intentionally minimal (mirrors the sibling app): it sleeps
    /// briefly to let this process exit, mounts the dmg, copies the new bundle over
    /// the RUNNING one (`Bundle.main.bundlePath` — never a hardcoded /Applications),
    /// strips quarantine, detaches, and relaunches. Paths are interpolated inside
    /// double quotes, so spaces in the bundle/temp path are handled by the shell.
    private static func launchInstaller(dmgPath: String) throws {
        let appPath = Bundle.main.bundlePath
        let scriptPath = NSTemporaryDirectory() + "fb2-update.sh"
        let script = """
        #!/bin/bash
        sleep 1.5
        DMG="\(dmgPath)"
        APP="\(appPath)"
        MP=$(/usr/bin/hdiutil attach "$DMG" -nobrowse -noverify 2>/dev/null | grep '/Volumes/' | sed -n 's/.*\\(\\/Volumes\\/.*\\)/\\1/p' | tail -1)
        SRC="$MP/fb2-to-epub.app"
        if [ -n "$MP" ] && [ -d "$SRC" ]; then
          /bin/rm -rf "$APP"
          /bin/cp -R "$SRC" "$APP"
          /usr/bin/xattr -dr com.apple.quarantine "$APP" 2>/dev/null
          /usr/bin/hdiutil detach "$MP" >/dev/null 2>&1
          /usr/bin/open "$APP"
        fi
        /bin/rm -f "$DMG" "\(scriptPath)"
        """

        do {
            try script.write(toFile: scriptPath, atomically: true, encoding: .utf8)
        } catch {
            throw InstallError.scriptWriteFailed
        }

        let process = Process()
        process.executableURL = URL(fileURLWithPath: "/bin/bash")
        process.arguments = [scriptPath]
        do {
            try process.run()
        } catch {
            throw InstallError.launchFailed
        }

        // Quit so the script can replace the bundle. Main thread for AppKit.
        DispatchQueue.main.async {
            NSApp.terminate(nil)
        }
    }
}
app/EngineClient+Status.swift-636-        return Self.engineScriptNames.contains { scriptDiffers(name: $0, bundledDir: bundledDir) }
app/EngineClient+Status.swift-637-    }
app/EngineClient+Status.swift-638-
app/EngineClient+Status.swift-639-    /// Result of the on-launch engine-refresh check (fix #2), for tests/logging.
app/EngineClient+Status.swift-640-    enum EngineRefreshOutcome: Equatable {
app/EngineClient+Status.swift:641:        /// No plist → fresh install, not our job (firstRunSetupIfNeeded owns it).
app/EngineClient+Status.swift-642-        case skippedNoPlist
app/EngineClient+Status.swift-643-        /// Engine scripts already match → agent left completely untouched.
app/EngineClient+Status.swift-644-        case upToDate
app/EngineClient+Status.swift-645-        /// Engine changed → installer.sh re-ran successfully (bin+plist refreshed).
app/EngineClient+Status.swift-646-        case refreshed(watchDir: String)
--
app/EngineClient+Status.swift-649-        case refreshFailed
app/EngineClient+Status.swift-650-    }
app/EngineClient+Status.swift-651-
app/EngineClient+Status.swift-652-    /// Idempotent on-launch repair for the "stale agent after update" bug.
app/EngineClient+Status.swift-653-    ///
app/EngineClient+Status.swift:654:    /// Runs AFTER firstRunSetupIfNeeded(). It mutates the agent ONLY when the
app/EngineClient+Status.swift-655-    /// engine genuinely changed:
app/EngineClient+Status.swift-656-    ///   - no plist           → skip (a fresh install is handled elsewhere);
app/EngineClient+Status.swift-657-    ///   - plist + scripts same → no-op (e.g. an app-only update like v0.2.2:
app/EngineClient+Status.swift-658-    ///                            ZERO agent mutation);
app/EngineClient+Status.swift-659-    ///   - plist + any script differs → re-run installer.sh ONCE against the
--
app/EngineClient+Status.swift-667-    /// zero ⇒ `.refreshFailed`, which is the correct "leave it as-is" behavior.
app/EngineClient+Status.swift-668-    ///
app/EngineClient+Status.swift-669-    /// `extraEnv` is forwarded to the installer (tests inject HOME/FB2_SRC_DIR so
app/EngineClient+Status.swift-670-    /// the throwaway agent is touched, never the real one).
app/EngineClient+Status.swift-671-    @discardableResult
app/EngineClient+Status.swift:672:    func refreshEngineIfBundledChanged(extraEnv: [String: String]? = nil) -> EngineRefreshOutcome {
app/EngineClient+Status.swift:673:        // Fresh install (no plist) is firstRunSetupIfNeeded's job, not ours.
app/EngineClient+Status.swift-674-        guard plistExists() else { return .skippedNoPlist }
app/EngineClient+Status.swift-675-
app/EngineClient+Status.swift-676-        // Engine unchanged → do not touch the agent at all.
app/EngineClient+Status.swift-677-        guard bundledEngineDiffersFromInstalled() else { return .upToDate }
app/EngineClient+Status.swift-678-
--
app/main.swift-511-                    NSLog("fb2-to-epub: активация агента не удалась (rc=%d): %@", res.status, firstErr)
app/main.swift-512-                    return false
app/main.swift-513-                }
app/main.swift-514-                return true
app/main.swift-515-            } else {
app/main.swift:516:                switch engine.firstRunSetupIfNeeded() {
app/main.swift-517-                case .installedDefault, .migratedExisting:
app/main.swift-518-                    return true
app/main.swift-519-                case .agentSetupFailed, .needsEngine:
app/main.swift-520-                    NSLog("fb2-to-epub: активация агента (первый запуск) не удалась")
app/main.swift-521-                    return false
--
app/main.swift-536-        DispatchQueue.global(qos: .utility).async {
app/main.swift-537-            CalibreInstaller.cleanupLeftovers(home: home)
app/main.swift-538-        }
app/main.swift-539-        // Production launch: only install when there is no plist; an existing
app/main.swift-540-        // WATCH_DIR is read and kept (migration). This never clobbers the user.
app/main.swift:541:        let outcome = engine.firstRunSetupIfNeeded()
app/main.swift-542-
app/main.swift:543:        // Fix #2 — stale agent after an update. firstRunSetupIfNeeded() leaves an
app/main.swift-544-        // EXISTING plist untouched (.migratedExisting), so after a DMG/auto update
app/main.swift-545-        // the agent keeps running the OLD bin scripts + plist. Here we refresh them
app/main.swift-546-        // ONLY when the engine genuinely changed: compare the bundled scripts
app/main.swift-547-        // against the installed ones and, if any differ, re-run installer.sh once
app/main.swift-548-        // against the user's existing WATCH_DIR (idempotent; runner-preserve keeps
app/main.swift-549-        // FDA). An app-only update (identical bin, e.g. v0.2.2) does NOTHING. Run
app/main.swift-550-        // off the main thread so launch/UI never blocks; any failure is swallowed
app/main.swift-551-        // (logged) and retried on the next launch — never bricks the agent.
app/main.swift-552-        DispatchQueue.global(qos: .utility).async {
app/main.swift:553:            let result = engine.refreshEngineIfBundledChanged()
app/main.swift-554-            switch result {
app/main.swift-555-            case .refreshed(let dir):
app/main.swift-556-                NSLog("fb2-to-epub: engine changed on update → refreshed agent (watch: \(dir))")
app/main.swift-557-            case .refreshFailed:
app/main.swift-558-                NSLog("fb2-to-epub: engine changed but installer refresh failed; leaving agent as-is (will retry next launch)")

2026-07-23T04:44:53.741367Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_reasoning_summaries` at line 88 column 5
web search: 
web search: site:developer.apple.com/documentation/servicemanagement "BundleProgram" ...
codex
## 1) Вердикт — A: внешний immutable Mach-O + текущий LaunchAgent

Выбрать standalone `~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner` без `.sh`, сохранив обычный plist/`launchctl`.

Механика TCC:

- Для non-bundled binary идентификатором служит абсолютный путь, дополнительно проверяется `CodeRequirement`; для приложения — bundle ID + `CodeRequirement`. Встроенный helper наследует права enclosing app. Это документированная модель TCC/PPPC Apple: [PrivacyPreferencesPolicyControl Identity](https://developer.apple.com/documentation/devicemanagement/privacypreferencespolicycontrol/services-data.dictionary/identity).
- У ad-hoc подписи нет TeamID/signing identity. Проверка текущего `.app` показывает DR вида `cdhash H"…" or cdhash H"…"` — по одному CDHash на архитектуру universal-бинаря.
- Следовательно, A матчится не «только по пути» и не «только по cdhash», а по стабильной паре: **absolute path + CodeRequirement(cdhash)**. Замена файла байт-в-байт по тому же пути сохраняет грант; inode, mtime и xattr не существенны.
- Авто-апдейт `.app` не затрагивает эту identity, пока установленный helper остаётся байт-в-байт прежним.

Решения внутри A:

- **Артефакт:** хранить в обычном Git canonical, уже собранный и ad-hoc-подписанный Mach-O, плюс исходник, pinned-рецепт, SHA-256 и ожидаемый вывод `codesign -dr -`. Воспроизводимая сборка полезна для аудита, но не должна быть источником release-файла: смена Xcode/SDK/linker может изменить CDHash.
- **Архитектуры:** universal `arm64+x86_64`, поскольку приложение сейчас universal и заявляет macOS 11+. Никогда не делать thinning/lipo/re-sign после фиксации артефакта.
- **Запуск watcher:** helper запускает `/bin/bash watcher.sh` дочерним процессом и ждёт его, сохраняя себя responsible process; не заменяет себя через `exec`.
- **Quarantine:** снимать только `com.apple.quarantine` с установленного helper после проверки подписи и SHA. Это предотвращает безоконный Gatekeeper-блок из launchd и не меняет CDHash. Не применять рекурсивный `xattr -cr` к App Support.
- **Build signing:** перейти с рекурсивного пере-подписания helper к inside-out signing; проверять SHA helper до и после подписи `.app`.

Оценка альтернатив:

- **B — отклонить.** Да, embedded helper получает FDA приложения без второго UX. Но у текущего ad-hoc bundle DR закреплён на CDHash; изменение executable, `Info.plist` или ресурсов и новая подпись дают другой CDHash. Старое правило TCC может остаться в базе/UI, но новый `.app` ему не соответствует — функционально грант мёртв. Стабильного bundle ID недостаточно. Apple прямо предупреждает, что ad-hoc code не позволяет распознать N+1 как то же приложение: [On File System Permissions](https://developer.apple.com/forums/thread/678819), [TN3127](https://developer.apple.com/documentation/technotes/tn3127-inside-code-signing-requirements/). `identifier`-only custom DR небезопасен: его сможет воспроизвести любой ad-hoc binary.
- **C — отклонить для v1.0.2.** `SMAppService`/`BundleProgram` решает регистрацию и bundle-relative путь, но не создаёт стабильную signing identity. Helper всё равно должен быть Mach-O, а FDA всё равно наследует обновляемый ad-hoc bundle и ломается по CDHash. Дополнительно: API только macOS 13+, появляется отдельное одобрение Background Items, а динамические `WatchPaths`/пути Calibre нельзя записывать в подписанный bundled plist без переделки агента на внешнюю конфигурацию. Это крупный рефакторинг без решения исходной проблемы. См. [SMAppService](https://developer.apple.com/documentation/servicemanagement/smappservice) и [BundleProgram migration](https://developer.apple.com/documentation/servicemanagement/updating-helper-executables-from-earlier-versions-of-macos).

## 2) Компоненты

| Компонент | Ответственность / зависимости | Как тестировать |
|---|---|---|
| Native runner | Минимальный POSIX Mach-O; находит соседний watcher, порождает `/bin/bash`, ждёт, передаёт сигналы/exit code. Зависимости: libc, `/bin/bash` | Fake watcher: env, cwd, exit code, сигналы; `file`, `lipo`, `codesign`; Tahoe tccd-log показывает runner responsible |
| Golden artifact pipeline | Source + canonical universal binary + SHA/DR fixture; штатная сборка только копирует, не компилирует и не подписывает его заново | SHA до/после сборки `.app` и DMG одинаков; две сборки приложения содержат идентичный helper |
| `installer.sh` | Проверить артефакт, атомарно установить в новый путь, точечно снять quarantine, заменить `ProgramArguments[0]`, перезагрузить тот же label | Изолированный HOME; fresh/legacy/idempotent/corrupt-helper сценарии; сохранение `WATCH_DIR` и env |
| Build/update refresh | Положить golden payload в bundle без мутации; добавить native runner в перечень engine payload; legacy `.sh` считать обязательной миграцией | После замены `.app` новый helper обнаруживается как missing; повторный апдейт не меняет установленный SHA |
| `EngineClient` + FDA UI | Новый fallback-path; clipboard по-прежнему читает plist; карточка показывает `fb2-to-epub-runner`, не `.sh` | FDAContract: plist/fallback; UI-состояния denied/checking/recovered |
| README RU/EN | Исправить путь, схему responsible process и ложное утверждение, что один bundle ID сохраняет ad-hoc TCC | Поиск старого `runner.sh`; ручная сверка инструкции на чистом Tahoe |

## 3) Milestones по зависимостям

1. **M0 — TCC spike:** native prototype → реальный запуск на Tahoe 26.5.2 → доказать subject/responsible chain и сценарий child-not-exec.
2. **M1 — Freeze artifact:** universal build → ad-hoc sign один раз → зафиксировать binary/SHA/DR/source.
3. **M2 — Packaging и миграция:** build не мутирует artifact → installer ставит новый путь → plist переключается → update-refresh распознаёт legacy.
4. **M3 — UI, тесты, README:** новый fallback/текст/имя; обновить FDA, updater и live-E2E тесты.
5. **M4 — Release gate:** upgrade v1.0.1→v1.0.2 и v1.0.2→изменённый тестовый N+1 на чистой VM; Apple Silicon и Intel.

## 4) Миграция

- На запуске v1.0.2 явно распознать `ProgramArguments[0]` с `fb2-to-epub-runner.sh`; не ждать случайного изменения watcher.
- Считать существующий `WATCH_DIR`, установить и проверить Mach-O, затем `bootout` старого job, атомарно записать plist с новым `ProgramArguments[0]`, `bootstrap/enable/kickstart`.
- Миграция должна завершиться до показа FDA CTA, иначе clipboard кратковременно скопирует старый `.sh`.
- Старый `.sh` оставить неиспользуемым на один релиз для rollback; позже удалить. Старую запись FDA приложение удалить не может — после успешного нового гранта README предлагает удалить строку `.sh` вручную.
- Перенести старый грант невозможно: фактическим subject был `/bin/bash`. Для существующих установок неизбежен один новый FDA grant к Mach-O.

## 5) Тест «апдейт приложения → грант жив»

1. Чистая Tahoe 26.5.2 VM; убедиться, что FDA нет у приложения, Terminal и `/bin/bash`.
2. Установить N, зафиксировать path, SHA, DR и CDHash helper; выдать FDA только ему.
3. Доказать реальным чтением/конвертацией из Desktop и tccd-логом, что subject/responsible — native runner.
4. Установить N+1 штатным авто-апдейтом: `.app` и его CDHash обязаны измениться, watcher можно изменить, helper — нет.
5. Не трогая FDA, подтвердить прежние path/SHA/DR helper и успешную новую конвертацию.
6. Negative control: заменить helper пересобранным вариантом с другим CDHash — доступ обязан пропасть до нового гранта. Состояние переключателя само по себе не является доказательством; критерий — фактический доступ.

## 6) Риски

- **TCC остаётся implementation-sensitive:** возможность добавлять CLI в FDA уже регрессировала в macOS 26.2; обязательный реальный gate на поддерживаемых версиях.
- **Golden helper почти неизменяем:** любой баг/уязвимость в нём означает новый CDHash и повторный FDA UX. Поэтому код должен быть предельно мал и не парсить пользовательские данные.
- **Mutable watcher получает FDA через responsible runner:** проверять fixed path, regular file, owner/mode, запретить symlink. Полная защита от same-user tampering потребует подписи manifest/payload проектным ключом с публичным ключом внутри immutable helper.
- **Build может незаметно пере-подписать/thin helper:** SHA-check после `.app` signing и после извлечения из DMG должен быть release-blocking.
- **Миграционная гонка:** сначала валидный helper, затем переключение plist; при неуспешном bootstrap сохранять диагностируемое состояние и повторять миграцию, не менять `WATCH_DIR`.
- **B/C:** остаются допустимыми только после появления стабильного сертификатного anchor/TeamID; при текущем ad-hoc ограничении они не выполняют требование update-survival.
tokens used
137 151
## 1) Вердикт — A: внешний immutable Mach-O + текущий LaunchAgent

Выбрать standalone `~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-runner` без `.sh`, сохранив обычный plist/`launchctl`.

Механика TCC:

- Для non-bundled binary идентификатором служит абсолютный путь, дополнительно проверяется `CodeRequirement`; для приложения — bundle ID + `CodeRequirement`. Встроенный helper наследует права enclosing app. Это документированная модель TCC/PPPC Apple: [PrivacyPreferencesPolicyControl Identity](https://developer.apple.com/documentation/devicemanagement/privacypreferencespolicycontrol/services-data.dictionary/identity).
- У ad-hoc подписи нет TeamID/signing identity. Проверка текущего `.app` показывает DR вида `cdhash H"…" or cdhash H"…"` — по одному CDHash на архитектуру universal-бинаря.
- Следовательно, A матчится не «только по пути» и не «только по cdhash», а по стабильной паре: **absolute path + CodeRequirement(cdhash)**. Замена файла байт-в-байт по тому же пути сохраняет грант; inode, mtime и xattr не существенны.
- Авто-апдейт `.app` не затрагивает эту identity, пока установленный helper остаётся байт-в-байт прежним.

Решения внутри A:

- **Артефакт:** хранить в обычном Git canonical, уже собранный и ad-hoc-подписанный Mach-O, плюс исходник, pinned-рецепт, SHA-256 и ожидаемый вывод `codesign -dr -`. Воспроизводимая сборка полезна для аудита, но не должна быть источником release-файла: смена Xcode/SDK/linker может изменить CDHash.
- **Архитектуры:** universal `arm64+x86_64`, поскольку приложение сейчас universal и заявляет macOS 11+. Никогда не делать thinning/lipo/re-sign после фиксации артефакта.
- **Запуск watcher:** helper запускает `/bin/bash watcher.sh` дочерним процессом и ждёт его, сохраняя себя responsible process; не заменяет себя через `exec`.
- **Quarantine:** снимать только `com.apple.quarantine` с установленного helper после проверки подписи и SHA. Это предотвращает безоконный Gatekeeper-блок из launchd и не меняет CDHash. Не применять рекурсивный `xattr -cr` к App Support.
- **Build signing:** перейти с рекурсивного пере-подписания helper к inside-out signing; проверять SHA helper до и после подписи `.app`.

Оценка альтернатив:

- **B — отклонить.** Да, embedded helper получает FDA приложения без второго UX. Но у текущего ad-hoc bundle DR закреплён на CDHash; изменение executable, `Info.plist` или ресурсов и новая подпись дают другой CDHash. Старое правило TCC может остаться в базе/UI, но новый `.app` ему не соответствует — функционально грант мёртв. Стабильного bundle ID недостаточно. Apple прямо предупреждает, что ad-hoc code не позволяет распознать N+1 как то же приложение: [On File System Permissions](https://developer.apple.com/forums/thread/678819), [TN3127](https://developer.apple.com/documentation/technotes/tn3127-inside-code-signing-requirements/). `identifier`-only custom DR небезопасен: его сможет воспроизвести любой ad-hoc binary.
- **C — отклонить для v1.0.2.** `SMAppService`/`BundleProgram` решает регистрацию и bundle-relative путь, но не создаёт стабильную signing identity. Helper всё равно должен быть Mach-O, а FDA всё равно наследует обновляемый ad-hoc bundle и ломается по CDHash. Дополнительно: API только macOS 13+, появляется отдельное одобрение Background Items, а динамические `WatchPaths`/пути Calibre нельзя записывать в подписанный bundled plist без переделки агента на внешнюю конфигурацию. Это крупный рефакторинг без решения исходной проблемы. См. [SMAppService](https://developer.apple.com/documentation/servicemanagement/smappservice) и [BundleProgram migration](https://developer.apple.com/documentation/servicemanagement/updating-helper-executables-from-earlier-versions-of-macos).

## 2) Компоненты

| Компонент | Ответственность / зависимости | Как тестировать |
|---|---|---|
| Native runner | Минимальный POSIX Mach-O; находит соседний watcher, порождает `/bin/bash`, ждёт, передаёт сигналы/exit code. Зависимости: libc, `/bin/bash` | Fake watcher: env, cwd, exit code, сигналы; `file`, `lipo`, `codesign`; Tahoe tccd-log показывает runner responsible |
| Golden artifact pipeline | Source + canonical universal binary + SHA/DR fixture; штатная сборка только копирует, не компилирует и не подписывает его заново | SHA до/после сборки `.app` и DMG одинаков; две сборки приложения содержат идентичный helper |
| `installer.sh` | Проверить артефакт, атомарно установить в новый путь, точечно снять quarantine, заменить `ProgramArguments[0]`, перезагрузить тот же label | Изолированный HOME; fresh/legacy/idempotent/corrupt-helper сценарии; сохранение `WATCH_DIR` и env |
| Build/update refresh | Положить golden payload в bundle без мутации; добавить native runner в перечень engine payload; legacy `.sh` считать обязательной миграцией | После замены `.app` новый helper обнаруживается как missing; повторный апдейт не меняет установленный SHA |
| `EngineClient` + FDA UI | Новый fallback-path; clipboard по-прежнему читает plist; карточка показывает `fb2-to-epub-runner`, не `.sh` | FDAContract: plist/fallback; UI-состояния denied/checking/recovered |
| README RU/EN | Исправить путь, схему responsible process и ложное утверждение, что один bundle ID сохраняет ad-hoc TCC | Поиск старого `runner.sh`; ручная сверка инструкции на чистом Tahoe |

## 3) Milestones по зависимостям

1. **M0 — TCC spike:** native prototype → реальный запуск на Tahoe 26.5.2 → доказать subject/responsible chain и сценарий child-not-exec.
2. **M1 — Freeze artifact:** universal build → ad-hoc sign один раз → зафиксировать binary/SHA/DR/source.
3. **M2 — Packaging и миграция:** build не мутирует artifact → installer ставит новый путь → plist переключается → update-refresh распознаёт legacy.
4. **M3 — UI, тесты, README:** новый fallback/текст/имя; обновить FDA, updater и live-E2E тесты.
5. **M4 — Release gate:** upgrade v1.0.1→v1.0.2 и v1.0.2→изменённый тестовый N+1 на чистой VM; Apple Silicon и Intel.

## 4) Миграция

- На запуске v1.0.2 явно распознать `ProgramArguments[0]` с `fb2-to-epub-runner.sh`; не ждать случайного изменения watcher.
- Считать существующий `WATCH_DIR`, установить и проверить Mach-O, затем `bootout` старого job, атомарно записать plist с новым `ProgramArguments[0]`, `bootstrap/enable/kickstart`.
- Миграция должна завершиться до показа FDA CTA, иначе clipboard кратковременно скопирует старый `.sh`.
- Старый `.sh` оставить неиспользуемым на один релиз для rollback; позже удалить. Старую запись FDA приложение удалить не может — после успешного нового гранта README предлагает удалить строку `.sh` вручную.
- Перенести старый грант невозможно: фактическим subject был `/bin/bash`. Для существующих установок неизбежен один новый FDA grant к Mach-O.

## 5) Тест «апдейт приложения → грант жив»

1. Чистая Tahoe 26.5.2 VM; убедиться, что FDA нет у приложения, Terminal и `/bin/bash`.
2. Установить N, зафиксировать path, SHA, DR и CDHash helper; выдать FDA только ему.
3. Доказать реальным чтением/конвертацией из Desktop и tccd-логом, что subject/responsible — native runner.
4. Установить N+1 штатным авто-апдейтом: `.app` и его CDHash обязаны измениться, watcher можно изменить, helper — нет.
5. Не трогая FDA, подтвердить прежние path/SHA/DR helper и успешную новую конвертацию.
6. Negative control: заменить helper пересобранным вариантом с другим CDHash — доступ обязан пропасть до нового гранта. Состояние переключателя само по себе не является доказательством; критерий — фактический доступ.

## 6) Риски

- **TCC остаётся implementation-sensitive:** возможность добавлять CLI в FDA уже регрессировала в macOS 26.2; обязательный реальный gate на поддерживаемых версиях.
- **Golden helper почти неизменяем:** любой баг/уязвимость в нём означает новый CDHash и повторный FDA UX. Поэтому код должен быть предельно мал и не парсить пользовательские данные.
- **Mutable watcher получает FDA через responsible runner:** проверять fixed path, regular file, owner/mode, запретить symlink. Полная защита от same-user tampering потребует подписи manifest/payload проектным ключом с публичным ключом внутри immutable helper.
- **Build может незаметно пере-подписать/thin helper:** SHA-check после `.app` signing и после извлечения из DMG должен быть release-blocking.
- **Миграционная гонка:** сначала валидный helper, затем переключение plist; при неуспешном bootstrap сохранять диагностируемое состояние и повторять миграцию, не менять `WATCH_DIR`.
- **B/C:** остаются допустимыми только после появления стабильного сертификатного anchor/TeamID; при текущем ad-hoc ограничении они не выполняют требование update-survival.
