# Status — онбординг Calibre v1.0.0 ✅ · FDA-онбординг v1.0.1 ✅ · бинарный раннер v1.0.2 ✅

> **🎉 v1.0.2 ВЫПУЩЕН 2026-07-23** («да» человека): PR #28 (merge c8b1588) →
> https://github.com/ArrivaRUS/fb2-to-epub/releases/tag/v1.0.2 · DMG sha256
> `2b8b2ed2…4163ac` (сверен повторным скачиванием). Helper в бандле байт-в-байт (`926fc039…`).
> **Фикс Tahoe-FDA:** launchd-агент запускается через замороженный Mach-O `fb2-to-epub-agent`
> (spawn+wait watcher, НЕ exec) — TCC видит наш бинарь, а не /bin/bash (platform hard-deny).
> Диагноз 2 архитектора (конвергенция) + ревью ×2 APPROVE + 607/0 тестов + T0-гейт (tccd
> subject=helper). Находка A/B: non-platform helper читает Desktop БЕЗ гранта (переход
> script→Mach-O чинит доступ сам) — возможно, человеку грант вообще не понадобится.
> **Грант в изоляции неразрешим** (TCC.db без FDA не прочитать; грант не переносится на
> тест-метку; Desktop-проба = шум) → авторитетная проверка ТОЛЬКО на боевой машине человека
> после установки. Уроки: [[tahoe-tcc-no-fda-for-scripts]] (обновлён A/B-находкой), .patches/020.
> **ЖДЁМ: человек ставит v1.0.2 → Юрка читает РЕАЛЬНЫЙ folder_access боевого агента = финал.**

> **v1.0.1 FDA-онбординг (2026-07-22, D46):** боевой тест 1.0.0 вскрыл дыру CJM — агент без
> Full Disk Access молча «работает» при неконвертируемых книгах. Двойной архитектор (Claude ∥
> Sol) → синтез `arch/plan-fda-synthesis.md` → developer сдал FDA-1/2/3 (R1-валидация denied
> на боевом Маке изолированным агентом; проба+контракт agent.folder_access; FolderAccessCard
> 4 подачи; recheck kickstart-без-k + ts-семантика; клипборд+панель+fallback; probe-тесты 26/26,
> +18 юнитов, lib-prod-guard по уроку 018) → ревью (Claude+Codex): НУЖНЫ ПРАВКИ — 🟠A1
> (терминальный recheck глушит живое ok) + 🟠A2 (секундный ts → ложный таймаут) + A3-A6/ниты →
> **цикл правок №1 СДАН** (probe 29/0, юниты 147/0, сборка OK) → **ре-ревью PASS** (раунд 2,
> Claude сам прогнал; остаточное наблюдение timeout — не блокер). Live-verify Юрки: офскрин-рендеры
> подач сверены ✓; живое ОКНО тест-сборки невозможно (тот же bundle-id + свой пробящий агент —
> урок `.patches/019`) → клик кнопки+edge-pop проверяются на боевом denied-стенде ПОСЛЕ релиза
> (реальный TCC + грант — user-only). Изоляция всех прогонов чистая (боевой state.json так и не
> создан = denied-стенд сохранён). **🎉 v1.0.1 ВЫПУЩЕН 2026-07-23** («да» человека):
> PR #27 (merge ce608a6, 17 файлов, +1878) → https://github.com/ArrivaRUS/fb2-to-epub/releases/tag/v1.0.1
> DMG sha256 `b039a3d4…ba1ced60` (сверен повторным скачиванием). Боевая проверка человека:
> обновление → карточка «Нет доступа» сама → грант → «Проверить снова» → конвертация.
>
> **⚡ БОЕВОЙ ИНЦИДЕНТ v1.0.1 (2026-07-23) → фича v1.0.2 «бинарный раннер»:** CJM отработал
> (карточка сама, edge-pop поднял окно — в логе «raising app»), человек выдал FDA раннеру И
> приложению — агент ВСЁ РАВНО denied после полной перезагрузки (bootout/bootstrap/kickstart —
> Юрка проверил на боевом). **Debugger доказал tccd-логом:** Tahoe (26.5.2) атрибутирует TCC-запрос
> к Mach-O бинарю процесса — для shebang-скрипта subject=/bin/bash («AUTHREQ_SUBJECT subject=
> /bin/bash», bundle attribution null, «Platform binary prompting is Deny» → отказ всегда молчалив);
> путь runner.sh в запросе не фигурирует = грант скрипту мёртв как класс (на старом Маке работал —
> до-Tahoe матчил по пути скрипта). Грант приложению агента не покрывает. Папка вне TCC-зон
> работает без грантов (E2b «ok»). Дочерние атрибутируются к responsible — чинить только
> ProgramArguments[0]. Улики: scratchpad/tcc_e1.log, tcc_e2.log. Боевое не тронуто (sha-страж).
> **Суд двух архитекторов ЗАВЕРШЁН (2026-07-23): КОНВЕРГЕНЦИЯ** — оба независимо выбрали
> вариант A (standalone immutable Mach-O helper) + spawn+wait-НЕ-exec + замороженный артефакт
> в git; оба отклонили B по одной причине (ad-hoc cdhash → апдейт молча убивает грант).
> Синтез: `arch/plan-binrunner-synthesis.md`. Решения судьи: имя `fb2-to-epub-agent` (мёртвый
> runner.sh в панели человека — похожее имя спровоцировало бы «уже сделано»); runner.sh остаётся
> один релиз (rollback); миграция plist ДО показа карточки (иначе клипборд скопирует старый
> путь — находка Sol); точечный quarantine-strip после sha; байт-страж артефакта в сборке
> release-blocking; upgrade-тест с negative control; артефакт в публичный git с PROVENANCE — ок.
> **BR-0/2/3 СДАНЫ (2026-07-23): T0-гейт ПРОЙДЕН** — tccd: subject=/…/bin/fb2-to-epub-agent,
> «/bin/bash: 0 упоминаний», внук (ls из bash-стаба) атрибутирован к responsible-хелперу;
> артефакт заморожен (sha `926fc039…`, cdhash arm64 `6b6429…`/x86_64 `030c3d…`, PROVENANCE.md,
> freeze-guard в build-once.sh). Интеграция: installer (helper+cmp-preserve+sha→точечный xattr,
> plist→helper, runner.sh остаётся 1 релиз), build-app (артефакт в Resources + release-blocking
> байт-страж), гонка клипборда закрыта СИНХРОННЫМ refresh ДО окна (остаточное: refreshFailed →
> старый путь, конвертация в этом состоянии всё равно мертва, ретрай на след. запуске).
> Тесты: **новый run-agent-helper-tests 33/33** + вся регрессия зелёная, build 1.0.2-dev RC=0.
> **Находка T0:** helper не платформенный → Tahoe МОЖЕТ показать честный системный промпт
> «доступ к папке "Рабочий стол"» — один клик «Разрешить» может заменить FDA целиком
> (карточка остаётся страховкой); учесть в BR-1/боевом e2e.
> **Ревью ЗАВЕРШЕНО: APPROVE с замечаниями** (Claude полный проход: C-хелпер 0 warnings на
> -Wall/-Wextra/-Wpedantic/-Wconversion/-Wshadow, «хелпер МОЖНО замораживать НАВСЕГДА»;
> Codex: «No blocking correctness issues»; расхождений нет). Все находки закрыты микро-пакетом:
> name-parity guard секция D (35/35, негатив доказан), микро-окна 2-3 → PROVENANCE, TODO v1.0.3
> в live-e2e (RUNNER→helper при выпиле runner.sh). Находка 4 (run() без таймаута) — беклог;
> находка 5 (refreshFailed→старый путь в клипборде) — known-issues строка релиз-нот.
> **BR-1 В ОЖИДАНИИ ЧЕЛОВЕКА:** артефакт положен Юркой на боевой путь аддитивно
> (`~/Library/Application Support/fb2-to-epub/bin/fb2-to-epub-agent`, sha=PROVENANCE `926fc039…`,
> CDHash `6b6429…`, quarantine снят; боевой plist его пока НЕ знает — инертен). Человеку отправлена
> точная инструкция гранта (панель FDA → «+» → путь → тоггл; старую строку runner.sh можно удалить).
> После «готово»: allow-проверка тест-меткой (throwaway WATCH_DIR-подпапка Desktop + throwaway
> state/covers/log env, скелет scratchpad/t0-gate/run-t0.sh) → BR-4 (negative control: пересобранный
> cdhash ОБЯЗАН потерять доступ + полная регрессия) → release notes (пишутся) → v1.0.2 ⛔ СТОП.
> Человек: «срочности нет, хочу чтобы приложение заработало» — обход сменой папки отклонён,
> чиним по-настоящему. Его текущие гранты (runner.sh, приложение) инертны, не чистим.

> ⚑ Версия релиза: **1.0.0** (решение человека D45, 2026-07-22; было предложение 0.10.0).
> Всё, что ниже писалось как «0.10.0», читается как 1.0.0; dev-сборки — `1.0.0-dev`.

> Живой лог исполнения `plans.md`. Обновляется по ходу разработки.
> Резюмируемость: первый пункт «Next» = первый незакрытый milestone в plans.md.

## Текущая фаза
**Разработка (2026-07-21).** Планирование закрыто целиком: архитектура G4 (D37–D42) +
дизайн-спец принята G3 (**D43**, 2026-07-21) → блокеров планирования нет, все милейстоны открыты.
Идёт CAL-1; после него CAL-2 ∥ CAL-3.

## Done
- [x] Ресёрч автоустановки (`research/calibre-auto-install.md`): URL/SHA-512/quarantine/GPL.
- [x] UX-флоу + макеты A/B (`design/calibre-onboarding/{flow.md,direction-A-banner.html,direction-B-blocker.html}`), G2b: D37 (гибрид), D38 (имя кнопки), D39 (место установки).
- [x] Двойной архитектор: `arch/plan-calibre-onboarding-claude.md` ∥ `arch/plan-calibre-onboarding-codex.md` → синтез `arch/plan-calibre-onboarding-synthesis.md` (каркас #1 + находки #2).
- [x] Развилки решены человеком (G4): **D40** (закрытие окна не прерывает установку; Cmd-Q спрашивает; copy/promote откладывает выход), **D41** (пре-чек 1,5 ГБ, текст «~1 ГБ»), **D42** (macOS < 14 → только «Вручную», без легаси-пиннинга).
- [x] Июньский execution-pack фичи «DMG-инсталлер» заархивирован: `arch/archive-2026-06-dmg-installer/{plans,status,test-plan}.md`.
- [x] Новый execution-pack собран: `plans.md` (CAL-1…CAL-5) + `status.md` + `test-plan.md` (2026-07-19).

## 🎉 ФИЧА ВЫПУЩЕНА — v1.0.0 (2026-07-22)
**PR #26 (merge 2623e35, коммит 912b4c7, 35 файлов, +6215) → релиз
https://github.com/ArrivaRUS/fb2-to-epub/releases/tag/v1.0.0 · DMG sha256
`2db58c9a…3282b519` (сверен повторным скачиванием, байт-в-байт).** «Выкладывай» человека
получено 2026-07-22; человек проверяет В БОЮ (на его ноуте Calibre нет — настоящий онбординг
через «Проверить обновление» из 0.9.8). Память проекта (plans/status/arch/design/.patches/
decisions/HEARTBEAT) в публичный репо НЕ коммитится — паттерн прежних релизов.
Все милейстоны CAL-1…CAL-5 закрыты. Финальная регрессия: **509/0** (12 наборов).

## Done (доки)
- [x] README RU/EN — «движок ставится сам», uninstall-примечание, паритет 1-в-1 (177 строк).
- [x] release-notes-v1.0.0.md — тело GitHub-релиза «работает из коробки».
- [x] **Фикс sudden-termination СДАН + перепроверен Юркой живьём** (2026-07-22): оба ключа
  NSSupports*Termination удалены из plist-шаблона build-app.sh, grep-страж 8→10 (негатив
  проверен). Сборка GREEN, plutil: ключей нет. **D40-диалог ЖИВОЙ:** на 62% скачивания
  «Завершить» из меню → алерт «Установка движка ещё идёт» ([Продолжить установку] default /
  [Прервать и выйти]), скачивание продолжает идти ПОД диалогом → «Продолжить» → установка
  доехала → агент активен. Уроки записаны: `.patches/017` (plist-бойлерплейт отрезал
  делегатный lifecycle; правила: осознанные plist-ключи, lifecycle только живым бинарём,
  страж, RU-раскладка vs синтетический cmd+q) и `.patches/018` (класс 015→B1→H1 — долг
  процесса: снапшот-страж боевых артефактов в раннерах, задача после релиза).
- [x] **Live-verify Юрки (5.2) — ПОЛНОСТЬЮ ЗЕЛЁНЫЙ** (все сценарии, вкл. D40-диалог).
- [x] **Debugger: корень «тихого Quit» НАЙДЕН и доказан A/B** (2026-07-22):
  `NSSupportsSuddenTermination=true` в plist-шаблоне `build/build-app.sh:224` (лежит с ПЕРВОГО
  коммита нативного UI 4691e55, бойлерплейт) → AppKit на terminate:/quit-AppleEvent делает exit()
  НЕ ВЫЗЫВАЯ applicationShouldTerminate → вся D40-ветка (диалог/terminateLater/зачистка) была
  мертворождённой во всех сборках. Прогон A (ключ true): terminate → смерть без входа в делегат;
  прогон B (тот же бинарь, ключ NO): вход в делегат → «D40 branch — showing alert» → алерт висит,
  приложение живо. Гипотезы «не та фаза/второй store/runModal-мгновенный-возврат» опровергнуты.
  Инструментирование откачено (grep DIAG = 0), боевое не тронуто.
- [x] **Live-verify Юрки (5.2) — остальное ЗЕЛЕНО** (2026-07-22, сборка 1.0.0-dev с меню):
  пиксель-фиксы П2/П3/П4 подтверждены side-by-side (кольцо-градиент, danger-точка, точка-бейдж,
  semibold-акценты); Направление A отснято и сверено (5 состояний, вкл. errnet foot-dot danger);
  happy path вживую (тап→прогресс→активация→агент активен→блокер ушёл); Отмена ✓;
  D40 закрытие окна ✓ (установка доехала в фоне: движок+plist на месте; reopen показал готовое);
  2-сек success-экран пойман живьём; главное меню в баре («Правка»/«Окно», Cmd-C/V для полей).
  Находки по пути: (а) фикстура < санити-порога 50 МБ → харнесс печёт 55 МБ (порог проверен
  живьём: маленький DMG честно давал .error(.network)); (б) сервер замедлен до ~2 МБ/с;
  (в) синтетический cmd+q от computer-use не матчится (RU-раскладка/CGEvent) — проверка через
  пункт меню, физический Cmd-Q человека проверить на демо.
- [x] **Цикл правок №2 СДАН**: H1 — 4 ключа изоляции в plist строго под защёлкой, боевой plist
  байт-в-байт (негатив-тест all-4-absent); mtime/size/хэш боевых лога и covers/jobs до/после
  полного прогона — НЕ изменились (H1 закрыт с байтовым доказательством). Хвосты: cleanupLeftovers
  восстанавливает валидный .old при отсутствующем calibre.app; Setup-кнопки под onboardingAction;
  bash-близнец латча отвергает «/» и passwd-home (parity 9/9, +T9); e2e-trap detach с ретраями.
  Тесты: install **134/134**, остальное зелёное, сборка rc=0. Watcher не трогали (уже уважал env).
- [x] Ре-ревью: фиксы №1 — все ✓; e2e-скрипт — PASS по изоляции; НО найдена 🟠 H1 (тест-агент
  реального bootstrap работает с боевым HOME — launchd не наследует env; следы на диске: тестовые
  строки в реальном логе). На этом ноуте вреда нет (боевого агента не существует), но дыра класса
  урока 015 в харнесах → чинить ДО live-verify.
- [x] Цикл правок №1 СДАН: все 11 находок закрыты; install-тесты 99→**115/115** (+16: S1-откат,
  halfCocked-гейт), локатор 44→**47/47** (+C6/C7 защёлка), сборка EXIT 0 / 0 warnings.
- [x] `tests/run-calibre-live-e2e.sh` написан (задача 5.1): dry-run 17/17, изоляция
  верифицирована (боевой App Support байт-в-байт цел). Живой запуск — после цикла №2.

## Next
1. Фиксы по ревью (если будут) → **живой e2e** (RUN_LIVE=1, один раз, ~330 МБ).
2. Live-verify Юрки руками (computer-use по `tests/run-calibre-ui-fixture.sh`): happy / D40-сценарии
   (закрыть окно, Dock, Cmd-Q-диалог) / отмена / manual / Setup-ветка.
3. Пиксель-ревью design-reviewer (учесть D44: 2 текста сверять по решению, не по ref-PNG).
4. README RU/EN + changelog (tech-writer, changelog-writer) → ⛔ СТОП-точка человека → релиз.
5. UX-развилка на живом показе: после success из Setup-ветки человек оказывается на Status
   (не на зелёном Setup) — подтвердить/поменять по ощущению вживую.

## Решения (канон — decisions/log.md)
- D37 гибрид блокер/баннер (по СЫРОМУ snapshot, не loadState) · D38 CTA «Установить Calibre» ·
  D39 установка в `~/Library/Application Support/fb2-to-epub/calibre.app` ·
  D40 фоновая установка при закрытом окне · D41 диск 1,5 ГБ / текст «~1 ГБ» ·
  D42 macOS < 14 → manual-only.
- Синтез-вливания Codex: cover-finder на env `EBOOK_META`; plist несёт `CALIBRE_MACOS_DIR`+`EBOOK_*`;
  `agentActivationFailed` — отдельное состояние + «Повторить запуск агента»; разделение
  `blockedNoCalibre` → `needsEngine`/`agentSetupFailed`; `codesign --verify --strict`;
  `LSMinimumSystemVersion`-чек после mount; `hdiutil -plist`-парсинг; hardcode-grep-тест;
  защёлка `FB2_CALIBRE_TEST_MODE=1` + `FB2_CALIBRE_TEST_ROOT`.
- Отброшено (не реанимировать без нового решения): transaction-journal, homebrew/PATH-кандидаты,
  6-state RuntimeHealth, renameatx_np, смена `didShowSetup`, докачка/resume, GPG, легаси-пиннинг.

## Допущения (проверяемые)
- `NSHomeDirectory()` уважает `$HOME` (unsandboxed) — проверяется первым шагом fixture-теста CAL-3;
  нет → харнесс через инжект `home:` в тест-бинарь.
- `xcrun swiftc -typecheck app/*.swift` как быстрый чек — может не взлететь на top-level main.swift;
  тогда валидировать `build/build-app.sh`.
- `/dist/osx` и `/signatures/` живы в форме ресёрча — проба CAL-3.0 до кода конвейера.

## Команды (copy-paste)
```sh
cd "/Users/arrivarus/Documents/VibeCoding2/2026.06 fb2-to-epub"
build/build-app.sh 0.10.0-dev                 # полная сборка приложения
tests/run-clear-history-tests.sh              # регрессия: baselines/история (104)
tests/run-cover-job-tests.sh                  # регрессия: cover-джобы (40)
tests/run-fb3-tests.sh                        # регрессия: FB3-трансформ (25)
tests/run-fb2-regression-test.sh              # регрессия: FB2-путь (20)
tests/run-sticky-batch-test.sh                # регрессия: липкая пачка (30)
tests/run-update-install-test.sh              # регрессия: авто-апдейт приложения
tests/run-cover-edit-test.sh                  # регрессия: правка метаданных
# новые (появятся в CAL-1/CAL-3/CAL-5):
tests/run-calibre-locator-tests.sh
tests/run-calibre-locator-parity.sh
tests/run-calibre-hardcode-grep.sh
tests/run-calibre-install-tests.sh
RUN_LIVE=1 tests/run-calibre-live-e2e.sh      # ЖИВОЙ e2e — 1 раз перед релизом (урок 016)
```

## Блокеры
- Нет. Дизайн-спец принята (D43, 2026-07-21) — CAL-2/CAL-4 разблокированы.
- Учти для CAL-5: на машине разработки Calibre **отсутствует** (проверено `mdfind`, 2026-07-21) —
  это идеальные условия для живого e2e автоустановки (догфудинг), но любой тест, требующий
  реального `ebook-convert`, до установки движка не пройдёт.

## Smoke / demo-проверки (после каждого milestone)
- Машина С Calibre в /Applications: экраны не изменились, конвертация живёт.
- Изолированный запуск без движка (защёлка + throwaway HOME): онбординг виден, состояния листаются
  через `FB2_FORCE_INSTALL_STATE`.

## Аудит-лог
- 2026-07-19 · architect-claude: независимый план (`arch/plan-calibre-onboarding-claude.md`).
- 2026-07-19 · Юрка: суд двух архитекторов → синтез; человек решил развилки → D40–D42.
- 2026-07-19 · architect-claude: заархивирован июньский pack (`arch/archive-2026-06-dmg-installer/`);
  собран новый execution-pack (plans/status/test-plan). Разработка не начата, код не менялся.
- 2026-07-21 · Юрка: человек вернулся к проекту (заметил дыру в CJM на новом ноуте) → показ
  финальных макетов в Safari → **G3 принят (D43)**; запущен developer на CAL-1. Старт разработки.
- 2026-07-21 · developer: **CAL-1 сдан** — locator 44/44 · parity 8/8 · grep 8/8 + вся регрессия
  зелёная (104/40/20/30/23/51/11); Swift-сборка 0 warnings. Отступления: FB2_AGENT_LABEL под полной
  защёлкой (строже плана, урок 015); +read-only `FB2_CALIBRE_DETECT_ONLY=1` в installer.sh для parity.
- 2026-07-21 · Юрка: **сборка иконки починена** (поломка переезда): `brew install cairo` +
  пересоздан `build/.venv`; корень — venv в пути с ПРОБЕЛОМ делает bin/cairosvg `#!/bin/sh`-обёрткой,
  SIP вырезает DYLD_* при exec sh → фикс в build-app.sh: `python3 -m cairosvg` + export
  DYLD_FALLBACK_LIBRARY_PATH. Рендер сверен с иконкой отгруженного v0.9.8 — пиксель-в-пиксель.
  Человек выбрал «вернуть как было» (без перевода иконки на нативный рендер).
- 2026-07-21 · Юрка: CAL-2 и CAL-3 запущены ПАРАЛЛЕЛЬНО (два developer, эксклюзивные списки файлов;
  CAL-2 рисует display-only Phase-enum сам, сшивка с InstallStore — CAL-4).
- 2026-07-21 · developer: **CAL-3 сдан** — `CalibreInstaller.swift` (headless) + fixture-DMG харнесс,
  **65/65** (happy+идемпотентность, битый sha, no-app, verify-fail, no-space, отмена, LSMin>OS,
  защёлка-выкл, root-вне-TEST_ROOT, OS-гейт D42, cleanupLeftovers, re-entry guard). Регрессия CAL-1
  зелёная. Пробы 3.0 вживую: `/dist/osx` → 302 → GitHub v9.11.0 (~328,5 МБ), sha512-сайдкар 200/128hex,
  значение совпало с ресёрчем. Тест-прогресс — троттленный localhost-HTTP (file:// риск снят).
  Решение Юрки по ремарке dev: GitHub-API фолбэк версии (план 3.6) ОСТАВЛЕН — `api.github.com`
  вынесен в отдельную metadata-only константу (не в allow-хосты бинарей); это буква принятого плана.
  `CalibreInstaller.swift` пока НЕ в SWIFT_SRCS build-app.sh — добавить при сшивке CAL-4.
- 2026-07-21 · developer: **CAL-2 сдан** — hasRawHistory (113/113 clear-history, вкл. D37-гард
  F5 «сброс не превращает баннер в блокер»), честный бейдж/футер, `EngineSetupCard.swift`
  (10 фаз × 4 подачи, display-only), гибрид в StatusView, янтарный Setup, строка Настроек,
  `FB2_FORCE_INSTALL_STATE`. Sticky 30/30, CAL-1-гарды PASS, **build-app.sh 0.10.0-dev GREEN**
  (иконка починена). 14 скриншотов состояний сняты изолированно (защёлка, боевой агент не тронут).
- 2026-07-21 · Юрка (visual-verify скриншотов CAL-2): блокер B / баннер A / Setup — по макетам;
  подтверждены 2 текст-вопроса → человеку: (а) sub Настроек обрезается «…без нег…»;
  (б) «Не закрывай окно, пока идёт загрузка» в баннере downloading противоречит D40
  (закрытие БЕЗОПАСНО — макет писался до решения D40). Версия «0.10.0-…» в Настройках
  обрезается только из-за dev-суффикса — в релизе «0.10.0» влезает, не баг.
- 2026-07-21 · человек: **D44** — sub Настроек = коротко «движок конвертации»; downloading-текст =
  «Окно можно закрыть — загрузка продолжится.» Записано в decisions/log.md + design-spec §0 (п.9–10).
- 2026-07-21 · developer: **CAL-4 сдан** — активация через installer.sh (bootout→bootstrap→enable→
  kickstart, plist перепечён на app-owned, агент реально поднят под тест-меткой), agentActivationFailed
  + «Повторить запуск агента» (без скачивания), success→2с→present, колбэки во всех 4 подачах,
  D40-lifecycle (NSAlert на Cmd-Q в download, terminateLater в installing), D44-тексты применены.
  **build GREEN**; install-тесты **99/99** (65+34), гарды/регрессия зелёные. Новый однокомандный
  live-UI харнесс `tests/run-calibre-ui-fixture.sh` (для live-verify Юрки; пример в plans.md CAL-4
  Validation битый — file:// без прогресса, использовать харнесс). Заметки: launchd-остатки
  `.test.*` инертны (2 записи, чистятся только sudo); precheck-вспышка «0 из 0 МБ» — на дизайн-ревью;
  UX-развилка Setup→Status — на живой показ.
- 2026-07-21 · code-reviewer (Claude + Codex gpt-5.5): **НУЖНЫ ПРАВКИ**. 🔴 B1 ui-fixture-харнесс:
  HOME вне TEST_ROOT → защёлка мутаций молча падала → live-verify Юрки скачал бы прод-330МБ и
  ВЫГРУЗИЛ боевой агент (класс урока 015; пойман ДО запуска харнесса — предохранитель сработал).
  🟠 S1 сбой 2-го rename в свопе теряет рабочий движок (`.old` добит cleanup'ом на старте);
  🟠 S2 «Отмена» в Настройках активна в installing и no-op (класс патча 005). 🟡 m1–m8 (битый
  `${…:+}`-запуск харнесса, TEST_ROOT=/-дыра защёлки, isUpdateInFlight не провязан, прогресс без
  троттла, живые кнопки в FORCE_INSTALL_STATE, .venv не в gitignore, редиректы fetchText без гейта,
  cancel в precheck). Codex нашёл B1/S1/m1 (совпадение с Claude по S1 независимое), пустышек нет.
  Инварианты 1–10: OK, кроме п.6-в-харнессе (=B1). Решения Юрки: громкий отказ при полузведённой
  защёлке (дополнение п.6), все m-фиксы — делать. Запущен цикл правок №1.
- 2026-07-21 · developer: **e2e-скрипт 5.1 готов** (`run-calibre-live-e2e.sh` + Swift-драйвер;
  прод-код не тронут). Dry-run **17/17**; изоляция доказана snapshot'ом (реальный App Support
  байт-в-байт цел, 0 launchd-остатков финальной версией). Решения Юрки по развилкам dev:
  (1) активация в e2e — через launchctl-шайбу (installer.sh проходит целиком, реальный launchd
  не мутируется) — ПРИНЯТО: настоящий bootstrap под тест-меткой уже доказан живьём в CAL-4
  (`agentStatus().isActive`), уникальная ценность e2e — реальные DMG/codesign/quarantine/движок,
  все звенья закрыты по-настоящему хотя бы в одном прогоне; вариант с реальным bootstrap НЕ нужен.
  (2) фикстурный .fb2 генерится инлайн (статичного в tests/ нет — политика репо «на лету») — ОК.
  (3) ⚠️ перед живым запуском проверить стабильность `CalibreInstaller.swift` (дев поймал его
  посреди параллельной правки m7) — живой e2e гнать только ПОСЛЕ финиша цикла правок + ре-ревью.

---
## v1.0.2 боевой инцидент → доведение хвостов (2026-07-25, после рестарта Claude)
- **АВТОРИТЕТНЫЙ УСПЕХ:** после ручного репойнта агента на helper — folder_access:ok, книга
  `Mannergeym…Memuaryi.117444` сконвертирована БЕЗ гранта (человек отменил все FDA) → фикс v1.0.2
  доказан вживую; non-platform helper читает Desktop без FDA. Юрка репойнтнул боевой plist руками
  (бэкап `…agent.plist.pre-migrate.bak`).
- **Хвост 1 (в разборе debugger):** авто-апдейт не репойнтнул plist сам (агент остался на runner.sh).
  Гипотеза — артефакт BR-1 (предзаложенный helper → нет diff → installer не запущен); watcher.sh
  в v1.0.2 не менялся. Debugger моделирует ЧИСТЫЙ апдейт → артефакт vs реальный баг (→ v1.0.3?).
- **Хвост 2 (в разборе):** кнопка «скопировать путь» не кладёт в буфер (FolderAccessCard/main.swift).
- Среда восстановлена рестартом Claude (доступ к ~/Documents + субагенты вернулись).
- HANDOFF-v1.0.2-tails.md влит сюда → можно удалить.

## 🎉 v1.0.3 ВЫПУЩЕН 2026-07-25 («да, выкладывай» человека)
PR #29 (merge 98c66a0, 10 файлов, +750/-68) → https://github.com/ArrivaRUS/fb2-to-epub/releases/tag/v1.0.3
DMG sha256 `b9fba0bd…4e0d605e` (сверен повторным скачиванием; 1-я попытка упала на сетевом EOF, 2-я ок).
Helper в бандле байт-в-байт `926fc039…` — **выданный FDA-грант остаётся в силе**.
**Содержание:** (1) самолечение настройки агента — refresh гонит идемпотентный installer не только
при байт-диффе engine-файлов, но и когда PA0 ≠ helper (PA0 читается напрямую plutil, НЕ через
runnerPath() — fallback замаскировал бы протухший таргет); PA0-ветка загейчена calibreInstalled()
(без движка installer падает до репойнта → петли на каждом старте нет). (2) `installedRunnerPA0()`
трёхзначно (helper/не-helper/нет-значения) + `installedRunnerIsStale()` = «PA0 ЕСТЬ И ≠ helper» —
не запрещает копирование при отсутствующем/битом plist. (3) dead-path guard: протухший PA0 → НЕ
копируем мёртвый путь (обе дороги: .refreshFailed И .upToDate-без-Calibre), честный хинт вместо
молчания. (4) ack «Путь скопирован ✓» только при реальном setString и только с CTA карточки
(кросс-экранный флип из Настроек убран). (5) шаг 2 не обещает буфер при показанном хинте.
(6) строка FDA в Настройках теперь копирует путь.
**Проверки:** регрессия **584/0** (Юрка прогнал независимо; changelog поправлен с 493→584),
clear-history 160→**186/0** (+E6/E7/E8 +CT1–CT6), ревью Claude+Codex 2 раунда → PASS,
**зубы тестов доказаны 3 мутациями** (включая ловушку over-guard из оговорки ревьюера).
Боевой сценарий человека (PA0=helper) запинен тестом CT6: самолечение = no-op, копирование как раньше.
**Долг процесса (CLAUDE.md §2):** FDA/stale-агент узел чинился 3 релиза подряд (1.0.1→1.0.2→1.0.3) —
каждый раз находка приходила из БОЕВОГО теста человека, не из тестов. Урок 021 (ниже) — про это.
